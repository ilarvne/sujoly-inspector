{
  "name": "agent",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Python build handled by uv'",
    "dev": "uv run python src/agent/server.py",
    "serve": "uv run python src/agent/server.py",
    "test": "uv run pytest",
    "lint": "uv run ruff check .",
    "codegen": "buf generate --template buf.gen.yaml ../../libs/shared-protos"
  }
}
