---
phase: 01-foundation-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - apps/api/alembic.ini
  - apps/api/alembic/env.py
  - apps/api/alembic/versions/0001_initial.py
  - apps/api/src/api/models/__init__.py
  - apps/api/src/api/models/provenance.py
  - apps/api/src/api/models/structure.py
  - apps/api/src/api/services/__init__.py
  - apps/api/src/api/services/minio_client.py
  - apps/api/src/api/services/provenance_service.py
  - apps/api/src/api/routes/provenance.py
  - apps/api/src/api/routes/minio.py
  - apps/api/src/api/main.py
  - apps/api/tests/test_provenance.py
  - apps/api/tests/test_minio.py
  - apps/api/tests/test_schema.py
autonomous: true
requirements:
  - DATA-07
  - INT-04
must_haves:
  truths:
    - "Developer creates a provenance record via POST /api/v1/provenance and retrieves it via GET /api/v1/provenance/{id}"
    - "Developer queries provenance by source_type, confidence_level, and timestamp range via GET /api/v1/provenance"
    - "Developer generates a presigned upload URL via POST /api/v1/minio/presign and a presigned download URL via GET /api/v1/minio/presign/{object_name}"
    - "Structure features are stored in PostGIS with Geometry(Point, srid=4326) — no binary data in PostgreSQL"
    - "Imagery evidence is stored in MinIO buckets (sujoly-imagery, sujoly-documents, sujoly-photos) — separate from PostGIS"
    - "Every structure record has a provenance_id foreign key referencing the provenance table"
    - "Every structure fact has its own provenance_id foreign key — facts are individually traceable"
  artifacts:
    - path: "apps/api/src/api/models/provenance.py"
      provides: "ProvenanceModel ORM — source_type, source_reference, confidence_level, contributor, recorded_at"
      exports: ["ProvenanceModel"]
      contains: "__tablename__ = .provenance."
    - path: "apps/api/src/api/models/structure.py"
      provides: "StructureModel with PostGIS Geometry + StructureFactModel with JSONB"
      exports: ["StructureModel", "StructureFactModel"]
      contains: "Geometry"
    - path: "apps/api/src/api/services/minio_client.py"
      provides: "MinIOService — ensure_bucket, presigned_upload_url, presigned_download_url"
      exports: ["MinIOService"]
    - path: "apps/api/src/api/services/provenance_service.py"
      provides: "Provenance CRUD service — create, get, query"
      exports: ["create_provenance", "get_provenance", "query_provenance"]
    - path: "apps/api/src/api/routes/provenance.py"
      provides: "Provenance REST endpoints"
      exports: ["router", "ProvenanceCreate", "ProvenanceResponse"]
    - path: "apps/api/src/api/routes/minio.py"
      provides: "MinIO presigned URL endpoints"
      exports: ["router", "PresignUploadRequest", "PresignResponse"]
    - path: "apps/api/alembic/versions/0001_initial.py"
      provides: "Initial migration creating provenance, structures, structure_facts tables"
      contains: "create_table"
  key_links:
    - from: "apps/api/src/api/routes/provenance.py"
      to: "apps/api/src/api/services/provenance_service.py"
      via: "service function calls"
      pattern: "create_provenance|get_provenance|query_provenance"
    - from: "apps/api/src/api/routes/minio.py"
      to: "apps/api/src/api/services/minio_client.py"
      via: "request.app.state.minio"
      pattern: "app.state.minio"
    - from: "apps/api/src/api/models/structure.py"
      to: "apps/api/src/api/models/provenance.py"
      via: "ForeignKey provenance.id"
      pattern: "ForeignKey.*provenance.id"
    - from: "apps/api/src/api/main.py"
      to: "apps/api/src/api/routes/provenance.py"
      via: "app.include_router"
      pattern: "include_router.*provenance"
---

<objective>
Deliver the two Phase 1 requirements: provenance tracking (DATA-07) and architecture separation proof (INT-04). Creates the PostGIS schema with provenance on all structure records, provenance CRUD API endpoints, MinIO presigned URL endpoints, and Alembic migration infrastructure.

Purpose: Provenance tracking is the core architecture principle — every fact has a source, confidence, and timestamp. Architecture separation proves binary assets live in MinIO while vector features live in PostGIS. Together these establish the data contracts all subsequent phases build on.

Output: Alembic migration creating provenance + structures + structure_facts tables, provenance CRUD REST endpoints, MinIO presigned URL endpoints, MinIOService class, and integration tests proving both requirements.
</objective>

<execution_context>
@/home/lain/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lain/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/STATE.md
@.planning/workstreams/backend/ROADMAP.md
@.planning/workstreams/backend/REQUIREMENTS.md
@.planning/workstreams/backend/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/workstreams/backend/phases/01-foundation-infrastructure/01-PATTERNS.md
@.planning/workstreams/backend/phases/01-foundation-infrastructure/01-VALIDATION.md

# Plan 01 output — the FastAPI app, settings, database infrastructure, and Docker stack
@.planning/workstreams/backend/phases/01-foundation-infrastructure/01-01-SUMMARY.md

# Analog source files
@apps/agent/src/agent/routes/agent.py
@apps/agent/src/agent/infrastructure/thread_ownership.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic setup + ORM models + initial migration</name>
  <files>
    apps/api/alembic.ini,
    apps/api/alembic/env.py,
    apps/api/alembic/versions/0001_initial.py,
    apps/api/src/api/models/__init__.py,
    apps/api/src/api/models/provenance.py,
    apps/api/src/api/models/structure.py
  </files>
  <read_first>
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-RESEARCH.md (lines 206-288 provenance schema pattern with full model definitions, lines 471-490 Alembic GeoAlchemy2 helpers, lines 492-499 anti-patterns for create_all vs Alembic),
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-PATTERNS.md (models/provenance.py pattern lines 154-192, models/structure.py pattern lines 196-210, database.py Base import pattern),
    apps/api/src/api/infrastructure/database.py (Plan 01 output — Base, engine, async_session to import),
    apps/api/src/api/config/settings.py (Plan 01 output — sync_database_url for Alembic)
  </read_first>
  <action>
    Create the Alembic migration infrastructure and ORM models for provenance tracking and structure storage.

    1. **apps/api/alembic.ini**: Standard Alembic config. `script_location = alembic`, `sqlalchemy.url` will be overridden in env.py from settings. Set `file_template = %%(year)d_%%(month).2d_%%(day).2d_%%(hour).2d%%(minute).2d-%%(rev)s_%%(slug)s` for readable migration filenames. Set `prepend_sys_path = .` and `version_path_separator = os`.

    2. **apps/api/alembic/env.py**: Configure Alembic to use sync database URL from settings. Import `from api.config.settings import settings` and set `config.set_main_option("sqlalchemy.url", settings.sync_database_url)`. Import `from api.infrastructure.database import Base` and `import api.models.provenance` + `import api.models.structure` to register models on Base.metadata. Set `target_metadata = Base.metadata`. CRITICAL: Import and use GeoAlchemy2 alembic helpers (Pitfall #5): `from geoalchemy2 import alembic_helpers`, then in `context.configure()` add `include_object=alembic_helpers.include_object`, `process_revision_directives=alembic_helpers.writer`, `render_item=alembic_helpers.render_item`. Use psycopg sync driver (settings.sync_database_url uses `postgresql://` not `postgresql+asyncpg://`).

    3. **apps/api/src/api/models/provenance.py**: SQLAlchemy 2.0 ORM model using `Mapped` + `mapped_column` (NOT legacy `Column()`). Import `from api.infrastructure.database import Base`. `class ProvenanceModel(Base)` with `__tablename__ = "provenance"`. Fields per RESEARCH.md lines 223-239: `id: Mapped[uuid.UUID] = mapped_column(Uuid, primary_key=True, default=uuid.uuid4)`, `source_type: Mapped[str] = mapped_column(String(50), nullable=False, index=True)` (values: kazvodhoz_spreadsheet, osm, satellite, ocr, manual, ai_inferred, inspection), `source_reference: Mapped[str | None] = mapped_column(Text, nullable=True)` (URL, file path, OSM element ID, satellite scene ID), `confidence_level: Mapped[str] = mapped_column(String(10), nullable=False, default="HIGH")` (HIGH, MEDIUM, LOW), `contributor: Mapped[str | None] = mapped_column(String(255), nullable=True)`, `recorded_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=datetime.utcnow)`. Add a CheckConstraint for confidence_level IN ('HIGH', 'MEDIUM', 'LOW').

    4. **apps/api/src/api/models/structure.py**: Two ORM models. Import `from api.infrastructure.database import Base`, `from geoalchemy2 import Geometry`, `from sqlalchemy.dialects.postgresql import JSONB`. `class StructureModel(Base)` with `__tablename__ = "structures"`: `id: Mapped[uuid.UUID] = mapped_column(Uuid, primary_key=True, default=uuid.uuid4)`, `name_ru: Mapped[str | None] = mapped_column(String(500), nullable=True)`, `name_kk: Mapped[str | None] = mapped_column(String(500), nullable=True)`, `name_en: Mapped[str | None] = mapped_column(String(500), nullable=True)`, `type: Mapped[str] = mapped_column(String(100), nullable=False)`, `geometry = mapped_column(Geometry("Point", srid=4326), nullable=False)`, `provenance_id: Mapped[uuid.UUID] = mapped_column(Uuid, ForeignKey("provenance.id"), nullable=False)`, `created_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=datetime.utcnow)`, `updated_at: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=datetime.utcnow, onupdate=datetime.utcnow)`. `class StructureFactModel(Base)` with `__tablename__ = "structure_facts"`: `id: Mapped[uuid.UUID] = mapped_column(Uuid, primary_key=True, default=uuid.uuid4)`, `structure_id: Mapped[uuid.UUID] = mapped_column(Uuid, ForeignKey("structures.id"), nullable=False, index=True)`, `attribute_name: Mapped[str] = mapped_column(String(100), nullable=False)` (condition, capacity_m3s, length_km, wear_percent, etc.), `attribute_value: Mapped[dict] = mapped_column(JSONB, nullable=False)`, `provenance_id: Mapped[uuid.UUID] = mapped_column(Uuid, ForeignKey("provenance.id"), nullable=False)`, `valid_from: Mapped[datetime] = mapped_column(DateTime(timezone=True), nullable=False, default=datetime.utcnow)`, `valid_to: Mapped[datetime | None] = mapped_column(DateTime(timezone=True), nullable=True)` (NULL = currently valid).

    5. **apps/api/src/api/models/__init__.py**: Barrel export importing both models so Alembic can discover them: `from api.models.provenance import ProvenanceModel`, `from api.models.structure import StructureModel, StructureFactModel`. Set `__all__ = ["ProvenanceModel", "StructureModel", "StructureFactModel"]`.

    6. **apps/api/alembic/versions/0001_initial.py**: Write the initial migration MANUALLY (do not use autogenerate — it breaks with GeoAlchemy2). Migration revision ID `0001`, down_revision `None`. `upgrade()` function: create `provenance` table with all columns + index on source_type + check constraint on confidence_level. Create `structures` table with all columns + foreign key to provenance.id + GiST spatial index on geometry column (use `op.execute("CREATE INDEX ... USING GIST ...")` or GeoAlchemy2 helper). Create `structure_facts` table with all columns + foreign keys to structures.id and provenance.id + index on structure_id. `downgrade()` function: drop tables in reverse order (structure_facts, structures, provenance). Use `from alembic import op` and `import sqlalchemy as sa`. For the geometry column, use `sa.Column("geometry", Geometry("Point", srid=4326), nullable=False)` — import `from geoalchemy2 import Geometry` at top of migration file.
  </action>
  <verify>
    <automated>cd /home/lain/Development/hackday/apps/api && uv run alembic check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/alembic/env.py contains `from geoalchemy2 import alembic_helpers` and `alembic_helpers.include_object` and `alembic_helpers.writer` and `alembic_helpers.render_item`
    - apps/api/alembic/env.py contains `target_metadata = Base.metadata` and `settings.sync_database_url`
    - apps/api/src/api/models/provenance.py contains `__tablename__ = "provenance"`, `source_type`, `confidence_level`, `recorded_at`, `mapped_column`
    - apps/api/src/api/models/structure.py contains `Geometry("Point", srid=4326)`, `ForeignKey("provenance.id")`, `JSONB`, `StructureFactModel`
    - apps/api/alembic/versions/0001_initial.py contains `create_table` for provenance, structures, structure_facts and `drop_table` in downgrade
    - No file uses `Base.metadata.create_all` — schema is managed by Alembic only
    - No file uses legacy `Column()` style — all use `Mapped` + `mapped_column`
  </acceptance_criteria>
  <done>
    Alembic is configured with GeoAlchemy2 helpers. Three ORM models (ProvenanceModel, StructureModel, StructureFactModel) define the provenance tracking schema. Initial migration creates all three tables with proper foreign keys, spatial index, and check constraints. `alembic check` passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provenance service + MinIO service + REST endpoints + tests</name>
  <files>
    apps/api/src/api/services/__init__.py,
    apps/api/src/api/services/minio_client.py,
    apps/api/src/api/services/provenance_service.py,
    apps/api/src/api/routes/provenance.py,
    apps/api/src/api/routes/minio.py,
    apps/api/src/api/main.py,
    apps/api/tests/test_provenance.py,
    apps/api/tests/test_minio.py,
    apps/api/tests/test_schema.py
  </files>
  <read_first>
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-RESEARCH.md (lines 300-322 MinIOService class, lines 213-288 provenance schema for service operations, lines 629-686 health endpoint for route pattern reference),
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-PATTERNS.md (services/provenance_service.py pattern lines 321-370, routes/provenance.py pattern lines 273-306, routes/minio.py pattern lines 309-317, main.py route registration lines 449-455),
    apps/api/src/api/main.py (Plan 01 output — add route registration here),
    apps/api/src/api/models/provenance.py (Task 1 output — ProvenanceModel for service operations),
    apps/api/src/api/routes/health.py (Plan 01 output — route pattern reference),
    apps/agent/src/agent/infrastructure/thread_ownership.py (async DB service pattern — create with idempotent check, query pattern),
    apps/agent/src/agent/routes/agent.py (router declaration, Pydantic request/response models, endpoint error handling pattern)
  </read_first>
  <behavior>
    - Test 1: POST /api/v1/provenance with body {source_type: "kazvodhoz_spreadsheet", confidence_level: "HIGH", contributor: "ingest_pipeline"} returns 201 with id (UUID), source_type, confidence_level, contributor, recorded_at (ISO timestamp)
    - Test 2: GET /api/v1/provenance/{id} with valid UUID returns 200 with full provenance record matching the created one
    - Test 3: GET /api/v1/provenance/{id} with non-existent UUID returns 404
    - Test 4: GET /api/v1/provenance?source_type=kazvodhoz_spreadsheet returns 200 with list filtered by source_type
    - Test 5: GET /api/v1/provenance?confidence_level=HIGH returns 200 with list filtered by confidence_level
    - Test 6: POST /api/v1/minio/presign with body {bucket: "sujoly-documents", object_name: "test/file.pdf"} returns 200 with presigned_url (string, non-empty)
    - Test 7: GET /api/v1/minio/presign/test/file.pdf returns 200 with presigned_url (string, non-empty, different from upload URL)
    - Test 8: test_schema — structures table has geometry column of type Geometry (test_geometry_in_postgis)
    - Test 9: test_schema — no bytea or binary large object columns in structures or structure_facts tables (test_no_binary_in_postgis)
  </behavior>
  <action>
    Create the service layer, REST endpoints, and integration tests for provenance CRUD and MinIO presigned URLs. Wire new routes into the existing FastAPI app.

    1. **apps/api/src/api/services/minio_client.py**: `class MinIOService` per RESEARCH.md lines 300-322. Constructor takes `endpoint: str, access_key: str, secret_key: str, secure: bool` and creates `self.client = Minio(endpoint, access_key=access_key, secret_key=secret_key, secure=secure)`. Method `ensure_bucket(self, bucket_name: str) -> None`: check `self.client.bucket_exists(bucket_name)`, if False call `self.client.make_bucket(bucket_name)`. Method `presigned_upload_url(self, bucket: str, object_name: str, expires: timedelta = timedelta(hours=1)) -> str`: return `self.client.presigned_put_object(bucket, object_name, expires=expires)`. Method `presigned_download_url(self, bucket: str, object_name: str, expires: timedelta = timedelta(hours=2)) -> str`: return `self.client.presigned_get_object(bucket, object_name, expires=expires)`. Import `from minio import Minio` and `from datetime import timedelta`.

    2. **apps/api/src/api/services/provenance_service.py**: Async service functions following the agent's thread_ownership.py pattern. Import `from api.infrastructure.database import async_session`, `from api.models.provenance import ProvenanceModel`, `from sqlalchemy import select`, `from sqlalchemy.ext.asyncio import AsyncSession`. Function `async def create_provenance(source_type: str, confidence_level: str, source_reference: str | None = None, contributor: str | None = None) -> ProvenanceModel`: open `async with async_session() as session: async with session.begin():` create `ProvenanceModel(...)`, `session.add(model)`, `await session.flush()`, `await session.refresh(model)`, return model. Function `async def get_provenance(provenance_id: uuid.UUID) -> ProvenanceModel | None`: query by id, return scalar_one_or_none(). Function `async def query_provenance(source_type: str | None = None, confidence_level: str | None = None, offset: int = 0, limit: int = 100) -> list[ProvenanceModel]`: build select query with optional `.where()` filters, add `.offset(offset).limit(limit)`, return `result.scalars().all()`.

    3. **apps/api/src/api/services/__init__.py**: Barrel export: `from api.services.minio_client import MinIOService` with `__all__ = ["MinIOService"]`.

    4. **apps/api/src/api/routes/provenance.py**: FastAPI APIRouter with `prefix="/api/v1", tags=["provenance"]`. Pydantic models: `ProvenanceCreate(BaseModel)` with `source_type: str` (Field with description listing valid values), `source_reference: str | None = None`, `confidence_level: str = "HIGH"` (Literal["HIGH", "MEDIUM", "LOW"]), `contributor: str | None = None`. `ProvenanceResponse(BaseModel)` with `id: uuid.UUID`, `source_type: str`, `source_reference: str | None`, `confidence_level: str`, `contributor: str | None`, `recorded_at: datetime` (use `model_config = ConfigDict(from_attributes=True)` for ORM compatibility). Endpoints: `POST /provenance` accepting `ProvenanceCreate`, calling `create_provenance()`, returning `ProvenanceResponse` with status 201. `GET /provenance/{provenance_id}` accepting UUID path param, calling `get_provenance()`, returning `ProvenanceResponse` or raising `HTTPException(404)`. `GET /provenance` accepting query params `source_type: str | None = None`, `confidence_level: str | None = None`, `offset: int = 0`, `limit: int = 100`, calling `query_provenance()`, returning `list[ProvenanceResponse]`.

    5. **apps/api/src/api/routes/minio.py**: FastAPI APIRouter with `prefix="/api/v1/minio", tags=["minio"]`. Pydantic models: `PresignUploadRequest(BaseModel)` with `bucket: str`, `object_name: str`. `PresignResponse(BaseModel)` with `presigned_url: str`, `expires_in_seconds: int`. Endpoints: `POST /presign` accepting `PresignUploadRequest`, accessing `request.app.state.minio` (MinIOService set in lifespan), calling `presigned_upload_url(bucket, object_name)`, returning `PresignResponse(presigned_url=url, expires_in_seconds=3600)`. `GET /presign/{object_name:path}` accepting object_name path param, using `settings.minio_bucket` as default bucket (or accept `bucket` query param), calling `presigned_download_url(bucket, object_name)`, returning `PresignResponse(presigned_url=url, expires_in_seconds=7200)`. Import `from api.config.settings import settings` for default bucket.

    6. **apps/api/src/api/main.py** (MODIFY — add route registration): Add imports `from api.routes import health, provenance, minio` (replacing the existing `from api.routes import health`). Add `app.include_router(provenance.router)` and `app.include_router(minio.router)` after the existing `app.include_router(health.router)`. Replace the inline MinIO client init in lifespan with `from api.services.minio_client import MinIOService` and use the service class (if Plan 01 used inline Minio, refactor to use MinIOService).

    7. **apps/api/tests/test_provenance.py**: Test class `TestProvenanceEndpoints` with tests matching behavior block tests 1-5. Mock `async_session` and provenance_service functions. Test create returns 201 with all fields. Test get by id returns 200. Test get non-existent returns 404. Test query by source_type returns filtered list. Test query by confidence_level returns filtered list.

    8. **apps/api/tests/test_minio.py**: Test class `TestMinIOEndpoints` with tests matching behavior block tests 6-7. Mock `app.state.minio` as a MagicMock MinIOService. Test POST /presign returns 200 with non-empty URL string. Test GET /presign/{object_name} returns 200 with non-empty URL string. Add `test_presigned_roundtrip` for SC-3: verify upload URL and download URL are different strings.

    9. **apps/api/tests/test_schema.py**: Tests for SC-4 (architecture separation). `test_geometry_in_postgis`: query `information_schema.columns` for structures table, assert geometry column exists with data type containing "geometry". `test_no_binary_in_postgis`: query `information_schema.columns` for structures and structure_facts tables, assert no column has data type "bytea" or "oid" or "blob". These tests require a running PostgreSQL with the migration applied — mark with `@pytest.mark.integration` and skip if DB unavailable.
  </action>
  <verify>
    <automated>cd /home/lain/Development/hackday/apps/api && uv run pytest tests/test_provenance.py tests/test_minio.py tests/test_schema.py tests/test_health.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/api/services/minio_client.py contains `class MinIOService`, `ensure_bucket`, `presigned_upload_url`, `presigned_download_url`, `presigned_put_object`, `presigned_get_object`
    - apps/api/src/api/services/provenance_service.py contains `async def create_provenance`, `async def get_provenance`, `async def query_provenance`
    - apps/api/src/api/routes/provenance.py contains `prefix="/api/v1"`, `POST` with status 201, `GET /provenance/{provenance_id}` with 404, query params `source_type`, `confidence_level`
    - apps/api/src/api/routes/minio.py contains `prefix="/api/v1/minio"`, `POST /presign`, `GET /presign/{object_name:path}`, `app.state.minio`
    - apps/api/src/api/main.py contains `include_router(provenance.router)` and `include_router(minio.router)` and `from api.services.minio_client import MinIOService`
    - apps/api/tests/test_provenance.py contains tests for create (201), get (200), get-nonexistent (404), query-by-source, query-by-confidence
    - apps/api/tests/test_minio.py contains tests for presign-upload, presign-download, presigned-roundtrip
    - apps/api/tests/test_schema.py contains `test_geometry_in_postgis` and `test_no_binary_in_postgis`
    - `uv run pytest tests/ -x` exits 0 (all tests pass including Plan 01 health tests)
  </acceptance_criteria>
  <done>
    Provenance CRUD API operational (POST creates, GET retrieves, GET queries by source_type/confidence). MinIO presigned URL endpoints operational (POST upload, GET download). All routes wired into FastAPI app. Integration tests prove DATA-07 (provenance tracking) and INT-04 (architecture separation — geometry in PostGIS, no binary in PostGIS, assets in MinIO). All tests pass.
  </done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces (Plan 02)

| Artifact | Type | Location | Consumers |
|----------|------|----------|-----------|
| `ProvenanceModel` | ORM model | `apps/api/src/api/models/provenance.py` | provenance_service, StructureModel FK, StructureFactModel FK, Alembic |
| `StructureModel` | ORM model | `apps/api/src/api/models/structure.py` | Phase 2 ingestion, TiPG, future CRUD routes |
| `StructureFactModel` | ORM model | `apps/api/src/api/models/structure.py` | Phase 3 risk models, inspection history |
| `MinIOService` class | Service | `apps/api/src/api/services/minio_client.py` | main.py lifespan, minio routes, Phase 3 document attachment |
| `create_provenance()`, `get_provenance()`, `query_provenance()` | Service functions | `apps/api/src/api/services/provenance_service.py` | provenance routes, Phase 2 ingestion, Phase 3 risk overrides |
| `router`, `ProvenanceCreate`, `ProvenanceResponse` | Route + DTOs | `apps/api/src/api/routes/provenance.py` | main.py, frontend API |
| `router`, `PresignUploadRequest`, `PresignResponse` | Route + DTOs | `apps/api/src/api/routes/minio.py` | main.py, frontend upload/download |
| `0001_initial` migration | Alembic migration | `apps/api/alembic/versions/0001_initial.py` | Alembic upgrade, all future migrations depend on this |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External client → POST /api/v1/provenance | Untrusted input — source_type, confidence_level strings cross here |
| External client → POST /api/v1/minio/presign | Untrusted input — bucket name, object_name strings cross here |
| FastAPI → PostgreSQL (provenance writes) | Internal Docker network. SQLAlchemy ORM parameterized queries. |
| FastAPI → MinIO (presigned URL generation) | Internal Docker network. MinIO SDK handles S3 signature v4. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | POST /api/v1/provenance (SQL injection) | mitigate | SQLAlchemy ORM parameterized queries — no raw SQL with string formatting. Pydantic v2 validation on ProvenanceCreate body (Literal["HIGH","MEDIUM","LOW"] for confidence_level, typed fields for all inputs). |
| T-02-02 | Elevation of Privilege | POST /api/v1/minio/presign (presigned URL abuse) | mitigate | Short expiry: 1 hour for upload URLs, 2 hours for download URLs. Bucket-scoped permissions. Client cannot access other buckets without specifying them — but bucket names are not validated against a whitelist yet (defer to Phase 3 RBAC). |
| T-02-03 | Information Disclosure | GET /api/v1/provenance (no auth) | accept | Phase 1 has no auth — provenance records are infrastructure metadata (source type, confidence), not PII or sensitive engineering data. RBAC in Phase 3 (RISK-07) will protect access. |
| T-02-04 | Tampering | Alembic migration (schema integrity) | mitigate | Migrations are version-controlled in git. Initial migration creates tables with proper FK constraints (provenance → structures → structure_facts) and check constraint on confidence_level. GiST spatial index on geometry column. |
| T-02-05 | Tampering | object_name path traversal in MinIO presign | mitigate | MinIO SDK validates object names. Presigned URLs are scoped to specific bucket + object. Client cannot traverse to other objects — each presigned URL is for one specific object only. |
</threat_model>

<verification>
- `cd apps/api && uv run pytest tests/test_provenance.py tests/test_minio.py tests/test_schema.py -x -v` — all feature tests pass
- `cd apps/api && uv run pytest tests/ -x -v` — full test suite green (health + provenance + minio + schema)
- `cd apps/api && uv run alembic upgrade head` — migration applies cleanly to running PostgreSQL
- `curl -X POST http://localhost:8000/api/v1/provenance -H "Content-Type: application/json" -d '{"source_type":"kazvodhoz_spreadsheet","confidence_level":"HIGH"}'` returns 201
- `curl http://localhost:8000/api/v1/provenance` returns 200 with list
- `curl -X POST http://localhost:8000/api/v1/minio/presign -H "Content-Type: application/json" -d '{"bucket":"sujoly-documents","object_name":"test/file.pdf"}'` returns 200 with presigned URL
</verification>

<success_criteria>
- SC-2: Database schema includes provenance tracking — developer can query source, confidence, and timestamp of any stored fact via GET /api/v1/provenance
- SC-3: MinIO object storage serves presigned URLs correctly via POST/GET /api/v1/minio/presign
- SC-4: Architecture separation established — structures in PostGIS with Geometry, binary assets in MinIO buckets, no binary columns in PostgreSQL
- DATA-07: Every structure record and structure fact has a provenance_id FK — provenance is queryable by source_type, confidence_level, and timestamp
- INT-04: Imagery evidence in MinIO (sujoly-imagery bucket), structure features in PostGIS (Geometry column) — separation proven by schema tests
</success_criteria>

<output>
Create `.planning/workstreams/backend/phases/01-foundation-infrastructure/01-02-SUMMARY.md` when done
</output>
