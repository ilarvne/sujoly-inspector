---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker/postgres/Dockerfile
  - docker/postgres/init-extensions.sql
  - .env.example
  - .gitignore
  - apps/api/Dockerfile
  - apps/api/pyproject.toml
  - apps/api/src/api/__init__.py
  - apps/api/src/api/config/__init__.py
  - apps/api/src/api/config/settings.py
  - apps/api/src/api/infrastructure/__init__.py
  - apps/api/src/api/infrastructure/database.py
  - apps/api/src/api/utils/__init__.py
  - apps/api/src/api/utils/logging.py
  - apps/api/src/api/routes/__init__.py
  - apps/api/src/api/routes/health.py
  - apps/api/src/api/main.py
  - apps/api/src/api/celery_app.py
  - apps/api/src/api/tasks/__init__.py
  - apps/api/src/api/tasks/celery_tasks.py
  - apps/api/tests/conftest.py
  - apps/api/tests/test_health.py
autonomous: false
requirements:
  - INT-04
must_haves:
  truths:
    - "Developer runs docker compose up -d and all 5 services report healthy within 60 seconds"
    - "GET /health/live returns 200 with {\"status\": \"ok\"}"
    - "GET /health/ready returns 200 with status=healthy and checks for postgres, redis, minio all ok"
    - "GET /health/ready returns 503 when any service is down, with the failing service marked error"
    - "Celery worker processes health_check_task and returns a result via Redis"
  artifacts:
    - path: "docker-compose.yml"
      provides: "5-service Docker Compose stack with health checks"
      contains: "service_healthy"
    - path: "docker/postgres/Dockerfile"
      provides: "Custom postgis+pgvector Docker image"
      contains: "postgresql-17-pgvector"
    - path: "apps/api/src/api/main.py"
      provides: "FastAPI app with lifespan, middleware, route registration"
      exports: ["app", "lifespan"]
    - path: "apps/api/src/api/routes/health.py"
      provides: "Health check endpoints (live + ready)"
      exports: ["router", "HealthStatus", "ComponentHealth"]
    - path: "apps/api/src/api/config/settings.py"
      provides: "Pydantic Settings with local Docker defaults"
      exports: ["Settings", "settings"]
    - path: "apps/api/src/api/infrastructure/database.py"
      provides: "SQLAlchemy async engine and session factory"
      exports: ["engine", "async_session", "get_session", "Base"]
    - path: "apps/api/src/api/celery_app.py"
      provides: "Celery app with Redis broker"
      exports: ["celery_app"]
  key_links:
    - from: "apps/api/src/api/main.py"
      to: "apps/api/src/api/routes/health.py"
      via: "app.include_router(health.router)"
      pattern: "include_router.*health"
    - from: "apps/api/src/api/routes/health.py"
      to: "apps/api/src/api/infrastructure/database.py"
      via: "async_session() for DB probe"
      pattern: "async_session"
    - from: "docker-compose.yml"
      to: "apps/api/Dockerfile"
      via: "api service build context"
      pattern: "context.*apps/api"
---

<objective>
Walking Skeleton: prove the full Docker stack (PostgreSQL/PostGIS/pgvector, Redis, MinIO, FastAPI, Celery) works end-to-end with a single `docker compose up` command, and the FastAPI `/health/ready` endpoint verifies connectivity to all three infrastructure services.

Purpose: Establishes the architectural foundation (INT-04 — architecture separation: PostGIS for vector features, MinIO for binary assets) that every subsequent phase builds on. Proves the stack is viable before adding feature complexity.

Output: A running 5-service Docker Compose stack, a FastAPI application with health check endpoints, Celery worker with Redis broker, and test scaffolding for health endpoint verification.

> **Scope note:** This plan lists 22 files_modified, but 8 are trivial `__init__.py` barrel-export files (one-line docstrings). The effective complexity is ~14 substantive files across 3 tasks — acceptable for a Walking Skeleton scaffold that establishes the entire project directory structure in one pass. Task 2 creates 15 files but the `__init__.py` files and small utility modules keep context cost within budget.
</objective>

<execution_context>
@/home/lain/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/lain/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/backend/STATE.md
@.planning/workstreams/backend/ROADMAP.md
@.planning/workstreams/backend/REQUIREMENTS.md
@.planning/workstreams/backend/phases/01-foundation-infrastructure/01-RESEARCH.md
@.planning/workstreams/backend/phases/01-foundation-infrastructure/01-PATTERNS.md
@.planning/workstreams/backend/phases/01-foundation-infrastructure/01-VALIDATION.md

# Analog source files — follow established patterns, deviate where RESEARCH.md specifies
@apps/agent/src/agent/server.py
@apps/agent/src/agent/config/settings.py
@apps/agent/src/agent/infrastructure/database.py
@apps/agent/src/agent/routes/health.py
@apps/agent/src/agent/utils/logging.py
@apps/agent/Dockerfile
@apps/agent/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker infrastructure + FastAPI project scaffold</name>
  <files>
    docker-compose.yml,
    docker/postgres/Dockerfile,
    docker/postgres/init-extensions.sql,
    .env.example,
    .gitignore,
    apps/api/Dockerfile,
    apps/api/pyproject.toml
  </files>
  <read_first>
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-RESEARCH.md (lines 329-428 Docker Compose YAML, lines 601-615 postgres Dockerfile, lines 618-625 init-extensions.sql, lines 78-86 uv add commands),
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-PATTERNS.md (Dockerfile pattern lines 529-586, pyproject.toml pattern lines 589-642),
    apps/agent/Dockerfile (multi-stage uv build pattern to adapt),
    apps/agent/pyproject.toml (project metadata, pytest config, package layout to copy),
    .gitignore (existing ignore rules to extend)
  </read_first>
  <action>
    Create the Docker Compose stack with 5 services, each with health checks and `depends_on: condition: service_healthy`:

    1. **docker/postgres/Dockerfile**: `FROM postgis/postgis:17-3.5`, then `RUN apt-get update && apt-get install -y --no-install-recommends postgresql-17-pgvector && rm -rf /var/lib/apt/lists/*`. The PGDG apt repo is already configured in the base image (VERIFIED per RESEARCH.md). COPY `init-extensions.sql` into `/docker-entrypoint-initdb.d/`.

    2. **docker/postgres/init-extensions.sql**: Three `CREATE EXTENSION IF NOT EXISTS` statements: `postgis`, `vector`, `pg_trgm` (pg_trgm installed now for Phase 2 fuzzy matching, avoids future migration per RESEARCH.md assumption A4).

    3. **docker-compose.yml**: Define 5 services following RESEARCH.md lines 329-428 exactly:
       - `postgres`: build from `./docker/postgres`, env POSTGRES_DB/USER/PASSWORD with defaults `sujoly`/`sujoly`/`sujoly_dev`, port 5432, volume `pgdata`, healthcheck using `pg_isready` AND `psql` checking `postgis` extension exists (prevents Pitfall #1 race condition), start_period 30s, retries 10.
       - `redis`: image `redis:7-alpine`, port 6379, volume `redisdata`, healthcheck `redis-cli ping`.
       - `minio`: image `minio/minio:latest`, command `server /data --console-address ":9001"`, env MINIO_ROOT_USER/PASSWORD with defaults `minioadmin`/`minioadmin`, ports 9000+9001, volume `miniodata`, healthcheck `mc ready local` (NOT curl — Pitfall #2, curl removed from recent MinIO images).
       - `api`: build from `./apps/api`, port 8000, env vars DATABASE_URL/REDIS_URL/MINIO_ENDPOINT/MINIO_ACCESS_KEY/MINIO_SECRET_KEY/MINIO_USE_SSL referencing Docker service hostnames (`postgres:5432`, `redis:6379`, `minio:9000`), depends_on postgres+redis+minio with `condition: service_healthy`, healthcheck `curl -f http://localhost:8000/health`, start_period 20s.
       - `celery-worker`: build from `./apps/api`, command `celery -A api.celery_app worker --loglevel=info`, same env vars as api, depends_on postgres+redis with `condition: service_healthy`.
       - Define volumes: `pgdata`, `redisdata`, `miniodata`.

    4. **.env.example**: Update the existing file. Add a section `# ---- API (Backend FastAPI) ----` with local Docker defaults: `API_DATABASE_URL=postgresql+asyncpg://sujoly:sujoly_dev@postgres:5432/sujoly`, `API_SYNC_DATABASE_URL=postgresql://sujoly:sujoly_dev@postgres:5432/sujoly`, `API_REDIS_URL=redis://redis:6379/0`, `API_MINIO_ENDPOINT=minio:9000`, `API_MINIO_ACCESS_KEY=minioadmin`, `API_MINIO_SECRET_KEY=minioadmin`, `API_MINIO_USE_SSL=false`, `API_ALLOWED_ORIGINS=http://localhost:3000`. Add comment: `# Remote alem.ai credentials for production — set in .env, NOT here`.

    5. **.gitignore**: Add `.env.remote` to the environment variables section (after `.env.*.local`). This prevents committing remote production credentials.

    6. **apps/api/pyproject.toml**: Project name `api`, version `0.1.0`, requires-python `>=3.12`. Dependencies (12 core): `fastapi>=0.138.0`, `uvicorn>=0.49.0`, `sqlalchemy>=2.0.0`, `geoalchemy2>=0.20.0`, `asyncpg>=0.31.0`, `alembic>=1.18.0`, `celery>=5.6.0`, `redis>=8.0.0`, `minio>=7.2.0`, `pgvector>=0.4.0`, `pydantic-settings>=2.0.0`, `structlog>=24.1.0`, `python-multipart>=0.0.9`, `python-dotenv>=1.2.1`, `psycopg[binary,pool]>=3.2.0`, `python-json-logger>=2.0.7`. Dev deps: `ruff>=0.14.10`, `pytest>=8.0.0`, `pytest-asyncio>=0.23.5`, `httpx>=0.27.0`. pytest config: `asyncio_mode = "auto"`, `asyncio_default_fixture_loop_scope = "function"`. Package layout: `[tool.setuptools.package-dir] "" = "src"`, `[tool.setuptools.packages.find] where = ["src"]`. Entry point: `api-server = "api.main:main"`.

    7. **apps/api/Dockerfile**: Multi-stage build following agent Dockerfile pattern. Builder stage: `FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim`, WORKDIR /app, COPY pyproject.toml, `ENV UV_HTTP_TIMEOUT=300`, `RUN uv sync --no-dev --no-install-project --index-strategy unsafe-best-match`, COPY . ., `RUN uv sync --frozen --no-dev`. Runtime stage: `FROM python:3.12-slim-bookworm`, WORKDIR /app, `apt-get install curl`, COPY `.venv` and `src` from builder, `ENV PATH="/app/.venv/bin:$PATH"`, `ENV PYTHONPATH="/app/src"`, create non-root user `appuser`, EXPOSE 8000, HEALTHCHECK with `curl -f http://localhost:8000/health`, CMD `["uvicorn", "api.main:app", "--host", "0.0.0.0", "--port", "8000"]`. Do NOT include `UV_EXTRA_INDEX_URL` (no PyTorch). Do NOT include `:/app/src/agent/gen` in PYTHONPATH (no protobuf gen).
  </action>
  <verify>
    <automated>cd /home/lain/Development/hackday && docker compose config --quiet 2>&1 | grep -v "^$" | head -5 && echo "compose-valid"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains the string `postgresql-17-pgvector` nowhere (that's in the Dockerfile, not compose) but contains `service_healthy` at least 4 times (postgres, redis, minio dependencies)
    - docker/postgres/Dockerfile contains `FROM postgis/postgis:17-3.5` and `postgresql-17-pgvector`
    - docker/postgres/init-extensions.sql contains `CREATE EXTENSION IF NOT EXISTS postgis`, `CREATE EXTENSION IF NOT EXISTS vector`, `CREATE EXTENSION IF NOT EXISTS pg_trgm`
    - docker-compose.yml healthcheck for minio uses `mc ready local` (not curl)
    - docker-compose.yml healthcheck for postgres checks for `postgis` extension (not just pg_isready)
    - .env.example contains `API_DATABASE_URL=postgresql+asyncpg://sujoly:sujoly_dev@postgres:5432/sujoly`
    - .gitignore contains `.env.remote`
    - apps/api/pyproject.toml contains `fastapi`, `geoalchemy2`, `celery`, `minio`, `pgvector`, `asyncio_mode = "auto"`
    - apps/api/Dockerfile contains `api.main:app` in CMD and does NOT contain `UV_EXTRA_INDEX_URL`
    - `docker compose config --quiet` exits 0 (valid compose file)
  </acceptance_criteria>
  <done>
    Docker Compose file is valid and defines 5 health-checked services. Custom postgres Dockerfile extends postgis/postgis:17-3.5 with pgvector. FastAPI project scaffold has all 12 core dependencies in pyproject.toml and a multi-stage Dockerfile. .env.example has local Docker defaults with API_ prefix. .gitignore excludes .env.remote.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FastAPI application with health endpoint + Celery + tests</name>
  <files>
    apps/api/src/api/__init__.py,
    apps/api/src/api/config/__init__.py,
    apps/api/src/api/config/settings.py,
    apps/api/src/api/infrastructure/__init__.py,
    apps/api/src/api/infrastructure/database.py,
    apps/api/src/api/utils/__init__.py,
    apps/api/src/api/utils/logging.py,
    apps/api/src/api/routes/__init__.py,
    apps/api/src/api/routes/health.py,
    apps/api/src/api/main.py,
    apps/api/src/api/celery_app.py,
    apps/api/src/api/tasks/__init__.py,
    apps/api/src/api/tasks/celery_tasks.py,
    apps/api/tests/conftest.py,
    apps/api/tests/test_health.py
  </files>
  <read_first>
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-RESEARCH.md (lines 435-468 lifespan, lines 629-686 health endpoint, lines 688-714 Celery app, lines 717-754 Pydantic Settings),
    .planning/workstreams/backend/phases/01-foundation-infrastructure/01-PATTERNS.md (settings.py pattern lines 52-98, database.py pattern lines 102-151, health.py pattern lines 214-269, main.py pattern lines 374-455, logging.py pattern lines 459-507),
    apps/agent/src/agent/server.py (lifespan, middleware, exception handler, route registration patterns),
    apps/agent/src/agent/config/settings.py (Pydantic Settings pattern — note CRITICAL deviation: use local Docker defaults not remote alem.ai),
    apps/agent/src/agent/infrastructure/database.py (asyncpg SSL handling pattern lines 25-37, session factory pattern),
    apps/agent/src/agent/routes/health.py (ComponentHealth, HealthStatus models, readiness check pattern),
    apps/agent/src/agent/utils/logging.py (structlog configuration — copy nearly verbatim)
  </read_first>
  <behavior>
    - Test 1: GET /health/live returns 200 with JSON body {"status": "ok"}
    - Test 2: GET /health/ready with DB, Redis, MinIO all mocked healthy returns 200, response.status == "healthy", all checks have status "ok"
    - Test 3: GET /health/ready with DB connection failure returns 503, checks["postgres"].status == "error"
    - Test 4: GET /health/ready with Redis ping failure returns 503, checks["redis"].status == "error"
    - Test 5: GET /health/ready with MinIO bucket_exists failure returns 503, checks["minio"].status == "error"
  </behavior>
  <action>
    Create the FastAPI application, configuration, infrastructure, health endpoints, Celery integration, and test scaffolding. All `__init__.py` files are barrel exports with a one-line docstring (follow agent app pattern from PATTERNS.md).

    1. **apps/api/src/api/config/settings.py**: Pydantic Settings class with `env_prefix="API_"` (NOT "AGENT_"), `env_file=".env"`, `extra="ignore"`, `case_sensitive=False`. Load .env from project root using `Path(__file__).parent.parent.parent.parent / ".env"` + `load_dotenv()`. Fields with LOCAL Docker defaults (CRITICAL: do NOT use remote alem.ai credentials — RESEARCH.md Anti-Pattern #6): `environment: str = "development"`, `debug: bool = True`, `database_url: str = "postgresql+asyncpg://sujoly:sujoly_dev@postgres:5432/sujoly"`, `sync_database_url: str = "postgresql://sujoly:sujoly_dev@postgres:5432/sujoly"`, `redis_url: str = "redis://redis:6379/0"`, `minio_endpoint: str = "minio:9000"`, `minio_access_key: str = "minioadmin"`, `minio_secret_key: str = "minioadmin"`, `minio_bucket: str = "sujoly-documents"`, `minio_use_ssl: bool = False`, `allowed_origins: str = "http://localhost:3000"`. Instantiate `settings = Settings()` at module level.

    2. **apps/api/src/api/infrastructure/database.py**: SQLAlchemy async engine following agent pattern. Import `from api.config.settings import settings`. Declare `Base = declarative_base()`. Handle asyncpg SSL incompatibility (Pitfall #4): if `"sslmode=disable"` in `settings.database_url`, strip it and set `connect_args={"ssl": None}`. Create `engine = create_async_engine(_db_url, echo=settings.debug, connect_args=_connect_args)`. Create `async_session = sessionmaker(engine, class_=AsyncSession, expire_on_commit=False)`. Create `async def get_session()` generator yielding `async with async_session() as session`. Do NOT include `init_db()` with `create_all` — schema management is via Alembic (RESEARCH.md Anti-Pattern #5).

    3. **apps/api/src/api/utils/logging.py**: Copy the agent's logging.py nearly verbatim — `configure_logging(level: str = "INFO")` and `get_logger(name: str)`. Uses structlog with ConsoleRenderer in TTY, JSONRenderer otherwise. Keep `pythonjsonlogger` import (it's in deps).

    4. **apps/api/src/api/utils/__init__.py**: Barrel export `from api.utils.logging import configure_logging, get_logger` with `__all__ = ["configure_logging", "get_logger"]`.

    5. **apps/api/src/api/routes/health.py**: FastAPI APIRouter with `tags=["health"]`. Define `ComponentHealth(BaseModel)` with `status: Literal["ok", "error"]` and `message: str | None = None`. Define `HealthStatus(BaseModel)` with `status: Literal["healthy", "degraded", "unhealthy"]` and `checks: dict[str, ComponentHealth]`. Endpoint `GET /health/live` returns `{"status": "ok"}`. Endpoint `GET /health/ready` with `response_model=HealthStatus`: check PostgreSQL via `async with async_session() as session: await session.execute(text("SELECT 1"))`, check Redis via `Redis.from_url(settings.redis_url)` + `await redis.ping()` + `await redis.aclose()`, check MinIO via `Minio(settings.minio_endpoint, access_key=..., secret_key=..., secure=settings.minio_use_ssl)` + `minio.bucket_exists(settings.minio_bucket)`. Each check wrapped in try/except, setting `ComponentHealth(status="error", message=str(e))` on failure and `overall = "degraded"`. Return `HealthStatus(status=overall, checks=checks)`. Use `from api.infrastructure.database import async_session` and `from api.config.settings import settings`.

    6. **apps/api/src/api/main.py**: FastAPI app with `@asynccontextmanager` lifespan. Startup: call `configure_logging(level="DEBUG" if settings.debug else "INFO")`, create `MinIOService` (import from `api.services.minio_client` — this will be created in Plan 02; for now, inline a simple Minio client init using `from minio import Minio`), ensure buckets `["sujoly-imagery", "sujoly-documents", "sujoly-photos"]` exist via `minio.ensure_bucket(bucket)` or `if not client.bucket_exists(bucket): client.make_bucket(bucket)`, store on `app.state.minio`. Shutdown: `await engine.dispose()`. Create `app = FastAPI(title="SuJoly Inspector API", version="0.1.0", lifespan=lifespan, docs_url="/docs" if settings.environment == "development" else None, redoc_url="/redoc" if settings.environment == "development" else None)`. Add global exception handler (copy from agent server.py lines 59-81 — catches HTTPException separately, logs unhandled exceptions, returns generic 500). Add security headers middleware (copy from agent server.py lines 84-137 — BUT remove admin route API key auth block and metrics auth block, keep only request ID, timing, and security headers: HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff, X-XSS-Protection, Referrer-Policy, Permissions-Policy, Content-Security-Policy default-src 'none'). Add CORS middleware with `allow_origins` from `settings.allowed_origins.split(",")`. Register routers: `app.include_router(health.router)`. Define `def main(): uvicorn.run(app, host="0.0.0.0", port=8000)`.

    7. **apps/api/src/api/celery_app.py**: Celery instance `celery_app = Celery("sujoly_api", broker=settings.redis_url, backend=settings.redis_url)`. Configure `task_serializer="json"`, `result_serializer="json"`, `accept_content=["json"]`, `timezone="UTC"`, `enable_utc=True`.

    8. **apps/api/src/api/tasks/celery_tasks.py**: Import `from api.celery_app import celery_app`. Define `@celery_app.task def health_check_task():` returning `{"status": "healthy", "timestamp": datetime.now(timezone.utc).isoformat()}`.

    9. **apps/api/tests/conftest.py**: Add `sys.path.append(str(Path(__file__).parent.parent / "src"))` for import resolution. Create fixtures: `mock_db_session` (MagicMock AsyncSession), `mock_redis` (MagicMock with `ping` returning AsyncMock), `mock_minio` (MagicMock with `bucket_exists` returning True), `test_client` (FastAPI TestClient with mocked app.state dependencies — mock DB, Redis, MinIO by patching the health endpoint's imports).

    10. **apps/api/tests/test_health.py**: Test class `TestHealthEndpoints` with tests matching the behavior block. Use `from fastapi.testclient import TestClient`. Mock `async_session`, `Redis.from_url`, and `Minio` in the health module. Test all-healthy returns 200. Test each service failure returns 503 with the correct check marked error.
  </action>
  <verify>
    <automated>cd /home/lain/Development/hackday/apps/api && uv run pytest tests/test_health.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/api/config/settings.py contains `env_prefix="API_"` and `database_url: str = "postgresql+asyncpg://sujoly:sujoly_dev@postgres:5432/sujoly"` (NOT alem.ai)
    - apps/api/src/api/infrastructure/database.py contains `sslmode=disable` handling and does NOT contain `create_all`
    - apps/api/src/api/routes/health.py contains `class ComponentHealth`, `class HealthStatus`, `GET /health/live`, `GET /health/ready`
    - apps/api/src/api/main.py contains `lifespan`, `Strict-Transport-Security`, `X-Frame-Options`, `include_router(health.router)`, `engine.dispose`
    - apps/api/src/api/celery_app.py contains `Celery("sujoly_api"` and `broker=settings.redis_url`
    - apps/api/tests/test_health.py contains `test_health_live`, `test_health_ready` (or equivalent test names covering liveness + readiness + failure cases)
    - `uv run pytest tests/test_health.py -x` exits 0 (all health tests pass)
    - No file imports from `agent.` — all imports use `api.` prefix
  </acceptance_criteria>
  <done>
    FastAPI application starts with lifespan initializing MinIO buckets. /health/live returns 200. /health/ready probes PostgreSQL, Redis, and MinIO, returns 200 when all healthy, 503 when any fails. Celery app configured with Redis broker. All health tests pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full Docker stack health</name>
  <files>docker-compose.yml</files>
  <action>
    Human verifies the full Docker Compose stack starts and all services report healthy, and the FastAPI /health/ready endpoint confirms DB, Redis, and MinIO connectivity. This proves the Walking Skeleton capability end-to-end.
  </action>
  <verify>
    <automated>docker compose ps --format json 2>/dev/null | grep -c '"healthy"' | xargs -I{} test {} -ge 4 && echo "all-healthy"</automated>
    <human-check>Run `docker compose up -d --build`, wait 60s, run `docker compose ps` — all 5 services show "healthy". Then `curl http://localhost:8000/health/ready` returns 200 with all checks "ok".</human-check>
  </verify>
  <what-built>
    Docker Compose stack with 5 services (PostgreSQL/PostGIS/pgvector, Redis, MinIO, FastAPI, Celery) and a FastAPI health check endpoint that probes all infrastructure services.
  </what-built>
  <how-to-verify>
    1. Start the stack: `cd /home/lain/Development/hackday && docker compose up -d --build`
    2. Wait ~60 seconds for services to initialize (PostGIS extension install restarts the DB)
    3. Check all services are healthy: `docker compose ps` — all 5 services should show "healthy" status
    4. Test liveness: `curl http://localhost:8000/health/live` — expect `{"status": "ok"}`
    5. Test readiness: `curl http://localhost:8000/health/ready` — expect 200 with `{"status": "healthy", "checks": {"postgres": {"status": "ok"}, "redis": {"status": "ok"}, "minio": {"status": "ok"}}}`
    6. Test API docs (dev only): `curl -s http://localhost:8000/docs | head -5` — expect HTML (Swagger UI)
    7. Check MinIO console: open `http://localhost:9001` in browser, login with minioadmin/minioadmin, verify buckets `sujoly-imagery`, `sujoly-documents`, `sujoly-photos` exist
    8. Tear down: `docker compose down -v`
  </how-to-verify>
  <resume-signal>Type "approved" if all services healthy and /health/ready returns 200, or describe issues</resume-signal>
  <done>All 5 Docker Compose services show "healthy" status. /health/live returns 200. /health/ready returns 200 with all checks ok. MinIO console shows 3 buckets. Human types "approved".</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts This Phase Produces (Plan 01)

| Artifact | Type | Location | Consumers |
|----------|------|----------|-----------|
| `docker-compose.yml` | Infrastructure config | Project root | All services, developer workflow |
| `docker/postgres/Dockerfile` | Docker image def | `docker/postgres/` | postgres service |
| `docker/postgres/init-extensions.sql` | DB init script | `docker/postgres/` | postgres service entrypoint |
| `Settings` class + `settings` singleton | Config | `apps/api/src/api/config/settings.py` | All API modules |
| `engine`, `async_session`, `get_session()`, `Base` | DB infrastructure | `apps/api/src/api/infrastructure/database.py` | All services, routes, models |
| `configure_logging()`, `get_logger()` | Logging | `apps/api/src/api/utils/logging.py` | All API modules |
| `router`, `ComponentHealth`, `HealthStatus` | Health route | `apps/api/src/api/routes/health.py` | main.py, tests |
| `app`, `lifespan()` | FastAPI app | `apps/api/src/api/main.py` | uvicorn, Dockerfile CMD, tests |
| `celery_app` | Celery instance | `apps/api/src/api/celery_app.py` | celery-worker service, tasks |
| `health_check_task()` | Celery task | `apps/api/src/api/tasks/celery_tasks.py` | celery-worker |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External client → FastAPI :8000 | Untrusted HTTP input crosses here. No auth in Phase 1. |
| FastAPI → PostgreSQL :5432 | Internal Docker network. SQLAlchemy parameterized queries. |
| FastAPI → Redis :6379 | Internal Docker network. Redis used as broker/cache. |
| FastAPI → MinIO :9000 | Internal Docker network. MinIO SDK for bucket ops. |
| Docker host → Containers | Infrastructure boundary. Only necessary ports exposed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | FastAPI endpoints (no auth) | accept | Phase 1 has no auth — RBAC is Phase 3 (RISK-07). Docker network isolation; only port 8000 exposed externally. |
| T-01-02 | Tampering | Docker Compose env vars / secrets | mitigate | Secrets via env vars only, .env in .gitignore, no hardcoded credentials in settings defaults (local Docker defaults only, NOT remote alem.ai per RESEARCH.md Anti-Pattern #6) |
| T-01-03 | Information Disclosure | /health endpoint error messages | mitigate | Global exception handler returns generic "Internal Server Error" — no stack traces. Health endpoint returns structured ComponentHealth status, not raw exception messages. |
| T-01-04 | Information Disclosure | Debug mode exposing internals | mitigate | docs_url/redoc_url only enabled when `settings.environment == "development"`. Debug=False in production via env var override. |
| T-01-05 | Information Disclosure | Container port exposure | mitigate | Only ports 5432, 6379, 9000, 9001, 8000 exposed in dev. Internal services communicate via Docker network. |
| T-01-06 | Information Disclosure | Security headers missing | mitigate | Security headers middleware adds HSTS, X-Frame-Options DENY, X-Content-Type-Options nosniff, CSP default-src 'none', Referrer-Policy, Permissions-Policy on all responses. |
| T-01-SC | Tampering | uv/pip package installs | mitigate | All 12 core packages verified [OK] via slopcheck (RESEARCH.md Package Legitimacy Audit). No [SUS] or [SLOP] packages. All from PyPI with established age and download counts. |
</threat_model>

<verification>
- `docker compose config --quiet` exits 0 (valid compose file)
- `cd apps/api && uv run pytest tests/test_health.py -x -v` — all health tests pass
- `docker compose up -d --build && docker compose ps` — all 5 services show "healthy"
- `curl http://localhost:8000/health/live` returns 200 `{"status": "ok"}`
- `curl http://localhost:8000/health/ready` returns 200 with all checks "ok"
- `docker compose down -v` cleans up
</verification>

<success_criteria>
- SC-1: Developer starts all services with `docker compose up -d` and they report healthy
- FastAPI /health/ready endpoint verifies DB, Redis, and MinIO connectivity
- Celery worker processes tasks via Redis broker
- No remote alem.ai credentials in settings defaults — local Docker defaults only
- All health endpoint tests pass
</success_criteria>

<output>
Create `.planning/workstreams/backend/phases/01-foundation-infrastructure/01-01-SUMMARY.md` when done
</output>
