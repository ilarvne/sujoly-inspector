---
phase: 03-inspection-risk-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/lib/api/types.ts
  - apps/web/lib/api/mock-data.ts
  - apps/web/lib/api/client.ts
  - apps/web/lib/stores/auth-store.ts
  - apps/web/components/ui/dialog.tsx
  - apps/web/components/ui/tabs.tsx
  - apps/web/components/ui/table.tsx
  - apps/web/components/ui/tooltip.tsx
  - apps/web/components/ui/alert.tsx
  - apps/web/components/ui/textarea.tsx
  - apps/web/components/ui/label.tsx
  - apps/web/components/ui/input.tsx
  - apps/web/components/ui/progress.tsx
  - apps/web/components/ui/accordion.tsx
  - apps/web/components/ui/dropdown-menu.tsx
  - apps/web/components/ui/avatar.tsx
  - apps/web/components/ui/scroll-area.tsx
  - apps/web/messages/en.json
  - apps/web/messages/ru.json
  - apps/web/messages/kk.json
  - apps/web/tests/phase3-mocks.test.ts
autonomous: true
requirements: [DATA-05-FE, DATA-06-FE, RISK-06-FE, RISK-07-FE, RISK-08-FE]
user_setup: []

must_haves:
  truths:
    - "Phase 3 npm packages installed: react-hook-form@7.80.0, zod@4.4.3, @hookform/resolvers@5.4.0, papaparse@5.5.4, jspdf@4.2.1, jspdf-autotable@5.0.8, date-fns@4.4.0"
    - "13 new shadcn/ui components installed: dialog, tabs, table, tooltip, alert, textarea, label, input, progress, accordion, dropdown-menu, avatar, scroll-area"
    - "TypeScript types for InspectionRecord, DocumentMeta, RiskScore, EngineerOverride, UserRole, AuthUser defined in lib/api/types.ts"
    - "Mock data functions for inspections, documents, risk scores, overrides in lib/api/mock-data.ts"
    - "TanStack Query hooks useInspections, useDocuments, useRiskScore, useOverrides in lib/api/client.ts"
    - "Zustand auth store with persist middleware in lib/stores/auth-store.ts — 4 roles (admin, engineer, inspector, viewer)"
    - "i18n namespaces added to all 3 message files: inspection, documents, risk, auth, export, override, passportTabs"
    - "Unit tests for new mock data and auth store pass"
  artifacts:
    - path: "apps/web/lib/api/types.ts"
      provides: "Extended types: UserRole, AuthUser, InspectionRecord, DocumentMeta, RiskScore, RiskComponent, EngineerOverride, OverrideField"
      contains: "export type UserRole"
    - path: "apps/web/lib/api/mock-data.ts"
      provides: "Mock data functions for inspections, documents, risk scores, overrides"
      exports: ["mockInspections", "mockDocuments", "mockRiskScore", "mockOverrides", "mockAddDocument", "mockAddOverride"]
    - path: "apps/web/lib/api/client.ts"
      provides: "Extended TanStack Query hooks for Phase 3 data"
      exports: ["useInspections", "useDocuments", "useRiskScore", "useOverrides"]
    - path: "apps/web/lib/stores/auth-store.ts"
      provides: "Zustand auth store with localStorage persistence for mock RBAC"
      exports: ["useAuthStore", "UserRole"]
    - path: "apps/web/tests/phase3-mocks.test.ts"
      provides: "Unit tests for Phase 3 mock data, auth store, and type validation"
      contains: "describe('Phase 3 mock data'"
  key_links:
    - from: "apps/web/lib/api/client.ts"
      to: "apps/web/lib/api/mock-data.ts"
      via: "Phase 3 query hooks call mock inspection/document/risk/override functions — swap point for real API"
      pattern: "mockInspections"
    - from: "apps/web/lib/stores/auth-store.ts"
      to: "localStorage"
      via: "Zustand persist middleware saves auth state to localStorage key 'sujoly-auth'"
      pattern: "persist"
---

## Phase Goal

**As a** water resource manager, **I want to** view inspection history, manage documents, see risk scores, control access by role, and export data, **so that** I can make informed decisions about hydraulic structure maintenance and compliance.

---

<objective>
Install all Phase 3 packages, shadcn/ui components, and create the full data layer (types, mock data, API hooks, auth store) and i18n keys. This is the foundation that Plans 02-05 build upon — after this plan, all Phase 3 infrastructure is in place for feature implementation.

Purpose: Establish the types, mock data, state management, and translations that all Phase 3 feature plans depend on. Without this, no other Phase 3 feature can function.
Output: Installed packages, 13 shadcn components, extended types/mock-data/client.ts, auth store, all i18n keys, passing unit tests.
</objective>

<execution_context>
@C:/Users/Ferumm/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Ferumm/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend/STATE.md
@.planning/workstreams/frontend/ROADMAP.md
@.planning/workstreams/frontend/phases/03-inspection-risk-ui/03-RESEARCH.md
@.planning/workstreams/frontend/phases/03-inspection-risk-ui/03-VALIDATION.md
@apps/web/package.json
@apps/web/lib/api/types.ts
@apps/web/lib/api/mock-data.ts
@apps/web/lib/api/client.ts
@apps/web/lib/stores/filter-store.ts
@apps/web/components.json
@apps/web/messages/en.json
@apps/web/messages/ru.json
@apps/web/messages/kk.json
@apps/web/vitest.config.ts
@apps/web/tsconfig.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Phase 3 packages and shadcn/ui components</name>
  <files>
    apps/web/package.json,
    apps/web/components/ui/dialog.tsx,
    apps/web/components/ui/tabs.tsx,
    apps/web/components/ui/table.tsx,
    apps/web/components/ui/tooltip.tsx,
    apps/web/components/ui/alert.tsx,
    apps/web/components/ui/textarea.tsx,
    apps/web/components/ui/label.tsx,
    apps/web/components/ui/input.tsx,
    apps/web/components/ui/progress.tsx,
    apps/web/components/ui/accordion.tsx,
    apps/web/components/ui/dropdown-menu.tsx,
    apps/web/components/ui/avatar.tsx,
    apps/web/components/ui/scroll-area.tsx
  </files>
  <read_first>
    apps/web/package.json (current deps from Phases 1-2, scripts),
    apps/web/components.json (shadcn config: style=radix-nova, config="" for Tailwind v4),
    apps/web/components/ui/button.tsx (existing shadcn component pattern for reference),
    apps/web/components/ui/sheet.tsx (existing shadcn component — uses radix-ui)
  </read_first>
  <behavior>
    - 7 npm packages install without errors with pinned versions
    - 13 shadcn/ui component files created in components/ui/
    - Build succeeds after all installs
    - No tailwind.config.js or tailwind.config.ts exists (Tailwind v4)
  </behavior>
  <action>
Install 7 npm packages with pinned versions from the apps/web directory:
```bash
npm install react-hook-form@7.80.0 zod@4.4.3 @hookform/resolvers@5.4.0 papaparse@5.5.4 jspdf@4.2.1 jspdf-autotable@5.0.8 date-fns@4.4.0
```

Install the TypeScript types for papaparse as a dev dependency:
```bash
npm install -D @types/papaparse@5.5.2
```

Install 13 shadcn/ui components by running from the apps/web directory:
```bash
npx shadcn@latest add dialog tabs table tooltip alert textarea label input progress accordion dropdown-menu avatar scroll-area
```
This creates 13 files in components/ui/ and adds any radix-ui dependencies to package.json. The components.json style is radix-nova (per A3 from RESEARCH.md).

Verify the build succeeds after all installs:
```bash
npm run build
```
  </action>
  <verify>
    <automated>cd apps/web; npm ls react-hook-form zod @hookform/resolvers papaparse jspdf jspdf-autotable date-fns 2>&1 | Select-String "react-hook-form@7" ; npm ls react-hook-form zod @hookform/resolvers papaparse jspdf jspdf-autotable date-fns 2>&1 | Select-String "zod@4" ; Test-Path -LiteralPath "components/ui/dialog.tsx" ; Test-Path -LiteralPath "components/ui/tabs.tsx" ; Test-Path -LiteralPath "components/ui/table.tsx" ; Test-Path -LiteralPath "components/ui/tooltip.tsx" ; Test-Path -LiteralPath "components/ui/alert.tsx" ; Test-Path -LiteralPath "components/ui/textarea.tsx" ; Test-Path -LiteralPath "components/ui/label.tsx" ; Test-Path -LiteralPath "components/ui/input.tsx" ; Test-Path -LiteralPath "components/ui/progress.tsx" ; Test-Path -LiteralPath "components/ui/accordion.tsx" ; Test-Path -LiteralPath "components/ui/dropdown-menu.tsx" ; Test-Path -LiteralPath "components/ui/avatar.tsx" ; Test-Path -LiteralPath "components/ui/scroll-area.tsx"</automated>
  </verify>
  <done>
    7 npm packages installed with correct pinned versions. 13 shadcn/ui component files exist in components/ui/. @types/papaparse installed as dev dependency. npm run build succeeds.
  </done>
  <acceptance_criteria>
    - npm ls output shows react-hook-form@7.80.0, zod@4.4.3, @hookform/resolvers@5.4.0, papaparse@5.5.4, jspdf@4.2.1, jspdf-autotable@5.0.8, date-fns@4.4.0
    - Test-Path returns True for all 13 shadcn component files (dialog, tabs, table, tooltip, alert, textarea, label, input, progress, accordion, dropdown-menu, avatar, scroll-area)
    - @types/papaparse@5.5.2 appears in devDependencies in package.json
    - npm run build exits with 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 3 types, mock data, auth store, and API hooks</name>
  <files>
    apps/web/lib/api/types.ts,
    apps/web/lib/api/mock-data.ts,
    apps/web/lib/api/client.ts,
    apps/web/lib/stores/auth-store.ts
  </files>
  <read_first>
    apps/web/lib/api/types.ts (existing types: ConditionStatus, InspectionStatus, StructureType, TrilingualText, StructureProperties, StructureFeature, StructureCollection, StructureDetail, StructureFilters),
    apps/web/lib/api/mock-data.ts (existing: generateMockStructures, mockStructures, mockStructureById, mockDistricts, mockBasins — add new functions alongside these),
    apps/web/lib/api/client.ts (existing: fetchStructures, fetchStructureDetail, useStructuresGeoJSON, useStructureDetail — add new hooks alongside these),
    apps/web/lib/stores/filter-store.ts (existing Zustand pattern: create() with interface, set function),
    apps/web/lib/stores/selection-store.ts (existing Zustand pattern),
    apps/web/tsconfig.json (path alias @/* maps to ./*)
  </read_first>
  <behavior>
    - UserRole type is union of 'admin' | 'engineer' | 'inspector' | 'viewer'
    - InspectionRecord has date, inspectorName, findings, photoUrls, conditionAtInspection
    - DocumentMeta has id, structureId, filename, fileType, fileSize, uploadedBy, uploadedAt, downloadUrl
    - RiskScore has overall (0-100), components array, explanation, computedAt
    - EngineerOverride has id, structureId, field, originalValue, newValue, reason, engineerName, timestamp
    - mockInspections(structureId) returns 3-8 InspectionRecord[] sorted by date descending
    - mockDocuments(structureId) returns 0-5 DocumentMeta[]
    - mockRiskScore(structureId) returns RiskScore with overall score and 4 components (structural, hydrological, operational, age)
    - mockOverrides(structureId) returns 0-3 EngineerOverride[]
    - mockAddDocument(structureId, meta) returns updated DocumentMeta with generated id and timestamp
    - mockAddOverride(structureId, data) returns EngineerOverride with generated id and timestamp
    - useInspections(structureId) TanStack Query hook with 100ms delay
    - useDocuments(structureId) TanStack Query hook
    - useRiskScore(structureId) TanStack Query hook
    - useOverrides(structureId) TanStack Query hook
    - useAuthStore: login(role) sets user, logout() clears user, hasRole(...roles) returns boolean
    - useAuthStore persists to localStorage key 'sujoly-auth'
  </behavior>
  <action>
**Extend lib/api/types.ts** — add these types at the end of the file (after existing StructureFilters):

```typescript
export type UserRole = 'admin' | 'engineer' | 'inspector' | 'viewer';

export interface AuthUser {
  id: string;
  name: string;
  role: UserRole;
}

export interface InspectionRecord {
  id: string;
  structureId: string;
  date: string;
  inspectorName: string;
  findings: string;
  photoUrls: string[];
  conditionAtInspection: ConditionStatus;
}

export interface DocumentMeta {
  id: string;
  structureId: string;
  filename: string;
  fileType: string;
  fileSize: number;
  uploadedBy: string;
  uploadedAt: string;
  downloadUrl: string;
}

export interface RiskComponent {
  key: 'structural' | 'hydrological' | 'operational' | 'age';
  label: string;
  score: number;
  weight: number;
  description: string;
}

export interface RiskScore {
  structureId: string;
  overall: number;
  components: RiskComponent[];
  explanation: string;
  computedAt: string;
}

export type OverrideField = 'inspection_interval' | 'repair_status';

export interface EngineerOverride {
  id: string;
  structureId: string;
  field: OverrideField;
  originalValue: string;
  newValue: string;
  reason: string;
  engineerName: string;
  timestamp: string;
}
```

**Extend lib/api/mock-data.ts** — add these functions at the end of the file (after existing mockBasins):

```typescript
import type {
  InspectionRecord,
  DocumentMeta,
  RiskScore,
  RiskComponent,
  EngineerOverride,
  OverrideField,
} from './types';

const inspectorNames = [
  'А. Беков', 'К. Жусупов', 'М. Сулейменов', 'Б. Ахметов', 'Д. Касымов',
];

const findingsText = [
  'Состояние удовлетворительное. Требуется плановый осмотр через 6 месяцев.',
  'Обнаружены трещины на бетонной части. Рекомендуется ремонт.',
  'Водосбросное сооружение работает в штатном режиме.',
  'Уровень воды близок к критическому. Усиленный мониторинг.',
  'Грунтовая плотина имеет просадку на 2 см. Наблюдение продолжается.',
  'Оборудование насосной станции требует замены отдельных узлов.',
  'Канал очищен от наносов. Пропускная способность восстановлена.',
  'Выявлена фильтрация через тело плотины. Срочный ремонт.',
];

const documentTypes = ['passport_scan', 'inspection_report', 'photo', 'technical_drawing', 'certificate'];
const documentExtensions: Record<string, string> = {
  passport_scan: 'pdf',
  inspection_report: 'pdf',
  photo: 'jpg',
  technical_drawing: 'pdf',
  certificate: 'pdf',
};

export function mockInspections(structureId: string): InspectionRecord[] {
  const seed = parseInt(structureId.replace(/\D/g, ''), 10) || 1;
  const count = 3 + (seed % 6);
  const records: InspectionRecord[] = [];
  for (let i = 0; i < count; i++) {
    const year = 2024 - i;
    const month = ((seed + i) % 12) + 1;
    const day = ((seed + i * 3) % 28) + 1;
    records.push({
      id: `${structureId}-INS-${String(i + 1).padStart(3, '0')}`,
      structureId,
      date: `${year}-${String(month).padStart(2, '0')}-${String(day).padStart(2, '0')}`,
      inspectorName: inspectorNames[(seed + i) % inspectorNames.length],
      findings: findingsText[(seed + i) % findingsText.length],
      photoUrls: i < 2 ? [`/mock-photos/${structureId}-${i + 1}.jpg`] : [],
      conditionAtInspection: conditions[(seed + i) % conditions.length] as ConditionStatus,
    });
  }
  return records.sort((a, b) => b.date.localeCompare(a.date));
}

export function mockDocuments(structureId: string): DocumentMeta[] {
  const seed = parseInt(structureId.replace(/\D/g, ''), 10) || 1;
  const count = seed % 4;
  const docs: DocumentMeta[] = [];
  for (let i = 0; i < count; i++) {
    const type = documentTypes[(seed + i) % documentTypes.length];
    docs.push({
      id: `${structureId}-DOC-${String(i + 1).padStart(3, '0')}`,
      structureId,
      filename: `${structureId}_${type}.${documentExtensions[type]}`,
      fileType: type,
      fileSize: 1024 * (100 + (seed + i) * 500),
      uploadedBy: inspectorNames[(seed + i) % inspectorNames.length],
      uploadedAt: `2024-${String(((seed + i) % 12) + 1).padStart(2, '0')}-15`,
      downloadUrl: `#mock-download/${structureId}/${type}/${i + 1}`,
    });
  }
  return docs;
}

export function mockAddDocument(structureId: string, meta: { filename: string; fileType: string; fileSize: number }): DocumentMeta {
  const id = `${structureId}-DOC-${Date.now()}`;
  return {
    id,
    structureId,
    filename: meta.filename,
    fileType: meta.fileType,
    fileSize: meta.fileSize,
    uploadedBy: 'Current User',
    uploadedAt: new Date().toISOString().split('T')[0],
    downloadUrl: `#mock-download/${structureId}/upload/${id}`,
  };
}

export function mockRiskScore(structureId: string): RiskScore {
  const seed = parseInt(structureId.replace(/\D/g, ''), 10) || 1;
  const components: RiskComponent[] = [
    {
      key: 'structural',
      label: 'Structural Integrity',
      score: 30 + (seed % 40),
      weight: 0.35,
      description: 'Condition of dam body, spillway, and load-bearing structures',
    },
    {
      key: 'hydrological',
      label: 'Hydrological Risk',
      score: 20 + (seed * 7 % 50),
      weight: 0.25,
      description: 'Flood probability, capacity utilization, basin characteristics',
    },
    {
      key: 'operational',
      label: 'Operational Status',
      score: 25 + (seed * 3 % 45),
      weight: 0.25,
      description: 'Equipment condition, maintenance frequency, operational readiness',
    },
    {
      key: 'age',
      label: 'Infrastructure Age',
      score: 35 + (seed * 11 % 50),
      weight: 0.15,
      description: 'Years since construction, design lifetime, obsolescence factors',
    },
  ];
  const overall = Math.round(components.reduce((sum, c) => sum + c.score * c.weight, 0));
  const riskLevel = overall >= 70 ? 'high' : overall >= 40 ? 'medium' : 'low';
  const explanations: Record<string, string> = {
    high: 'High risk score indicates urgent inspection and potential repair requirements.',
    medium: 'Moderate risk — scheduled inspection and monitoring recommended.',
    low: 'Low risk — routine inspection schedule applies.',
  };
  return {
    structureId,
    overall,
    components,
    explanation: explanations[riskLevel],
    computedAt: '2024-06-01',
  };
}

export function mockOverrides(structureId: string): EngineerOverride[] {
  const seed = parseInt(structureId.replace(/\D/g, ''), 10) || 1;
  const count = seed % 3;
  const overrides: EngineerOverride[] = [];
  const fields: OverrideField[] = ['inspection_interval', 'repair_status'];
  for (let i = 0; i < count; i++) {
    const field = fields[i % fields.length];
    overrides.push({
      id: `${structureId}-OVR-${String(i + 1).padStart(3, '0')}`,
      structureId,
      field,
      originalValue: field === 'inspection_interval' ? '12 months' : 'repair_required',
      newValue: field === 'inspection_interval' ? '6 months' : 'monitor',
      reason: field === 'inspection_interval'
        ? 'Increased risk due to age and recent findings of minor cracking.'
        : 'Visual inspection shows condition is stable. Monitoring recommended instead of immediate repair.',
      engineerName: inspectorNames[(seed + i) % inspectorNames.length],
      timestamp: `2024-${String(((seed + i) % 6) + 1).padStart(2, '0')}-10`,
    });
  }
  return overrides;
}

export function mockAddOverride(structureId: string, data: { field: OverrideField; originalValue: string; newValue: string; reason: string; engineerName: string }): EngineerOverride {
  return {
    id: `${structureId}-OVR-${Date.now()}`,
    structureId,
    ...data,
    timestamp: new Date().toISOString().split('T')[0],
  };
}
```

**Extend lib/api/client.ts** — add these hooks at the end of the file (after existing useStructureDetail):

```typescript
import { mockInspections, mockDocuments, mockRiskScore, mockOverrides } from './mock-data';
import type { InspectionRecord, DocumentMeta, RiskScore, EngineerOverride } from './types';

async function fetchInspections(structureId: string): Promise<InspectionRecord[]> {
  await new Promise((resolve) => setTimeout(resolve, 100));
  return mockInspections(structureId);
}

async function fetchDocuments(structureId: string): Promise<DocumentMeta[]> {
  await new Promise((resolve) => setTimeout(resolve, 100));
  return mockDocuments(structureId);
}

async function fetchRiskScore(structureId: string): Promise<RiskScore> {
  await new Promise((resolve) => setTimeout(resolve, 100));
  return mockRiskScore(structureId);
}

async function fetchOverrides(structureId: string): Promise<EngineerOverride[]> {
  await new Promise((resolve) => setTimeout(resolve, 100));
  return mockOverrides(structureId);
}

export function useInspections(structureId: string | null) {
  return useQuery({
    queryKey: ['inspections', structureId],
    queryFn: () => fetchInspections(structureId!),
    enabled: !!structureId,
  });
}

export function useDocuments(structureId: string | null) {
  return useQuery({
    queryKey: ['documents', structureId],
    queryFn: () => fetchDocuments(structureId!),
    enabled: !!structureId,
  });
}

export function useRiskScore(structureId: string | null) {
  return useQuery({
    queryKey: ['risk-score', structureId],
    queryFn: () => fetchRiskScore(structureId!),
    enabled: !!structureId,
  });
}

export function useOverrides(structureId: string | null) {
  return useQuery({
    queryKey: ['overrides', structureId],
    queryFn: () => fetchOverrides(structureId!),
    enabled: !!structureId,
  });
}
```

**Create lib/stores/auth-store.ts:**

```typescript
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

export type UserRole = 'admin' | 'engineer' | 'inspector' | 'viewer';

export interface AuthUser {
  id: string;
  name: string;
  role: UserRole;
}

interface AuthState {
  user: AuthUser | null;
  login: (role: UserRole) => void;
  logout: () => void;
  hasRole: (...roles: UserRole[]) => boolean;
}

const mockUsers: Record<UserRole, AuthUser> = {
  admin: { id: 'u-admin', name: 'Administrator', role: 'admin' },
  engineer: { id: 'u-engineer', name: 'Engineer', role: 'engineer' },
  inspector: { id: 'u-inspector', name: 'Inspector', role: 'inspector' },
  viewer: { id: 'u-viewer', name: 'Viewer', role: 'viewer' },
};

export const useAuthStore = create<AuthState>()(
  persist(
    (set, get) => ({
      user: null,
      login: (role) => set({ user: mockUsers[role] }),
      logout: () => set({ user: null }),
      hasRole: (...roles) => {
        const user = get().user;
        return user !== null && roles.includes(user.role);
      },
    }),
    { name: 'sujoly-auth' }
  )
);
```
  </action>
  <verify>
    <automated>cd apps/web; npm run build</automated>
  </verify>
  <done>
    types.ts has 8 new type definitions (UserRole, AuthUser, InspectionRecord, DocumentMeta, RiskComponent, RiskScore, OverrideField, EngineerOverride). mock-data.ts has 6 new export functions (mockInspections, mockDocuments, mockAddDocument, mockRiskScore, mockOverrides, mockAddOverride). client.ts has 4 new query hooks (useInspections, useDocuments, useRiskScore, useOverrides). auth-store.ts created with persist middleware. Build passes.
  </done>
  <acceptance_criteria>
    - lib/api/types.ts contains 'export type UserRole' with union 'admin' | 'engineer' | 'inspector' | 'viewer'
    - lib/api/types.ts contains 'export interface InspectionRecord'
    - lib/api/types.ts contains 'export interface DocumentMeta'
    - lib/api/types.ts contains 'export interface RiskScore'
    - lib/api/types.ts contains 'export interface EngineerOverride'
    - lib/api/mock-data.ts contains 'export function mockInspections'
    - lib/api/mock-data.ts contains 'export function mockDocuments'
    - lib/api/mock-data.ts contains 'export function mockRiskScore'
    - lib/api/mock-data.ts contains 'export function mockOverrides'
    - lib/api/client.ts contains 'export function useInspections'
    - lib/api/client.ts contains 'export function useDocuments'
    - lib/api/client.ts contains 'export function useRiskScore'
    - lib/api/client.ts contains 'export function useOverrides'
    - lib/stores/auth-store.ts contains 'useAuthStore' and 'persist'
    - lib/stores/auth-store.ts contains 'login' and 'logout' and 'hasRole'
    - npm run build exits with 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 3 i18n keys to all 3 message files</name>
  <files>
    apps/web/messages/en.json,
    apps/web/messages/ru.json,
    apps/web/messages/kk.json
  </files>
  <read_first>
    apps/web/messages/en.json (existing namespaces: common, nav, home, dashboard, map, passport, filter, objects, copilot, reports, hydrofinder),
    apps/web/messages/ru.json (same structure with Russian translations),
    apps/web/messages/kk.json (same structure with Kazakh translations — includes Kazakh-specific chars)
  </read_first>
  <behavior>
    - All 3 message files contain new namespaces: inspection, documents, risk, auth, exportNs, override, passportTabs
    - Russian and Kazakh translations use proper Cyrillic
    - Kazakh translations include Kazakh-specific characters where applicable
    - JSON is valid (no trailing commas, proper escaping)
    - Build succeeds with updated messages
  </behavior>
  <action>
Add these new namespaces to ALL 3 message files (en.json, ru.json, kk.json). The keys are identical across files — only the values differ by language.

**English (en.json) additions:**

```json
"inspection": {
  "title": "Inspection History",
  "timeline": "Timeline",
  "date": "Date",
  "inspector": "Inspector",
  "findings": "Findings",
  "photos": "Photos",
  "conditionAtInspection": "Condition at Inspection",
  "noInspections": "No inspections recorded",
  "loading": "Loading inspections..."
},
"documents": {
  "title": "Documents",
  "upload": "Upload Document",
  "dragDrop": "Drag and drop files here or click to browse",
  "browse": "Browse",
  "filename": "Filename",
  "fileType": "Type",
  "fileSize": "Size",
  "uploadedBy": "Uploaded By",
  "uploadedAt": "Uploaded At",
  "download": "Download",
  "noDocuments": "No documents attached",
  "uploading": "Uploading...",
  "uploadSuccess": "Document uploaded successfully",
  "uploadError": "Upload failed",
  "fileTooLarge": "File size exceeds 10MB limit",
  "invalidFileType": "Invalid file type. Allowed: PDF, JPG, PNG",
  "loading": "Loading documents..."
},
"risk": {
  "title": "Risk Assessment",
  "overallScore": "Overall Risk Score",
  "riskLevel": "Risk Level",
  "high": "High",
  "medium": "Medium",
  "low": "Low",
  "componentBreakdown": "Component Breakdown",
  "structural": "Structural Integrity",
  "hydrological": "Hydrological Risk",
  "operational": "Operational Status",
  "age": "Infrastructure Age",
  "weight": "Weight",
  "score": "Score",
  "explanation": "Explanation",
  "computedAt": "Computed At",
  "loading": "Loading risk assessment..."
},
"auth": {
  "login": "Login",
  "logout": "Logout",
  "loginTitle": "Sign In",
  "loginSubtitle": "Select your role to access the system",
  "role": "Role",
  "roleAdmin": "Administrator",
  "roleEngineer": "Engineer",
  "roleInspector": "Inspector",
  "roleViewer": "Viewer",
  "roleAdminDesc": "Full access to all features and settings",
  "roleEngineerDesc": "Can override inspection intervals and repair statuses",
  "roleInspectorDesc": "Can capture field inspections and upload documents",
  "roleViewerDesc": "Read-only access to maps and passports",
  "signIn": "Sign In",
  "welcome": "Welcome, {name}",
  "notLoggedIn": "Not signed in",
  "loginRequired": "Login required to access this feature",
  "permissionDenied": "You do not have permission to perform this action",
  "requiredRoles": "Required role: {roles}"
},
"exportNs": {
  "title": "Export Data",
  "subtitle": "Download structure data and inspection reports",
  "format": "Format",
  "csv": "CSV (Spreadsheet)",
  "geojson": "GeoJSON (GIS)",
  "pdf": "PDF (Inspection Report)",
  "export": "Export",
  "download": "Download",
  "selectStructures": "Structures to Export",
  "allStructures": "All Structures",
  "filteredStructures": "Filtered Structures",
  "selectStructure": "Select a structure for inspection report",
  "generating": "Generating export...",
  "exportComplete": "Export downloaded successfully",
  "exportError": "Export failed",
  "csvDescription": "Export structure list as CSV for spreadsheet analysis",
  "geojsonDescription": "Export structures as GeoJSON for GIS applications",
  "pdfDescription": "Generate a PDF inspection report for a specific structure"
},
"override": {
  "title": "Engineer Override",
  "field": "Field to Override",
  "inspectionInterval": "Inspection Interval",
  "repairStatus": "Repair Status",
  "originalValue": "Current Value",
  "newValue": "New Value",
  "reason": "Reason for Override",
  "reasonPlaceholder": "Provide a detailed engineering justification...",
  "engineer": "Engineer",
  "timestamp": "Override Date",
  "submit": "Submit Override",
  "cancel": "Cancel",
  "provenanceLog": "Override History",
  "noOverrides": "No overrides recorded",
  "overrideSuccess": "Override submitted successfully",
  "reasonRequired": "Reason must be at least 10 characters",
  "valueRequired": "New value is required",
  "loading": "Loading override history..."
},
"passportTabs": {
  "overview": "Overview",
  "inspections": "Inspections",
  "risk": "Risk",
  "documents": "Documents"
}
```

**Russian (ru.json) additions** — same keys, Russian values:

```json
"inspection": {
  "title": "История осмотров",
  "timeline": "Хронология",
  "date": "Дата",
  "inspector": "Инспектор",
  "findings": "Результаты",
  "photos": "Фотографии",
  "conditionAtInspection": "Состояние при осмотре",
  "noInspections": "Осмотры не зарегистрированы",
  "loading": "Загрузка осмотров..."
},
"documents": {
  "title": "Документы",
  "upload": "Загрузить документ",
  "dragDrop": "Перетащите файлы сюда или нажмите для выбора",
  "browse": "Обзор",
  "filename": "Имя файла",
  "fileType": "Тип",
  "fileSize": "Размер",
  "uploadedBy": "Загрузил",
  "uploadedAt": "Дата загрузки",
  "download": "Скачать",
  "noDocuments": "Документы не прикреплены",
  "uploading": "Загрузка...",
  "uploadSuccess": "Документ успешно загружен",
  "uploadError": "Ошибка загрузки",
  "fileTooLarge": "Размер файла превышает 10 МБ",
  "invalidFileType": "Неверный тип файла. Допустимо: PDF, JPG, PNG",
  "loading": "Загрузка документов..."
},
"risk": {
  "title": "Оценка риска",
  "overallScore": "Общий балл риска",
  "riskLevel": "Уровень риска",
  "high": "Высокий",
  "medium": "Средний",
  "low": "Низкий",
  "componentBreakdown": "Структура риска",
  "structural": "Конструктивная целостность",
  "hydrological": "Гидрологический риск",
  "operational": "Эксплуатационное состояние",
  "age": "Возраст инфраструктуры",
  "weight": "Вес",
  "score": "Балл",
  "explanation": "Объяснение",
  "computedAt": "Дата расчёта",
  "loading": "Загрузка оценки риска..."
},
"auth": {
  "login": "Вход",
  "logout": "Выход",
  "loginTitle": "Вход в систему",
  "loginSubtitle": "Выберите вашу роль для доступа",
  "role": "Роль",
  "roleAdmin": "Администратор",
  "roleEngineer": "Инженер",
  "roleInspector": "Инспектор",
  "roleViewer": "Наблюдатель",
  "roleAdminDesc": "Полный доступ ко всем функциям и настройкам",
  "roleEngineerDesc": "Может изменять интервалы осмотров и статусы ремонта",
  "roleInspectorDesc": "Может проводить полевые осмотры и загружать документы",
  "roleViewerDesc": "Только просмотр карт и паспортов",
  "signIn": "Войти",
  "welcome": "Добро пожаловать, {name}",
  "notLoggedIn": "Не выполнен вход",
  "loginRequired": "Требуется вход для доступа к этой функции",
  "permissionDenied": "У вас нет прав для выполнения этого действия",
  "requiredRoles": "Требуемая роль: {roles}"
},
"exportNs": {
  "title": "Экспорт данных",
  "subtitle": "Скачайте данные о сооружениях и отчёты по осмотрам",
  "format": "Формат",
  "csv": "CSV (Таблица)",
  "geojson": "GeoJSON (ГИС)",
  "pdf": "PDF (Отчёт по осмотру)",
  "export": "Экспорт",
  "download": "Скачать",
  "selectStructures": "Сооружения для экспорта",
  "allStructures": "Все сооружения",
  "filteredStructures": "Отфильтрованные сооружения",
  "selectStructure": "Выберите сооружение для отчёта",
  "generating": "Генерация экспорта...",
  "exportComplete": "Экспорт успешно завершён",
  "exportError": "Ошибка экспорта",
  "csvDescription": "Экспорт списка сооружений в CSV для анализа в таблице",
  "geojsonDescription": "Экспорт сооружений в GeoJSON для ГИС-приложений",
  "pdfDescription": "Создание PDF-отчёта по осмотру для конкретного сооружения"
},
"override": {
  "title": "Инженерное решение",
  "field": "Поле для изменения",
  "inspectionInterval": "Интервал осмотра",
  "repairStatus": "Статус ремонта",
  "originalValue": "Текущее значение",
  "newValue": "Новое значение",
  "reason": "Причина изменения",
  "reasonPlaceholder": "Укажите инженерное обоснование...",
  "engineer": "Инженер",
  "timestamp": "Дата изменения",
  "submit": "Применить",
  "cancel": "Отмена",
  "provenanceLog": "История изменений",
  "noOverrides": "Изменения не зарегистрированы",
  "overrideSuccess": "Изменение успешно применено",
  "reasonRequired": "Причина должна содержать не менее 10 символов",
  "valueRequired": "Новое значение обязательно",
  "loading": "Загрузка истории изменений..."
},
"passportTabs": {
  "overview": "Обзор",
  "inspections": "Осмотры",
  "risk": "Риск",
  "documents": "Документы"
}
```

**Kazakh (kk.json) additions** — same keys, Kazakh values (include Kazakh-specific chars ә, ғ, қ, ң, ө, ұ, ү, һ, і):

```json
"inspection": {
  "title": "Қарап шығу тарихы",
  "timeline": "Уақыт шкаласы",
  "date": "Күні",
  "inspector": "Қарап шыққан маман",
  "findings": "Нәтижелер",
  "photos": "Суреттер",
  "conditionAtInspection": "Қарап шығу кезіндегі жағдай",
  "noInspections": "Қарап шығулар тіркелмеген",
  "loading": "Қарап шығулар жүктелуде..."
},
"documents": {
  "title": "Құжаттар",
  "upload": "Құжат жүктеу",
  "dragDrop": "Файлдарды осында тартыңыз немесе таңдау үшін басыңыз",
  "browse": "Таңдау",
  "filename": "Файл атауы",
  "fileType": "Түрі",
  "fileSize": "Өлшемі",
  "uploadedBy": "Жүктеген",
  "uploadedAt": "Жүктеу күні",
  "download": "Жүктеу",
  "noDocuments": "Құжаттар тіркелмеген",
  "uploading": "Жүктелуде...",
  "uploadSuccess": "Құжат сәтті жүктелді",
  "uploadError": "Жүктеу қатесі",
  "fileTooLarge": "Файл өлшемі 10 МБ-тан аспауы тиіс",
  "invalidFileType": "Дұрыс емес файл түрі. Рұқсат етілген: PDF, JPG, PNG",
  "loading": "Құжаттар жүктелуде..."
},
"risk": {
  "title": "Тәуекелді бағалау",
  "overallScore": "Жалпы тәуекел баллы",
  "riskLevel": "Тәуекел деңгейі",
  "high": "Жоғары",
  "medium": "Орташа",
  "low": "Төмен",
  "componentBreakdown": "Тәуекел құрылымы",
  "structural": "Құрылымдық беріктік",
  "hydrological": "Гидрологиялық тәуекел",
  "operational": "Пайдалану жағдайы",
  "age": "Инфрақұрылым жасы",
  "weight": "Салмағы",
  "score": "Балл",
  "explanation": "Түсіндірме",
  "computedAt": "Есептеу күні",
  "loading": "Тәуекелді бағалау жүктелуде..."
},
"auth": {
  "login": "Кіру",
  "logout": "Шығу",
  "loginTitle": "Жүйеге кіру",
  "loginSubtitle": "Қол жеткізу үшін рөліңізді таңдаңыз",
  "role": "Рөл",
  "roleAdmin": "Әкімші",
  "roleEngineer": "Инженер",
  "roleInspector": "Қарап шығушы",
  "roleViewer": "Қараушы",
  "roleAdminDesc": "Барлық функцияларға толық қол жеткізу",
  "roleEngineerDesc": "Қарап шығу аралықтарын және жөндеу күйлерін өзгерте алады",
  "roleInspectorDesc": "Дала қарап шығуларын жүргізе алады және құжаттар жүктей алады",
  "roleViewer": "Карталар мен паспорттарды тек көруге",
  "signIn": "Кіру",
  "welcome": "Қош келдіңіз, {name}",
  "notLoggedIn": "Кірілмеген",
  "loginRequired": "Бұл функцияға қол жеткізу үшін кіру қажет",
  "permissionDenied": "Бұл әрекетті орындауға құқығыңыз жоқ",
  "requiredRoles": "Қажетті рөл: {roles}"
},
"exportNs": {
  "title": "Деректерді экспорттау",
  "subtitle": "Құрылымдар туралы деректерді және қарап шығу есептерін жүктеңіз",
  "format": "Пішім",
  "csv": "CSV (Кесте)",
  "geojson": "GeoJSON (ГИС)",
  "pdf": "PDF (Қарап шығу есебі)",
  "export": "Экспорт",
  "download": "Жүктеу",
  "selectStructures": "Экспортталатын құрылымдар",
  "allStructures": "Барлық құрылымдар",
  "filteredStructures": "Сүзгіленген құрылымдар",
  "selectStructure": "Есеп үшін құрылымды таңдаңыз",
  "generating": "Экспорт жасалуда...",
  "exportComplete": "Экспорт сәтті жүктелді",
  "exportError": "Экспорт қатесі",
  "csvDescription": "Құрылымдар тізімін CSV пішімінде экспорттау",
  "geojsonDescription": "Құрылымдарды GeoJSON пішімінде экспорттау",
  "pdfDescription": "Нақты құрылым үшін PDF қарап шығу есебін жасау"
},
"override": {
  "title": "Инженерлік шешім",
  "field": "Өзгертуге арналған өріс",
  "inspectionInterval": "Қарап шығу аралығы",
  "repairStatus": "Жөндеу күйі",
  "originalValue": "Ағымдағы мән",
  "newValue": "Жаңа мән",
  "reason": "Өзгерту себебі",
  "reasonPlaceholder": "Инженерлік негіздемені көрсетіңіз...",
  "engineer": "Инженер",
  "timestamp": "Өзгерту күні",
  "submit": "Қолдану",
  "cancel": "Бас тарту",
  "provenanceLog": "Өзгерту тарихы",
  "noOverrides": "Өзгертулер тіркелмеген",
  "overrideSuccess": "Өзгерту сәтті қолданылды",
  "reasonRequired": "Себеп кемінде 10 таңбадан тұруы тиіс",
  "valueRequired": "Жаңа мән міндетті",
  "loading": "Өзгерту тарихы жүктелуде..."
},
"passportTabs": {
  "overview": "Шолу",
  "inspections": "Қарап шығулар",
  "risk": "Тәуекел",
  "documents": "Құжаттар"
}
```

Each namespace must be added as a new top-level key in the JSON object, alongside the existing namespaces (common, nav, home, dashboard, map, passport, filter, objects, copilot, reports, hydrofinder). Ensure valid JSON — no trailing commas, proper escaping of quotes.
  </action>
  <verify>
    <automated>cd apps/web; npm run build</automated>
  </verify>
  <done>
    All 3 message files contain 7 new namespaces (inspection, documents, risk, auth, exportNs, override, passportTabs) with trilingual translations. JSON is valid. Build passes.
  </done>
  <acceptance_criteria>
    - messages/en.json contains "inspection", "documents", "risk", "auth", "exportNs", "override", "passportTabs" top-level keys
    - messages/ru.json contains the same 7 top-level keys with Russian translations
    - messages/kk.json contains the same 7 top-level keys with Kazakh translations
    - messages/kk.json contains Kazakh-specific chars (ә, ғ, қ, ң, ө, ұ, ү, һ, і) in at least one value
    - npm run build exits with 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unit tests for Phase 3 mock data and auth store</name>
  <files>
    apps/web/tests/phase3-mocks.test.ts
  </files>
  <read_first>
    apps/web/tests/mock-data.test.ts (existing Vitest test pattern for mock data),
    apps/web/tests/stores.test.ts (existing Vitest test pattern for Zustand stores),
    apps/web/vitest.config.ts (environment=jsdom, include=['tests/**/*.test.ts'], resolve alias @ to __dirname),
    apps/web/lib/api/types.ts (new types from Task 2),
    apps/web/lib/api/mock-data.ts (new mock functions from Task 2),
    apps/web/lib/stores/auth-store.ts (new auth store from Task 2)
  </read_first>
  <behavior>
    - mockInspections returns array sorted by date descending
    - mockInspections returns at least 3 records per structure
    - Each InspectionRecord has id, structureId, date, inspectorName, findings, photoUrls, conditionAtInspection
    - mockDocuments returns array with DocumentMeta objects
    - mockRiskScore returns RiskScore with overall (0-100), 4 components, explanation
    - mockRiskScore overall is weighted sum of component scores
    - mockOverrides returns array of EngineerOverride objects
    - mockAddDocument returns DocumentMeta with generated id and uploadedAt date
    - mockAddOverride returns EngineerOverride with generated id and timestamp
    - useAuthStore login sets user with correct role
    - useAuthStore logout clears user to null
    - useAuthStore hasRole returns true for matching role, false for non-matching
    - useAuthStore hasRole returns false when user is null
  </behavior>
  <action>
Create tests/phase3-mocks.test.ts (Vitest unit test). Import describe, it, expect, beforeEach, afterEach from vitest. Import mockInspections, mockDocuments, mockRiskScore, mockOverrides, mockAddDocument, mockAddOverride from @/lib/api/mock-data. Import useAuthStore from @/lib/stores/auth-store. Import types for reference.

Write test suites:

1. `describe('mockInspections')`:
   - Returns array with at least 3 records for 'KZ-ZH-0001'
   - Records are sorted by date descending (first date >= last date)
   - Each record has id, structureId, date, inspectorName, findings, photoUrls (array), conditionAtInspection
   - structureId in each record matches the input structureId
   - Returns empty array for non-existent structure (e.g., 'KZ-ZH-9999' returns [] or very few — actually mockInspections generates based on seed, so test for at least 3 for any valid-format ID)

2. `describe('mockDocuments')`:
   - Returns array of DocumentMeta objects for 'KZ-ZH-0001'
   - Each document has id, structureId, filename, fileType, fileSize, uploadedBy, uploadedAt, downloadUrl
   - downloadUrl starts with '#mock-download'

3. `describe('mockRiskScore')`:
   - Returns RiskScore for 'KZ-ZH-0001'
   - overall is a number between 0 and 100
   - components array has 4 items (structural, hydrological, operational, age)
   - Each component has key, label, score (0-100), weight (0-1), description
   - explanation is a non-empty string
   - computedAt is a date string

4. `describe('mockOverrides')`:
   - Returns array of EngineerOverride objects for 'KZ-ZH-0001'
   - Each override has id, structureId, field, originalValue, newValue, reason, engineerName, timestamp
   - field is either 'inspection_interval' or 'repair_status'

5. `describe('mockAddDocument')`:
   - Returns DocumentMeta with generated id containing structureId
   - uploadedAt is today's date (YYYY-MM-DD format)
   - filename and fileSize match input

6. `describe('mockAddOverride')`:
   - Returns EngineerOverride with generated id
   - timestamp is today's date
   - field, originalValue, newValue, reason, engineerName match input

7. `describe('useAuthStore')`:
   - beforeEach: logout to clear state
   - Initial state: user is null (or persists from previous test — use logout in beforeEach to reset)
   - login('engineer') sets user with role 'engineer'
   - login('admin') sets user with role 'admin'
   - logout() sets user to null
   - hasRole('engineer') returns true when logged in as engineer
   - hasRole('admin') returns false when logged in as engineer
   - hasRole('admin', 'engineer') returns true when logged in as engineer (multiple roles check)
   - hasRole('admin') returns false when user is null (not logged in)

Note: useAuthStore uses persist middleware with localStorage. In jsdom environment, localStorage may not be available. If tests fail due to localStorage, mock localStorage in beforeEach: set global.localStorage with getItem/setItem/removeItem implementations. Alternatively, if zustand persist handles the missing localStorage gracefully (returns undefined), the tests should work. If not, add a localStorage mock at the top of the test file.
  </action>
  <verify>
    <automated>cd apps/web; npx vitest run tests/phase3-mocks.test.ts</automated>
  </verify>
  <done>
    phase3-mocks.test.ts passes all tests. Mock data functions produce valid typed data. Auth store login/logout/hasRole work correctly. All existing tests still pass.
  </done>
  <acceptance_criteria>
    - npx vitest run tests/phase3-mocks.test.ts exits with 0 (all Phase 3 mock tests pass)
    - npx vitest run exits with 0 (all unit tests pass, including Phase 1-2 tests)
    - tests/phase3-mocks.test.ts contains describe blocks for mockInspections, mockDocuments, mockRiskScore, mockOverrides, mockAddDocument, mockAddOverride, useAuthStore
    - tests/phase3-mocks.test.ts tests auth store hasRole with matching and non-matching roles
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → package.json | Package installation from npm registry — supply chain risk |
| Mock data → TanStack Query → Components | Phase 3 mock data (inspections, documents, risk, overrides) flows through query hooks to components |
| Auth store → localStorage | Auth state persisted to localStorage — accessible to any script on the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering (Supply Chain) | npm package installs | high | mitigate | Package Legitimacy Audit in RESEARCH.md: all 7 packages verified OK. No postinstall scripts. All are established packages with millions of weekly downloads. |
| T-03-02 | Spoofing (fake auth) | auth-store.ts | medium | accept | Mock auth is intentionally insecure — no real authentication. This is a frontend-only mock for MVP. Real auth will use JWT/OAuth from backend. Documented in code comments and RESEARCH.md. |
| T-03-03 | Information Disclosure | localStorage auth | low | accept | Auth store persists role/name to localStorage. No sensitive data (no passwords, no tokens). Acceptable for MVP. Real auth will use httpOnly cookies. |
</threat_model>

<verification>
- npm run build passes without errors
- npx vitest run passes all unit tests (phase3-mocks + Phase 1-2 tests)
- All 7 npm packages installed with pinned versions
- All 13 shadcn/ui component files exist
- All 3 message files have 7 new namespaces
- Auth store, types, mock data, and API hooks compile correctly
</verification>

<success_criteria>
- 7 Phase 3 npm packages installed with pinned versions
- 13 shadcn/ui components installed (dialog, tabs, table, tooltip, alert, textarea, label, input, progress, accordion, dropdown-menu, avatar, scroll-area)
- TypeScript types for InspectionRecord, DocumentMeta, RiskScore, EngineerOverride, UserRole defined
- Mock data functions for inspections, documents, risk scores, overrides implemented
- TanStack Query hooks (useInspections, useDocuments, useRiskScore, useOverrides) implemented
- Auth store with persist middleware and 4 roles implemented
- 7 new i18n namespaces added to all 3 message files (RU/KK/EN)
- Unit tests for Phase 3 mock data and auth store pass
- All Phase 1-2 tests still pass (no regressions)
</success_criteria>

<output>
Create `.planning/workstreams/frontend/phases/03-inspection-risk-ui/03-01-SUMMARY.md` when done
</output>
