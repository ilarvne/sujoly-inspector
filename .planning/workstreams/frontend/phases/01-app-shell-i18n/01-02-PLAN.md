---
phase: "01"
plan: "02"
wave: 1
depends_on: ["01-01"]
must_haves:
  - "i18n routing configured (routing.ts, navigation.ts, request.ts) with defineRouting/createNavigation API"
  - "next-intl middleware created for locale detection and URL routing"
  - "Trilingual message files (en/ru/kk) with all navigation, page titles, and common.appTitle"
  - "Kazakh message file includes all 9 Kazakh-specific Cyrillic characters including һ (U+04BB)"
  - "Locale layout with Inter+Manrope fonts (cyrillic + cyrillic-ext subsets), NextIntlClientProvider, globals.css import"
  - "globals.css @theme configured with design tokens (OKLCH primary, 6 status colors, font mappings)"
files_modified:
  - "apps/web/i18n/routing.ts"
  - "apps/web/i18n/navigation.ts"
  - "apps/web/i18n/request.ts"
  - "apps/web/middleware.ts"
  - "apps/web/messages/en.json"
  - "apps/web/messages/ru.json"
  - "apps/web/messages/kk.json"
  - "apps/web/app/[locale]/layout.tsx"
  - "apps/web/app/[locale]/not-found.tsx"
  - "apps/web/app/not-found.tsx"
  - "apps/web/app/globals.css"
  - "apps/web/app/layout.tsx (deleted)"
  - "apps/web/app/page.tsx (deleted)"
autonomous: true
type: implementation
---

# Plan 01-02: Waves 1+2 — i18n Foundation & Design System

## Context

This plan covers Wave 1 (i18n foundation: routing, middleware, messages, locale layout) and Wave 2 (design system: globals.css @theme tokens). These waves touch disjoint file sets and can execute in parallel — Wave 1 creates `i18n/`, `middleware.ts`, `messages/`, and `app/[locale]/layout.tsx`; Wave 2 modifies `app/globals.css`.

**Critical fixes applied in this plan:**
- **B1:** `app/[locale]/layout.tsx` imports `../globals.css` — without it, no Tailwind, no @theme tokens, no font CSS variables
- **B3:** `kk.json` `home.subtitle` includes "жаһандық" to provide the һ character (U+04BB) required by fonts.spec.ts
- **W6:** All 3 message files include `common.appTitle` for localized app header title

## Tasks

<task id="01-05">
### Create i18n Routing Files

<files>
- apps/web/i18n/routing.ts (new)
- apps/web/i18n/navigation.ts (new)
- apps/web/i18n/request.ts (new)
</files>

<action>
Create `apps/web/i18n/routing.ts`:
```typescript
import { defineRouting } from 'next-intl/routing';

export const routing = defineRouting({
  locales: ['ru', 'kk', 'en'],
  defaultLocale: 'ru',
});
```

Create `apps/web/i18n/navigation.ts`:
```typescript
import { createNavigation } from 'next-intl/navigation';
import { routing } from './routing';

export const { Link, redirect, usePathname, useRouter, getPathname } =
  createNavigation(routing);
```

Create `apps/web/i18n/request.ts`:
```typescript
import { getRequestConfig } from 'next-intl/server';
import { hasLocale } from 'next-intl';
import { routing } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale = hasLocale(routing.locales, requested)
    ? requested
    : routing.defaultLocale;

  return {
    locale,
    messages: (await import(`../messages/${locale}.json`)).default,
  };
});
```
</action>

<verify>
```bash
cd apps/web && npm run build
```
Build may warn about missing messages/ files (created in 01-07), but routing config should be valid.
</verify>

<done>
- [ ] i18n/routing.ts created with defineRouting (locales: ru/kk/en, defaultLocale: ru)
- [ ] i18n/navigation.ts created with createNavigation(routing)
- [ ] i18n/request.ts created with getRequestConfig and hasLocale validation
- [ ] npm run build succeeds (or warns only about missing messages)
</done>
</task>

<task id="01-06">
### Create middleware.ts

<files>
- apps/web/middleware.ts (new)
</files>

<action>
Create `apps/web/middleware.ts`:
```typescript
import createMiddleware from 'next-intl/middleware';
import { routing } from './i18n/routing';

export default createMiddleware(routing);

export const config = {
  matcher: '/((?!api|trpc|_next|_vercel|.*\\..*).*)',
};
```
</action>

<verify>
```bash
cd apps/web && npm run build
```
Build completes without errors.
</verify>

<done>
- [ ] middleware.ts created at apps/web/ root
- [ ] Uses createMiddleware(routing) from next-intl
- [ ] Matcher excludes api, trpc, _next, _vercel, and static files
- [ ] npm run build succeeds
</done>
</task>

<task id="01-07">
### Create Message Files (en, ru, kk)

<files>
- apps/web/messages/en.json (new)
- apps/web/messages/ru.json (new)
- apps/web/messages/kk.json (new)
</files>

<action>
Create `apps/web/messages/en.json`:
```json
{
  "common": {
    "appTitle": "Zhambyl Hydraulic Structures",
    "locale": {
      "ru": "Русский",
      "kk": "Қазақша",
      "en": "English"
    }
  },
  "nav": {
    "home": "Home",
    "dashboard": "Dashboard",
    "map": "Map",
    "objects": "Objects",
    "copilot": "Copilot",
    "reports": "Reports",
    "hydrofinder": "HydroFinder"
  },
  "home": {
    "title": "Zhambyl Hydraulic Structures Catalog",
    "subtitle": "Digital operating system for hydraulic structure management"
  },
  "dashboard": {
    "title": "Dashboard",
    "subtitle": "Portfolio analytics and overview"
  },
  "map": {
    "title": "Map",
    "subtitle": "Interactive map of hydraulic structures"
  },
  "objects": {
    "title": "Objects",
    "subtitle": "Structure catalog and registry"
  },
  "copilot": {
    "title": "Copilot",
    "subtitle": "AI assistant for hydraulic structure analysis"
  },
  "reports": {
    "title": "Reports",
    "subtitle": "Export and report generation"
  },
  "hydrofinder": {
    "title": "HydroFinder",
    "subtitle": "Structure discovery and candidate search"
  }
}
```

Create `apps/web/messages/ru.json`:
```json
{
  "common": {
    "appTitle": "Жамбыл ГТС",
    "locale": {
      "ru": "Русский",
      "kk": "Қазақша",
      "en": "English"
    }
  },
  "nav": {
    "home": "Главная",
    "dashboard": "Панель управления",
    "map": "Карта",
    "objects": "Объекты",
    "copilot": "Помощник",
    "reports": "Отчёты",
    "hydrofinder": "Гидропоиск"
  },
  "home": {
    "title": "Каталог гидротехнических сооружений Жамбылской области",
    "subtitle": "Цифровая операционная система для управления гидротехническими сооружениями"
  },
  "dashboard": {
    "title": "Панель управления",
    "subtitle": "Аналитика и обзор портфеля сооружений"
  },
  "map": {
    "title": "Карта",
    "subtitle": "Интерактивная карта гидротехнических сооружений"
  },
  "objects": {
    "title": "Объекты",
    "subtitle": "Каталог и реестр сооружений"
  },
  "copilot": {
    "title": "Помощник",
    "subtitle": "ИИ-ассистент для анализа гидротехнических сооружений"
  },
  "reports": {
    "title": "Отчёты",
    "subtitle": "Экспорт и формирование отчётов"
  },
  "hydrofinder": {
    "title": "Гидропоиск",
    "subtitle": "Обнаружение сооружений и поиск кандидатов"
  }
}
```

Create `apps/web/messages/kk.json` — **B3 FIX: home.subtitle includes "жаһандық" to provide һ (U+04BB) required by fonts.spec.ts:**
```json
{
  "common": {
    "appTitle": "Жамбыл СҚ",
    "locale": {
      "ru": "Русский",
      "kk": "Қазақша",
      "en": "English"
    }
  },
  "nav": {
    "home": "Басты бет",
    "dashboard": "Басқару панелі",
    "map": "Карта",
    "objects": "Нысандар",
    "copilot": "Көмекші",
    "reports": "Есептер",
    "hydrofinder": "Гидроіздеу"
  },
  "home": {
    "title": "Жамбыл облысының гидротехникалық құрылымдар каталогы",
    "subtitle": "Гидротехникалық құрылымдарды жаһандық басқару үшін цифрлық операциялық жүйе"
  },
  "dashboard": {
    "title": "Басқару панелі",
    "subtitle": "Су шаруашылығы нысандарының аналитикасы мен шолуы"
  },
  "map": {
    "title": "Карта",
    "subtitle": "Гидротехникалық құрылымдардың интерактивті картасы"
  },
  "objects": {
    "title": "Нысандар",
    "subtitle": "Құрылымдар каталогы мен тізілімі"
  },
  "copilot": {
    "title": "Көмекші",
    "subtitle": "Гидротехникалық құрылымдарды талдауға арналған ЖИ көмекшісі"
  },
  "reports": {
    "title": "Есептер",
    "subtitle": "Экспорт және есептерді құру"
  },
  "hydrofinder": {
    "title": "Гидроіздеу",
    "subtitle": "Құрылымдарды табу және үміткерлерді іздеу"
  }
}
```

**Kazakh character coverage in kk.json (all 9 characters present):**
| Char | Example word | Location |
|------|-------------|----------|
| ә | Қазақша | common.locale.kk |
| ғ | шаруашылығы | dashboard.subtitle |
| қ | гидротехникалық, құрылымдар | home.title, multiple |
| ң | облысының | home.title |
| ө | Көмекші | nav.copilot |
| ұ | құрылымдар | home.title |
| ү | үшін, жүйе | home.subtitle |
| һ | жаһандық | home.subtitle (B3 fix) |
| і | Гидроіздеу, үшін | nav.hydrofinder, home.subtitle |

**W6 FIX: `common.appTitle` added to all 3 message files:**
- en: "Zhambyl Hydraulic Structures"
- ru: "Жамбыл ГТС"
- kk: "Жамбыл СҚ" (Су Құрылғылары = Hydraulic Structures)
</action>

<verify>
```bash
cd apps/web && npm run build
```
Build completes, messages resolve. Verify all 9 Kazakh characters are present in kk.json:
```bash
cd apps/web && node -e "const m=require('./messages/kk.json'); const s=JSON.stringify(m); ['ә','ғ','қ','ң','ө','ұ','ү','һ','і'].forEach(c=>{if(!s.includes(c))throw new Error('Missing '+c)}); console.log('All 9 Kazakh chars present')"
```
</verify>

<done>
- [ ] en.json created with common.appTitle, nav, and all 7 page namespaces
- [ ] ru.json created with common.appTitle ("Жамбыл ГТС"), nav, and all 7 page namespaces
- [ ] kk.json created with common.appTitle ("Жамбыл СҚ"), nav, and all 7 page namespaces
- [ ] kk.json home.subtitle contains "жаһандық" (includes һ U+04BB) — B3 fix
- [ ] All 9 Kazakh-specific characters present in kk.json
- [ ] npm run build succeeds
</done>
</task>

<task id="01-08">
### Create Locale Layout with Fonts + Providers + globals.css Import

<files>
- apps/web/app/[locale]/layout.tsx (new)
- apps/web/app/[locale]/not-found.tsx (new)
- apps/web/app/not-found.tsx (new)
- apps/web/app/layout.tsx (deleted — replaced by [locale]/layout.tsx)
- apps/web/app/page.tsx (deleted — replaced by [locale]/page.tsx in 01-12)
</files>

<action>
**B1 FIX: This layout MUST import `../globals.css` — without it, no Tailwind utilities, no @theme design tokens, and no font CSS variables are available. This is the root layout with `<html>`/`<body>`, so the CSS import must be here.**

Delete the default `app/layout.tsx` and `app/page.tsx` from create-next-app (replaced by the [locale] structure).

Create `apps/web/app/[locale]/layout.tsx`:
```tsx
import '../globals.css';
import { setRequestLocale } from 'next-intl/server';
import { hasLocale } from 'next-intl';
import { notFound } from 'next/navigation';
import { NextIntlClientProvider } from 'next-intl';
import { Inter, Manrope } from 'next/font/google';
import { routing } from '@/i18n/routing';
import { getMessages } from 'next-intl/server';

const inter = Inter({
  subsets: ['latin', 'cyrillic', 'cyrillic-ext'],
  display: 'swap',
  variable: '--font-inter',
});

const manrope = Manrope({
  subsets: ['latin', 'cyrillic', 'cyrillic-ext'],
  display: 'swap',
  variable: '--font-manrope',
});

export function generateStaticParams() {
  return routing.locales.map((locale) => ({ locale }));
}

type Props = {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
};

export default async function LocaleLayout({ children, params }: Props) {
  const { locale } = await params;
  if (!hasLocale(routing.locales, locale)) {
    notFound();
  }

  setRequestLocale(locale);
  const messages = await getMessages();

  return (
    <html lang={locale} className={`${inter.variable} ${manrope.variable} antialiased`}>
      <body>
        <NextIntlClientProvider messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

Create `apps/web/app/[locale]/not-found.tsx`:
```tsx
export default function NotFound() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-lg text-muted-foreground">404 — Page Not Found</p>
    </div>
  );
}
```

Create `apps/web/app/not-found.tsx`:
```tsx
export default function GlobalNotFound() {
  return (
    <div className="flex min-h-screen items-center justify-center">
      <p className="text-lg">404 — Page Not Found</p>
    </div>
  );
}
```
</action>

<verify>
```bash
cd apps/web && npm run build
```
Build completes, static params generated for all 3 locales. The `import '../globals.css'` line must be present at the top of `app/[locale]/layout.tsx`.
</verify>

<done>
- [ ] app/[locale]/layout.tsx created with `import '../globals.css'` at top (B1 fix)
- [ ] Inter font configured with subsets: ['latin', 'cyrillic', 'cyrillic-ext']
- [ ] Manrope font configured with subsets: ['latin', 'cyrillic', 'cyrillic-ext']
- [ ] generateStaticParams returns all 3 locales
- [ ] setRequestLocale called before rendering
- [ ] hasLocale validation with notFound() for invalid locales
- [ ] NextIntlClientProvider wraps children with messages
- [ ] app/[locale]/not-found.tsx created
- [ ] app/not-found.tsx created
- [ ] app/layout.tsx deleted (replaced by [locale]/layout.tsx)
- [ ] app/page.tsx deleted (replaced by [locale]/page.tsx in 01-12)
- [ ] npm run build succeeds
</done>
</task>

<task id="01-09">
### Configure globals.css @theme with Design Tokens

<files>
- apps/web/app/globals.css (modified)
</files>

<action>
Replace the contents of `apps/web/app/globals.css` with the project design system:
```css
@import "tailwindcss";
@import "tw-animate-css";

:root {
  --background: oklch(0.99 0 0);
  --foreground: oklch(0.15 0.01 230);
  --card: oklch(1 0 0);
  --card-foreground: oklch(0.15 0.01 230);
  --popover: oklch(1 0 0);
  --popover-foreground: oklch(0.15 0.01 230);

  --primary: oklch(0.42 0.08 230);
  --primary-foreground: oklch(0.98 0 0);

  --secondary: oklch(0.96 0.01 230);
  --secondary-foreground: oklch(0.20 0.02 230);

  --muted: oklch(0.96 0 0);
  --muted-foreground: oklch(0.52 0.01 230);

  --accent: oklch(0.95 0.02 230);
  --accent-foreground: oklch(0.20 0.02 230);

  --destructive: oklch(0.55 0.22 25);
  --destructive-foreground: oklch(0.98 0 0);

  --border: oklch(0.90 0.01 230);
  --input: oklch(0.90 0.01 230);
  --ring: oklch(0.42 0.08 230);

  --radius: 0.5rem;
}

@theme inline {
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);

  --font-sans: var(--font-inter), ui-sans-serif, system-ui, sans-serif;
  --font-display: var(--font-manrope), ui-sans-serif, system-ui, sans-serif;

  --radius-lg: var(--radius);
  --radius-md: calc(var(--radius) - 2px);
  --radius-sm: calc(var(--radius) - 4px);
}

@theme {
  --color-status-normal: oklch(0.65 0.18 145);
  --color-status-inspection: oklch(0.80 0.15 90);
  --color-status-repair: oklch(0.70 0.18 55);
  --color-status-critical: oklch(0.55 0.22 25);
  --color-status-unknown: oklch(0.60 0.02 300);
  --color-status-missing: oklch(0.55 0.01 250);
}

@layer base {
  * {
    border-color: var(--border);
  }
  body {
    background-color: var(--background);
    color: var(--foreground);
    font-family: var(--font-sans);
  }
  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font-display);
  }
}
```

**Design token reference:**
| Token | OKLCH | Hex equivalent | Purpose |
|-------|-------|----------------|---------|
| --primary | oklch(0.42 0.08 230) | ~#0b4f6c | Governmental teal-blue |
| --status-normal | oklch(0.65 0.18 145) | green | Normal condition |
| --status-inspection | oklch(0.80 0.15 90) | yellow | Inspection due |
| --status-repair | oklch(0.70 0.18 55) | orange | Repair needed |
| --status-critical | oklch(0.55 0.22 25) | red | Critical condition |
| --status-unknown | oklch(0.60 0.02 300) | purple | Unknown status |
| --status-missing | oklch(0.55 0.01 250) | gray | Missing data |
</action>

<verify>
```bash
cd apps/web && npx vitest run tests/design-tokens.test.ts
```
All design token assertions pass (imports tailwindcss, imports tw-animate-css, --primary is OKLCH, --font-sans and --font-display defined, all 6 status color tokens present).
</verify>

<done>
- [ ] globals.css imports tailwindcss and tw-animate-css
- [ ] :root defines --primary as oklch(0.42 0.08 230) (~#0b4f6c)
- [ ] @theme inline maps shadcn/ui semantic tokens + font variables
- [ ] @theme defines all 6 status color tokens
- [ ] @layer base sets body font-family to --font-sans (Inter)
- [ ] @layer base sets headings font-family to --font-display (Manrope)
- [ ] No tailwind.config.js exists
- [ ] npx vitest run tests/design-tokens.test.ts passes
</done>
</task>

## Success Criteria Coverage

| SC # | Criterion | Covered By Tasks | Verified By |
|------|-----------|-----------------|-------------|
| SC-1 | Next.js + next-intl configured | 01-05, 01-06 | Build passes |
| SC-3 | Kazakh font subsets (cyrillic + cyrillic-ext) | 01-08 | Build + fonts.spec.ts (Wave 4) |
| SC-4 | Design system: #0b4f6c primary, status colors, Inter/Manrope | 01-09 | design-tokens.test.ts |
| UI-01 | i18n routing, middleware, translations | 01-05, 01-06, 01-07 | i18n.spec.ts (Wave 4) |
| UI-02 | Kazakh chars including һ in messages | 01-07, 01-08 | fonts.spec.ts (Wave 4) |

## Execution Notes

- Wave 1 (tasks 01-05 to 01-08) and Wave 2 (task 01-09) touch disjoint file sets and can run in parallel
- **B1 FIX:** `app/[locale]/layout.tsx` MUST import `../globals.css` — this is the root layout
- **B3 FIX:** `kk.json` `home.subtitle` contains "жаһандық" to provide һ (U+04BB) for fonts.spec.ts
- **W6 FIX:** `common.appTitle` added to all 3 message files for localized header title
- MUST include both 'cyrillic' AND 'cyrillic-ext' in font subsets — not just cyrillic-ext
- MUST call setRequestLocale(locale) before rendering — prevents hydration errors
- Delete the default app/layout.tsx and app/page.tsx from create-next-app
