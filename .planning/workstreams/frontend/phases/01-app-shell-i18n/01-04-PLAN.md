---
phase: "01"
plan: "04"
wave: 4
depends_on: ["01-03"]
must_haves:
  - "Full test suite green: Vitest unit tests + Playwright e2e tests"
  - "Production build succeeds"
  - "Manual verification checklist completed (Kazakh rendering, design aesthetic, switcher UX)"
files_modified: []
autonomous: true
type: verification
---

# Plan 01-04: Wave 4 — Verification

## Context

This plan covers the final verification gate for Phase 1. All implementation is complete from Plans 01-01 through 01-03. This plan runs the full test suite (Vitest + Playwright) and performs manual verification of items that cannot be fully automated.

**W8 FIX:** The full Playwright suite runs ONLY in this plan (task 01-15). Previous plans used targeted test files for per-task verification to reduce latency.

## Tasks

<task id="01-15">
### Full Test Suite Run + Fix Failures

<files>
- (no new files — may modify existing files to fix failures)
</files>

<action>
Run the complete test suite and fix any failures:

1. Run production build:
```bash
cd apps/web && npm run build
```

2. Run all Vitest unit tests:
```bash
cd apps/web && npx vitest run
```

3. Run all Playwright e2e tests (full suite):
```bash
cd apps/web && npx playwright test
```

4. If any tests fail, diagnose and fix:
   - Check for missing `setRequestLocale` calls in pages
   - Check for `useTranslations()` in async components (should use `getTranslations` instead — B2)
   - Check for `next/navigation` imports instead of `@/i18n/navigation`
   - Check font subsets include both `cyrillic` and `cyrillic-ext`
   - Check `app/[locale]/layout.tsx` has `import '../globals.css'` (B1)
   - Check `kk.json` `home.subtitle` contains "жаһандық" with һ character (B3)
   - Check `components.json` has empty `"config"` value
   - Check `globals.css` has all required @theme tokens
   - Check AppShell uses `t('appTitle')` not hardcoded string (W6)

5. Re-run until all green.
</action>

<verify>
```bash
cd apps/web && npm run build && npx vitest run && npx playwright test
```
ALL tests green. Build succeeds. Vitest passes (design-tokens.test.ts). Playwright passes (i18n.spec.ts, routes.spec.ts, fonts.spec.ts).
</verify>

<done>
- [ ] npm run build succeeds
- [ ] npx vitest run — all unit tests pass (design-tokens.test.ts: 6 assertions)
- [ ] npx playwright test — all e2e tests pass
- [ ] tests/i18n.spec.ts: 6 tests pass (RU/KK/EN rendering, switcher, 404)
- [ ] tests/routes.spec.ts: 21 tests pass (7 routes x 3 locales = 200)
- [ ] tests/fonts.spec.ts: 3 tests pass (9 Kazakh chars, Manrope headings, Inter body)
- [ ] All failures diagnosed and fixed
</done>
</task>

<task id="01-16">
### Manual Verification Checklist

<files>
- (no files modified — documentation only)
</files>

<action>
Start dev server and perform manual verification:
```bash
cd apps/web && npm run dev
```

Open browser to `http://localhost:3000/ru` (should redirect from `/`).

Walk through each check:

| # | Check | How to Verify | Pass Criteria |
|---|-------|---------------|---------------|
| 1 | Kazakh character rendering | Open `http://localhost:3000/kk` in browser. Inspect ә, ғ, қ, ң, ө, ұ, ү, һ, і in DevTools -> Elements -> Computed -> font-family | Should show "Inter" or "Manrope", not a fallback system font |
| 2 | Governmental design style | Visual inspection of home page at `/ru` | Clean, professional, teal-blue primary (#0b4f6c), no playful gradients or animations |
| 3 | Language switcher UX | Click switcher in header, select each locale (RU, KK, EN) | Page text changes without full page reload; URL updates to /ru, /kk, /en; sidebar labels translate |
| 4 | Font weight rendering | Check headings vs body text in browser | Headings (h1-h6) use Manrope (display), body text uses Inter (sans) |
| 5 | Active nav state | Navigate between routes via sidebar | Current route highlighted with primary color background |
| 6 | 404 for invalid locale | Navigate to `http://localhost:3000/fr` | 404 page displayed |
| 7 | All 7 routes accessible | Click each sidebar link in all 3 locales | Each route renders with translated title and subtitle |
| 8 | Localized app title | Check header text in all 3 locales | EN: "Zhambyl Hydraulic Structures", RU: "Жамбыл ГТС", KK: "Жамбыл СҚ" |

Document any issues found.
</action>

<verify>
All 8 manual checks pass (document results).
</verify>

<done>
- [ ] Check 1: Kazakh character rendering — all 9 chars use Inter/Manrope
- [ ] Check 2: Governmental design style — clean, teal-blue, no gradients
- [ ] Check 3: Language switcher UX — text changes, URL updates, no full reload
- [ ] Check 4: Font weight rendering — Manrope headings, Inter body
- [ ] Check 5: Active nav state — current route highlighted
- [ ] Check 6: 404 for invalid locale — /fr returns 404
- [ ] Check 7: All 7 routes accessible in all 3 locales
- [ ] Check 8: Localized app title — correct in EN/RU/KK (W6)
- [ ] Results documented
</done>
</task>

## Success Criteria Coverage

| SC # | Criterion | Covered By Tasks | Verified By |
|------|-----------|-----------------|-------------|
| ALL | Full phase gate | 01-15 | Build + Vitest + Playwright |
| UI-02 | Visual Kazakh rendering | 01-16 | Manual check 1 |
| SC-4 | Design aesthetic | 01-16 | Manual check 2 |
| UI-01 | Switcher UX | 01-16 | Manual check 3 |
| W6 | Localized app title | 01-16 | Manual check 8 |

## Execution Notes

- **W8:** Full Playwright suite runs ONLY here. Previous plans used targeted test files.
- All 3 blockers (B1, B2, B3) and all 8 warnings (W1-W8) should be resolved by this point
- If tests fail, check the blocker fix list in task 01-15 action step 4
- This is the phase gate — all tests must be green before proceeding to /gsd-verify-work
