---
phase: 02-map-ui-digital-passport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/app/[locale]/layout.tsx
  - apps/web/app/[locale]/map/page.tsx
  - apps/web/lib/constants.ts
  - apps/web/lib/api/types.ts
  - apps/web/lib/api/mock-data.ts
  - apps/web/lib/api/client.ts
  - apps/web/lib/stores/map-store.ts
  - apps/web/lib/stores/filter-store.ts
  - apps/web/lib/stores/selection-store.ts
  - apps/web/components/providers/query-provider.tsx
  - apps/web/components/map/map-view.tsx
  - apps/web/components/map/passport-panel.tsx
  - apps/web/components/map/filter-panel.tsx
  - apps/web/components/ui/sheet.tsx
  - apps/web/components/ui/select.tsx
  - apps/web/components/ui/checkbox.tsx
  - apps/web/components/ui/badge.tsx
  - apps/web/components/ui/separator.tsx
  - apps/web/components/ui/card.tsx
  - apps/web/messages/en.json
  - apps/web/messages/ru.json
  - apps/web/messages/kk.json
  - apps/web/tests/mock-data.test.ts
  - apps/web/tests/stores.test.ts
  - apps/web/tests/types.test.ts
  - apps/web/tests/map.spec.ts
autonomous: true
requirements: [MAP-01, MAP-02]
user_setup: []

must_haves:
  truths:
    - "User sees an interactive map centered on Zhambyl Oblast at coordinates [72.6, 44.0] zoom 7"
    - "All mock structures appear as circle markers on the map"
    - "Structure markers are color-coded by condition: green=normal, yellow=inspection, orange=repair, red=critical, gray=missing"
    - "OSM raster tiles load as the base map with visible attribution"
    - "Map can be panned and zoomed interactively"
    - "Mock data layer produces valid GeoJSON FeatureCollection with typed StructureFeature objects"
    - "Zustand stores manage map viewport, filter criteria, and selection state"
    - "TanStack Query caches mock structure data with useStructuresGeoJSON and useStructureDetail hooks"
  artifacts:
    - path: "apps/web/lib/api/types.ts"
      provides: "TypeScript types for Structure domain (ConditionStatus, InspectionStatus, StructureType, TrilingualText, StructureProperties, StructureFeature, StructureCollection, StructureDetail, StructureFilters)"
      contains: "export type ConditionStatus"
    - path: "apps/web/lib/api/mock-data.ts"
      provides: "Mock structure fixtures and filter functions"
      exports: ["mockStructures", "mockStructureById", "mockDistricts", "mockBasins"]
    - path: "apps/web/lib/api/client.ts"
      provides: "TanStack Query hooks wrapping mock API"
      exports: ["useStructuresGeoJSON", "useStructureDetail"]
    - path: "apps/web/lib/stores/filter-store.ts"
      provides: "Zustand store for filter state"
      exports: ["useFilterStore"]
    - path: "apps/web/lib/stores/selection-store.ts"
      provides: "Zustand store for selected structure ID"
      exports: ["useSelectionStore"]
    - path: "apps/web/lib/stores/map-store.ts"
      provides: "Zustand store for map viewport state"
      exports: ["useMapStore"]
    - path: "apps/web/components/providers/query-provider.tsx"
      provides: "TanStack Query QueryClientProvider wrapper"
      exports: ["QueryProvider"]
    - path: "apps/web/components/map/map-view.tsx"
      provides: "react-map-gl MapLibre map with GeoJSON source and color-coded circle layer"
      exports: ["MapView"]
    - path: "apps/web/components/map/passport-panel.tsx"
      provides: "Stub component replaced by Plan 02"
      exports: ["PassportPanel"]
    - path: "apps/web/components/map/filter-panel.tsx"
      provides: "Stub component replaced by Plan 04"
      exports: ["FilterPanel"]
  key_links:
    - from: "apps/web/components/map/map-view.tsx"
      to: "apps/web/lib/api/client.ts"
      via: "useStructuresGeoJSON hook provides GeoJSON data to Source component"
      pattern: "useStructuresGeoJSON"
    - from: "apps/web/lib/api/client.ts"
      to: "apps/web/lib/api/mock-data.ts"
      via: "queryFn calls mockStructures(filters) — swap point for real API"
      pattern: "mockStructures"
    - from: "apps/web/components/map/map-view.tsx"
      to: "apps/web/lib/stores/map-store.ts"
      via: "viewport state from Zustand store drives map view"
      pattern: "useMapStore"
    - from: "apps/web/app/[locale]/layout.tsx"
      to: "apps/web/components/providers/query-provider.tsx"
      via: "QueryProvider wraps AppShell for client-side query caching"
      pattern: "QueryProvider"
---

## Phase Goal

**As a** water resource manager, **I want to** view all hydraulic structures on an interactive map with their digital passports and portfolio analytics, **so that** I can assess the condition and inspection status of infrastructure across Zhambyl Oblast.

---

<objective>
Build the interactive MapLibre map with color-coded structure markers, the mock data layer with TanStack Query, Zustand state stores, and all Phase 2 i18n keys and shadcn/ui components. This is the foundation vertical slice — after this plan, a user sees an interactive map with all structures color-coded by condition status.

Purpose: Establish the data layer, state management, and map rendering that Plans 02-04 build upon. Without this, no other Phase 2 feature can function.
Output: Working interactive map at /map showing color-coded structures, plus stub components for passport and filter panels that later plans replace.
</objective>

<execution_context>
@C:/Users/Ferumm/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Ferumm/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend/STATE.md
@.planning/workstreams/frontend/ROADMAP.md
@.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-RESEARCH.md
@.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-VALIDATION.md
@apps/web/package.json
@apps/web/app/[locale]/layout.tsx
@apps/web/app/[locale]/map/page.tsx
@apps/web/lib/constants.ts
@apps/web/i18n/routing.ts
@apps/web/messages/en.json
@apps/web/messages/ru.json
@apps/web/messages/kk.json
@apps/web/app/globals.css
@apps/web/components/layout/app-shell.tsx
@apps/web/components.json
@apps/web/tsconfig.json
@apps/web/vitest.config.ts
@apps/web/playwright.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install packages, shadcn components, QueryProvider, i18n keys, map constants</name>
  <files>
    apps/web/package.json,
    apps/web/app/[locale]/layout.tsx,
    apps/web/lib/constants.ts,
    apps/web/components/providers/query-provider.tsx,
    apps/web/components/ui/sheet.tsx,
    apps/web/components/ui/select.tsx,
    apps/web/components/ui/checkbox.tsx,
    apps/web/components/ui/badge.tsx,
    apps/web/components/ui/separator.tsx,
    apps/web/components/ui/card.tsx,
    apps/web/messages/en.json,
    apps/web/messages/ru.json,
    apps/web/messages/kk.json
  </files>
  <read_first>
    apps/web/package.json (current deps, scripts),
    apps/web/app/[locale]/layout.tsx (existing layout structure, provider wrapping order),
    apps/web/components.json (shadcn config: style=radix-nova, aliases),
    apps/web/messages/en.json (existing namespace structure: common, nav, home, dashboard, map, objects, copilot, reports, hydrofinder),
    apps/web/messages/ru.json (same structure as en.json),
    apps/web/messages/kk.json (same structure, includes Kazakh chars),
    apps/web/lib/constants.ts (existing navItems and locales exports),
    apps/web/app/globals.css (existing OKLCH theme tokens, 6 status colors in @theme block),
    apps/web/components/ui/button.tsx (existing shadcn component pattern: imports from radix-ui, uses cn() from @/lib/utils)
  </read_first>
  <behavior>
    - npm install of 5 packages completes without errors
    - shadcn add of 6 components creates files in components/ui/
    - QueryProvider wraps AppShell in layout, uses useState for single QueryClient instance
    - All 3 message files contain map, passport, dashboard, filter namespaces with trilingual keys
    - lib/constants.ts exports OSM_TILE_URL, ZHAMBYL_CENTER, STATUS_COLORS_HEX
  </behavior>
  <action>
Install 5 npm packages in apps/web: maplibre-gl@5.24.0, react-map-gl@8.1.1, @tanstack/react-query@5.101.1, zustand@5.0.14, recharts@3.9.0. Use npm install with exact version specifiers. Per A1, verify react-map-gl exposes the /maplibre subpath by checking node_modules/react-map-gl/package.json exports field after install — if the subpath is named differently, adjust all imports accordingly.

Install 6 shadcn/ui components by running: npx shadcn@latest add sheet select checkbox badge separator card — from the apps/web directory. This creates components/ui/sheet.tsx, select.tsx, checkbox.tsx, badge.tsx, separator.tsx, card.tsx and adds any radix-ui dependencies to package.json. The components.json style is radix-nova (per A5).

Create components/providers/query-provider.tsx as a 'use client' component. Export QueryProvider that creates a QueryClient via useState(() => makeQueryClient()) to ensure a single client per browser session (avoiding infinite refetch per Pitfall 5). Set defaultOptions.queries.staleTime to 60000 (1 minute). Import QueryClient and QueryClientProvider from @tanstack/react-query.

Modify app/[locale]/layout.tsx to import QueryProvider and wrap AppShell with it. The wrapping order must be: NextIntlClientProvider > QueryProvider > AppShell > children. Per A7, QueryProvider goes inside NextIntlClientProvider and outside AppShell. Keep the existing globals.css import as the first line.

Add map constants to lib/constants.ts: export OSM_TILE_URL as 'https://tile.openstreetmap.org/{z}/{x}/{y}.png' (per A2, HTTPS required per OSM tile policy). Export ZHAMBYL_CENTER as { longitude: 72.6, latitude: 44.0, zoom: 7 }. Export STATUS_COLORS_HEX as a Record mapping condition strings to hex color values: normal='#22c55e', inspection='#eab308', repair='#f97316', critical='#ef4444', missing='#9ca3af'. Per A6, these are hex values not CSS custom properties because MapLibre WebGL expressions cannot read var(). Keep existing navItems and locales exports unchanged.

Add ALL Phase 2 i18n keys to messages/en.json, ru.json, and kk.json. These are added upfront to avoid parallel file conflicts in Wave 2. Add these namespaces alongside existing ones:

map namespace (extend existing): add filterTitle, filterDistrict, filterBasin, filterType, filterCondition, filterInspectionStatus, filterReset, condition.normal, condition.inspection, condition.repair, condition.critical, condition.missing, inspectionStatus.current, inspectionStatus.overdue, inspectionStatus.due_soon, inspectionStatus.never, inspectionStatus.unknown, structureType.dam, structureType.reservoir, structureType.canal, structureType.pumping_station, structureType.spillway, structureType.other, legend.title, legend.normal, legend.inspection, legend.repair, legend.critical, legend.missing, loading, error

passport namespace (new): add title, identity, name, structureId, type, condition, geometry, coordinates, adminLocation, region, district, nearestSettlement, technicalSpecs, height, length, capacity, yearBuilt, designType, materials, status, inspectionStatus, provenance, source, confidence, lastVerified, confidenceHigh, confidenceMedium, confidenceLow, sourceKazvodhoz, sourceOsm, sourceSatellite, sourceManual, close

dashboard namespace (extend existing): add conditionDistribution, repairQueue, inspectionCoverage, geographicHeatmap, totalStructures, needsRepair, needsInspection, coverageRate, structuresByDistrict, structuresByType, noData

filter namespace (new): add title, district, basin, type, condition, inspectionStatus, all, reset, apply, active, results

All text must be properly translated in all 3 languages. Russian and Kazakh translations must use proper Cyrillic. Kazakh must include Kazakh-specific characters where applicable (ә, ғ, қ, ң, ө, ұ, ү, h, і).
  </action>
  <verify>
    <automated>cd apps/web; npm ls maplibre-gl react-map-gl @tanstack/react-query zustand recharts 2>&1 | Select-String "maplibre-gl@5" ; npm ls maplibre-gl react-map-gl @tanstack/react-query zustand recharts 2>&1 | Select-String "react-map-gl@8" ; Test-Path -LiteralPath "components/ui/sheet.tsx" ; Test-Path -LiteralPath "components/ui/select.tsx" ; Test-Path -LiteralPath "components/ui/checkbox.tsx" ; Test-Path -LiteralPath "components/ui/badge.tsx" ; Test-Path -LiteralPath "components/ui/separator.tsx" ; Test-Path -LiteralPath "components/ui/card.tsx" ; Test-Path -LiteralPath "components/providers/query-provider.tsx"</automated>
  </verify>
  <done>
    5 npm packages installed with correct versions. 6 shadcn/ui component files exist in components/ui/. QueryProvider component exists and is imported in layout.tsx wrapping AppShell. All 3 message files contain map, passport, dashboard, filter namespaces with keys in all 3 languages. lib/constants.ts exports OSM_TILE_URL, ZHAMBYL_CENTER, STATUS_COLORS_HEX.
  </done>
  <acceptance_criteria>
    - npm ls output shows maplibre-gl@5.24.0, react-map-gl@8.1.1, @tanstack/react-query@5.101.1, zustand@5.0.14, recharts@3.9.0
    - Test-Path returns True for all 6 shadcn component files (sheet, select, checkbox, badge, separator, card)
    - Test-Path returns True for components/providers/query-provider.tsx
    - query-provider.tsx contains 'use client' directive and QueryClientProvider import from @tanstack/react-query
    - layout.tsx contains QueryProvider wrapping AppShell (QueryProvider appears between NextIntlClientProvider and AppShell in JSX)
    - messages/en.json contains "passport" and "filter" top-level keys
    - messages/ru.json contains "passport" and "filter" top-level keys
    - messages/kk.json contains "passport" and "filter" top-level keys
    - lib/constants.ts exports OSM_TILE_URL, ZHAMBYL_CENTER, STATUS_COLORS_HEX
    - STATUS_COLORS_HEX maps normal to #22c55e, critical to #ef4444, missing to #9ca3af
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mock data layer, Zustand stores, stub components, unit tests</name>
  <files>
    apps/web/lib/api/types.ts,
    apps/web/lib/api/mock-data.ts,
    apps/web/lib/api/client.ts,
    apps/web/lib/stores/map-store.ts,
    apps/web/lib/stores/filter-store.ts,
    apps/web/lib/stores/selection-store.ts,
    apps/web/components/map/passport-panel.tsx,
    apps/web/components/map/filter-panel.tsx,
    apps/web/tests/mock-data.test.ts,
    apps/web/tests/stores.test.ts,
    apps/web/tests/types.test.ts
  </files>
  <read_first>
    apps/web/lib/api/types.ts (does not exist yet — creating),
    apps/web/lib/utils.ts (cn() utility pattern),
    apps/web/tsconfig.json (path alias @/* maps to ./*),
    apps/web/vitest.config.ts (environment=jsdom, include=['tests/**/*.test.ts'], resolve alias @ to __dirname),
    apps/web/tests/design-tokens.test.ts (existing test pattern for vitest),
    apps/web/i18n/routing.ts (locales array for reference)
  </read_first>
  <behavior>
    - mockStructures() returns valid GeoJSON FeatureCollection with type='FeatureCollection' and features array
    - mockStructures({condition:'critical'}) returns only structures with condition='critical'
    - mockStructures({district:'X'}) returns only structures in district X
    - mockStructures with all filters combined returns intersection of all criteria
    - mockStructureById('KZ-ZH-0001') returns a StructureDetail object, mockStructureById('nonexistent') returns null
    - mockStructures() includes at least 1 structure with each condition status (normal, inspection, repair, critical, missing)
    - mockStructures() includes structures with missing coordinates (coordinates null or [0,0]) that get gray color
    - useFilterStore.setFilter('district', 'Test') sets district filter; resetFilters() clears all filters
    - useSelectionStore.setSelectedId('KZ-ZH-0001') sets selectedId; setSelectedId(null) clears it
    - useMapStore.setViewport updates viewport state
    - TypeScript types compile without errors (types.test.ts validates type shapes at runtime)
  </behavior>
  <action>
Create lib/api/types.ts with TypeScript types matching the expected backend API contract (per A3, follows TiPG OGC API Features GeoJSON format). Export these types: ConditionStatus as union of 'normal'|'inspection'|'repair'|'critical'|'missing'. InspectionStatus as union of 'current'|'overdue'|'due_soon'|'never'|'unknown'. StructureType as union of 'dam'|'reservoir'|'canal'|'pumping_station'|'spillway'|'other'. TrilingualText as interface {ru, kk, en: string}. StructureProperties as interface with id, name (TrilingualText), type, condition, inspectionStatus, district, basin, optional height/length/capacity/yearBuilt, provenance object {source, confidence, lastVerified}. StructureFeature extending GeoJSON.Feature with geometry: GeoJSON.Point and properties: StructureProperties. StructureCollection extending GeoJSON.FeatureCollection with features: StructureFeature[]. StructureDetail extending StructureProperties with coordinates {lon, lat}, administrativeLocation {region, district, nearestSettlement}, technicalSpecs {height?, length?, capacity?, yearBuilt?, designType?, materials?}. StructureFilters with optional district, basin, type, condition, inspectionStatus (all string|null).

Create lib/api/mock-data.ts with 50+ mock structures in Zhambyl Oblast, Kazakhstan. Coordinates centered around [72.6, 44.0] (Taraz area). Each structure has a unique ID like 'KZ-ZH-0001' through 'KZ-ZH-0050+'. Include all 5 condition statuses (normal, inspection, repair, critical, missing) with at least 5 structures per status. Include all 6 structure types. Include structures across at least 4 districts (e.g., Жамбылский район, Меркенский район, Мойынкумский район, Таласский район) and at least 3 basins (e.g., р. Талас, р. Чу, р. Аса). Include at least 2 structures with missing coordinates (coordinates set to null) to test gray color rendering. Each structure has trilingual name (ru, kk, en), provenance with source and confidence. Export mockStructures(filters?: StructureFilters): StructureCollection that filters by each criterion and returns GeoJSON FeatureCollection. Export mockStructureById(id: string): StructureDetail|null. Export mockDistricts(): string[] returning unique district names. Export mockBasins(): string[] returning unique basin names. Per the architecture principle, each structure has one canonical record with provenance.

Create lib/api/client.ts with TanStack Query hooks. Import useQuery from @tanstack/react-query. Import mockStructures and mockStructureById from ./mock-data. Create fetchStructures(filters?) that awaits a 100ms delay then returns mockStructures(filters) — the delay simulates network latency for realistic testing. Create fetchStructureDetail(id) similarly. Export useStructuresGeoJSON(filters?) using useQuery with queryKey ['structures','geojson',filters] and queryFn calling fetchStructures(filters). Export useStructureDetail(id: string|null) using useQuery with queryKey ['structure',id], queryFn calling fetchStructureDetail(id!), and enabled: !!id. The query key includes filter values so refetch/recompute happens when filters change (per Pitfall 7).

Create lib/stores/map-store.ts using Zustand create(). Interface MapState with viewport {longitude, latitude, zoom} and setViewport function. Initial viewport uses ZHAMBYL_CENTER from constants. Export useMapStore.

Create lib/stores/filter-store.ts using Zustand create(). Interface FilterState with district, basin, type, condition, inspectionStatus (all string|null, initially null). setFilter(key, value) sets a single filter. resetFilters() clears all to null. Per Pitfall 7 anti-pattern: do NOT store derived/filtered GeoJSON in the store — only filter criteria. Export useFilterStore.

Create lib/stores/selection-store.ts using Zustand create(). Interface SelectionState with selectedId (string|null) and setSelectedId function. Export useSelectionStore.

Create components/map/passport-panel.tsx as a 'use client' STUB component. Export PassportPanel that returns null (or an empty fragment). This stub is replaced by Plan 02 with the real passport Sheet implementation. The stub exists so the map page can import it without errors.

Create components/map/filter-panel.tsx as a 'use client' STUB component. Export FilterPanel that returns null (or an empty fragment). This stub is replaced by Plan 04 with the real filter controls implementation. The stub exists so the map page can import it without errors.

Create tests/mock-data.test.ts (Vitest unit test). Test that mockStructures() returns object with type='FeatureCollection'. Test that features array is non-empty. Test that each feature has geometry.type='Point' and properties.id. Test filtering by each criterion (district, basin, type, condition, inspectionStatus) returns correct subset. Test that mockStructures with no matching filters returns empty features array. Test mockStructureById returns correct structure for valid ID and null for invalid ID. Test that all 5 condition statuses are represented in unfiltered data.

Create tests/stores.test.ts (Vitest unit test). Test useFilterStore: initial state has all filters null, setFilter updates the specified filter, resetFilters clears all. Test useSelectionStore: initial selectedId is null, setSelectedId updates it, setSelectedId(null) clears. Test useMapStore: initial viewport matches ZHAMBYL_CENTER, setViewport updates viewport.

Create tests/types.test.ts (Vitest unit test). Validate that mock data conforms to expected type shapes at runtime: StructureCollection has type='FeatureCollection', StructureFeature has type='Feature', properties has required fields (id, name with ru/kk/en, type, condition, district, basin). Validate condition values are within the ConditionStatus union. Validate provenance has source, confidence, lastVerified.
  </action>
  <verify>
    <automated>cd apps/web; npx vitest run tests/mock-data.test.ts tests/stores.test.ts tests/types.test.ts</automated>
  </verify>
  <done>
    lib/api/types.ts exports all 10 type definitions. lib/api/mock-data.ts exports mockStructures, mockStructureById, mockDistricts, mockBasins with 50+ structures covering all conditions and types. lib/api/client.ts exports useStructuresGeoJSON and useStructureDetail hooks. 3 Zustand stores created with correct initial state and actions. 2 stub components created returning null. 3 unit test files pass with all assertions green.
  </done>
  <acceptance_criteria>
    - npx vitest run tests/mock-data.test.ts tests/stores.test.ts tests/types.test.ts exits with 0 (all pass)
    - lib/api/types.ts contains 'export type ConditionStatus' with union including 'missing'
    - lib/api/mock-data.ts contains 'export function mockStructures' and 'export function mockStructureById'
    - mock-data.ts contains at least 50 structure entries (count of id fields >= 50)
    - mock-data.ts includes structures with condition='missing' and condition='critical'
    - lib/api/client.ts contains 'useStructuresGeoJSON' and 'useStructureDetail' exports
    - lib/stores/filter-store.ts contains 'export const useFilterStore' and 'resetFilters'
    - lib/stores/selection-store.ts contains 'export const useSelectionStore'
    - lib/stores/map-store.ts contains 'export const useMapStore'
    - components/map/passport-panel.tsx contains 'use client' and 'export function PassportPanel'
    - components/map/filter-panel.tsx contains 'use client' and 'export function FilterPanel'
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MapView component, map page replacement, E2E test</name>
  <files>
    apps/web/components/map/map-view.tsx,
    apps/web/app/[locale]/map/page.tsx,
    apps/web/tests/map.spec.ts
  </files>
  <read_first>
    apps/web/app/[locale]/map/page.tsx (current placeholder — being replaced),
    apps/web/components/layout/app-shell.tsx (layout structure: header + sidebar + main with p-8 padding),
    apps/web/app/globals.css (existing @theme tokens for status colors),
    apps/web/lib/constants.ts (OSM_TILE_URL, ZHAMBYL_CENTER, STATUS_COLORS_HEX from Task 1),
    apps/web/lib/api/client.ts (useStructuresGeoJSON hook from Task 2),
    apps/web/lib/stores/map-store.ts (useMapStore from Task 2),
    apps/web/lib/stores/selection-store.ts (useSelectionStore from Task 2),
    apps/web/components/map/passport-panel.tsx (stub from Task 2),
    apps/web/components/map/filter-panel.tsx (stub from Task 2),
    apps/web/tests/routes.spec.ts (existing Playwright test pattern: test() with page.goto and expect),
    apps/web/playwright.config.ts (baseURL=http://localhost:3000, testMatch=['**/*.spec.ts'], fullyParallel=false)
  </read_first>
  <behavior>
    - Map page at /ru/map renders an interactive map canvas (WebGL context)
    - Map canvas element exists in DOM (canvas tag inside .maplibregl-canvas container)
    - Map is centered on Zhambyl Oblast (longitude ~72.6, latitude ~44.0, zoom 7)
    - Structure markers are visible on the map (circle layer with features)
    - OSM tile attribution text is visible on the map
    - Map page renders in all 3 locales (ru, kk, en) with translated title/subtitle
    - Existing Phase 1 routes.spec.ts tests still pass (all 21 tests)
    - map.spec.ts E2E test passes for map rendering, structure visibility, and locale rendering
  </behavior>
  <action>
Create components/map/map-view.tsx as a 'use client' component. Import Map, Source, Layer, MapMouseEvent from 'react-map-gl/maplibre' (per A1, verify this subpath exists in node_modules/react-map-gl/package.json — if different, use the correct subpath). Import 'maplibre-gl/dist/maplibre-gl.css' at the top of the file (per Pitfall 2, without this CSS the map renders as blank canvas). Import useMapStore, useFilterStore, useSelectionStore from their respective store files. Import useStructuresGeoJSON from @/lib/api/client. Import OSM_TILE_URL, ZHAMBYL_CENTER, STATUS_COLORS_HEX from @/lib/constants.

The MapView component renders a react-map-gl Map component with initialViewState set to ZHAMBYL_CENTER. The mapStyle prop defines an OSM raster source using OSM_TILE_URL with tileSize 256 and attribution '© OpenStreetMap contributors' (per Pitfall 4, attribution is required by OSM tile usage policy). The raster layer references the osm source.

Inside the Map, render a Source with id='structures', type='geojson', and data bound to the result of useStructuresGeoJSON(useFilterStore()). Bind the filter store directly (not destructured properties) to ensure stable query key references. Inside the Source, render a Layer with type='circle' and paint properties: circle-radius=7, circle-color as a match expression on ['get','condition'] mapping to STATUS_COLORS_HEX values (normal→#22c55e, inspection→#eab308, repair→#f97316, critical→#ef4444, default→#9ca3af for missing), circle-stroke-width=1.5, circle-stroke-color='#ffffff'. Per A6, use hex values not CSS custom properties.

Add an onClick handler on the Map (or on the structures layer via the interactiveLayerIds prop) that extracts e.features[0].properties.id and calls useSelectionStore's setSelectedId with that ID. Use interactiveLayerIds={['structures']} on the Map component to restrict click events to the structures layer.

The onMove handler updates useMapStore's setViewport with e.viewState. The style prop on Map is set to width:'100%', height:'100%' so the map fills its container. Per Pitfall 1, this component MUST have 'use client' at the top — MapLibre requires WebGL/DOM and cannot SSR.

Replace app/[locale]/map/page.tsx to render the MapView alongside the stub components. Keep the existing getTranslations('map') and setRequestLocale(locale) calls. The page is a Server Component that renders client components as children. The JSX structure: a full-height container div (use h-[calc(100vh-3.5rem)] or similar to account for the AppShell header height of ~3.5rem, with the map filling remaining space). Inside, render a header section with the map title and subtitle (from existing translations), then a relative-positioned container with MapView, FilterPanel (stub), and PassportPanel (stub) as siblings. The MapView fills the container; FilterPanel and PassportPanel are positioned absolutely or as overlays.

Create tests/map.spec.ts (Playwright E2E test). Import test and expect from @playwright/test. Write tests: (1) '/ru/map renders map canvas' — navigate to /ru/map, wait for the .maplibregl-canvas element to be visible, assert it exists. (2) '/ru/map shows structure markers' — navigate to /ru/map, wait for map to load, assert that the structures source has features (can check via page.evaluate on the map instance or by checking for canvas content). (3) '/ru/map shows OSM attribution' — navigate to /ru/map, assert page contains text 'OpenStreetMap'. (4) '/ru/map renders in en locale' — navigate to /en/map, assert the page title text matches the English map.title translation. (5) '/ru/map renders in kk locale' — navigate to /kk/map, assert the page title text matches the Kazakh map.title translation. Use page.waitForSelector with a timeout for map canvas to account for WebGL initialization and tile loading.

After implementation, run the full test suite to verify no regressions: npx vitest run (all unit tests pass including Phase 1 tests) and npx playwright test (all E2E tests pass including routes.spec.ts).
  </action>
  <verify>
    <automated>cd apps/web; npx vitest run; npx playwright test tests/map.spec.ts tests/routes.spec.ts</automated>
  </verify>
  <done>
    MapView component renders interactive MapLibre map with OSM tiles and color-coded structure markers. Map page at /ru/map shows the map with title and subtitle. E2E test map.spec.ts passes all 5 tests. All existing Phase 1 tests (routes.spec.ts 21 tests, i18n.spec.ts 6 tests, fonts.spec.ts 3 tests, design-tokens.test.ts 6 tests) still pass. Build completes without errors.
  </done>
  <acceptance_criteria>
    - npx vitest run exits with 0 (all unit tests pass, including Phase 1 tests)
    - npx playwright test tests/map.spec.ts exits with 0 (all map E2E tests pass)
    - npx playwright test tests/routes.spec.ts exits with 0 (all 21 route tests still pass)
    - components/map/map-view.tsx contains 'use client' directive
    - components/map/map-view.tsx imports from 'react-map-gl/maplibre' (or verified alternative subpath)
    - components/map/map-view.tsx imports 'maplibre-gl/dist/maplibre-gl.css'
    - components/map/map-view.tsx contains 'match' expression for circle-color paint property
    - app/[locale]/map/page.tsx imports MapView, FilterPanel, PassportPanel
    - tests/map.spec.ts contains test for map canvas visibility and OSM attribution
    - npm run build completes without errors
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → MapLibre WebGL | Map rendering occurs in browser WebGL context; structure data flows from mock fixtures |
| Browser → OSM Tile Server | HTTP requests to tile.openstreetmap.org for raster base map tiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering (XSS) | map-view.tsx, passport-panel.tsx | medium | mitigate | Structure names rendered via React JSX interpolation (auto-escaped). Never use MapLibre setHTML() with user-controlled content. Mock data is controlled/static. |
| T-02-02 | Information Disclosure | OSM tile requests | low | accept | OSM tile server sees user IP and requested tile coordinates. Acceptable for dev/MVP. Production will proxy tiles through own server. HTTPS used per OSM policy. |
| T-02-03 | Denial of Service | OSM tile server dependency | low | mitigate | OSM tiles are best-effort with no SLA. Implement MapLibre error event handler for tile load failures. Map still shows structure markers if tiles fail (structures are from GeoJSON source, not tiles). |
| T-02-SC | Tampering (Supply Chain) | npm package installs | high | mitigate | Package Legitimacy Audit in RESEARCH.md: all 5 packages verified OK or SUS-false-positive (too-new flag on 2M-58M/wk downloads packages). No postinstall scripts. No SLOP packages. |
</threat_model>

<verification>
- npm run build passes without errors
- npx vitest run passes all unit tests (mock-data, stores, types + Phase 1 tests)
- npx playwright test passes all E2E tests (map.spec.ts + routes.spec.ts)
- Map renders at /ru/map, /kk/map, /en/map with structures visible and color-coded
- OSM attribution visible on map
</verification>

<success_criteria>
- Interactive MapLibre map renders at /map in all 3 locales
- All mock structures appear as color-coded circle markers (green/yellow/orange/red/gray by condition)
- Map centered on Zhambyl Oblast [72.6, 44.0] zoom 7
- OSM raster tiles load as base map with attribution
- Mock data layer produces valid GeoJSON with 50+ typed structures
- Zustand stores manage viewport, filter, and selection state
- TanStack Query hooks (useStructuresGeoJSON, useStructureDetail) cache mock data
- QueryProvider wraps app in root layout
- All Phase 1 tests still pass (no regressions)
</success_criteria>

<output>
Create `.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Types (lib/api/types.ts)
- `ConditionStatus` — union type for structure condition
- `InspectionStatus` — union type for inspection status
- `StructureType` — union type for structure type
- `TrilingualText` — interface for {ru, kk, en} text
- `StructureProperties` — interface for GeoJSON feature properties
- `StructureFeature` — extends GeoJSON.Feature with typed properties
- `StructureCollection` — extends GeoJSON.FeatureCollection with typed features
- `StructureDetail` — full structure detail with coordinates, admin location, specs
- `StructureFilters` — filter criteria interface

### Stores (lib/stores/)
- `useMapStore` — viewport state {longitude, latitude, zoom}
- `useFilterStore` — filter state {district, basin, type, condition, inspectionStatus}
- `useSelectionStore` — selected structure ID

### API Hooks (lib/api/client.ts)
- `useStructuresGeoJSON(filters?)` — TanStack Query hook returning StructureCollection
- `useStructureDetail(id)` — TanStack Query hook returning StructureDetail

### Mock Data (lib/api/mock-data.ts)
- `mockStructures(filters?)` — returns filtered StructureCollection
- `mockStructureById(id)` — returns StructureDetail or null
- `mockDistricts()` — returns unique district names
- `mockBasins()` — returns unique basin names

### Components
- `QueryProvider` (components/providers/) — TanStack Query provider
- `MapView` (components/map/) — react-map-gl MapLibre map with GeoJSON source
- `PassportPanel` (components/map/) — stub (replaced by Plan 02)
- `FilterPanel` (components/map/) — stub (replaced by Plan 04)

### shadcn/ui Components (components/ui/)
- `Sheet`, `Select`, `Checkbox`, `Badge`, `Separator`, `Card`

### Constants (lib/constants.ts)
- `OSM_TILE_URL`, `ZHAMBYL_CENTER`, `STATUS_COLORS_HEX`
