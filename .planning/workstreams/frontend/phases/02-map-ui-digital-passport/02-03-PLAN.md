---
phase: 02-map-ui-digital-passport
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/web/components/dashboard/dashboard-view.tsx
  - apps/web/components/dashboard/condition-donut.tsx
  - apps/web/components/dashboard/repair-queue.tsx
  - apps/web/components/dashboard/inspection-stats.tsx
  - apps/web/components/dashboard/heatmap-view.tsx
  - apps/web/app/[locale]/dashboard/page.tsx
  - apps/web/tests/dashboard.spec.ts
autonomous: true
requirements: [MAP-04]
user_setup: []

must_haves:
  truths:
    - "User navigates to /dashboard and sees portfolio analytics for all structures"
    - "Condition distribution donut chart shows count of structures by condition (normal, inspection, repair, critical, missing)"
    - "Repair queue shows structures needing repair (repair + critical conditions) in a list or bar chart"
    - "Inspection coverage stat cards show total structures, structures needing inspection, and coverage rate"
    - "Geographic distribution visualization shows structures grouped by district or region"
    - "Dashboard data reflects current filter state from useFilterStore"
    - "Dashboard renders with translated labels in all 3 locales"
  artifacts:
    - path: "apps/web/components/dashboard/dashboard-view.tsx"
      provides: "Dashboard orchestration component that renders all chart subcomponents"
      exports: ["DashboardView"]
    - path: "apps/web/components/dashboard/condition-donut.tsx"
      provides: "Recharts PieChart donut showing condition distribution"
      exports: ["ConditionDonut"]
    - path: "apps/web/components/dashboard/repair-queue.tsx"
      provides: "Repair queue showing structures needing repair"
      exports: ["RepairQueue"]
    - path: "apps/web/components/dashboard/inspection-stats.tsx"
      provides: "Stat cards for inspection coverage metrics"
      exports: ["InspectionStats"]
    - path: "apps/web/components/dashboard/heatmap-view.tsx"
      provides: "Geographic distribution visualization by district"
      exports: ["HeatmapView"]
  key_links:
    - from: "apps/web/components/dashboard/dashboard-view.tsx"
      to: "apps/web/lib/api/client.ts"
      via: "uses useStructuresGeoJSON(useFilterStore()) to get filtered structure data"
      pattern: "useStructuresGeoJSON"
    - from: "apps/web/components/dashboard/condition-donut.tsx"
      to: "apps/web/lib/constants.ts"
      via: "STATUS_COLORS_HEX for chart cell colors matching map symbology"
      pattern: "STATUS_COLORS_HEX"
    - from: "apps/web/app/[locale]/dashboard/page.tsx"
      to: "apps/web/components/dashboard/dashboard-view.tsx"
      via: "Server Component renders DashboardView client component"
      pattern: "DashboardView"
---

## Phase Goal

**As a** water resource manager, **I want to** view all hydraulic structures on an interactive map with their digital passports and portfolio analytics, **so that** I can assess the condition and inspection status of infrastructure across Zhambyl Oblast.

---

<objective>
Replace the placeholder /dashboard route from Phase 1 with a full portfolio dashboard. The dashboard shows condition distribution (donut chart), repair queue (structures needing repair), inspection coverage (stat cards), and geographic distribution (heatmap visualization). All data comes from the mock data layer via TanStack Query and respects the filter store state.

Purpose: MAP-04 (portfolio dashboard with condition distribution, repair queue, inspection coverage, geographic distribution) gives decision-makers a portfolio-level view of all hydraulic structures.
Output: Working dashboard at /dashboard with 4 visualization components backed by mock data.
</objective>

<execution_context>
@C:/Users/Ferumm/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Ferumm/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-RESEARCH.md
@.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-01-SUMMARY.md
@apps/web/app/[locale]/dashboard/page.tsx
@apps/web/lib/api/client.ts
@apps/web/lib/api/types.ts
@apps/web/lib/api/mock-data.ts
@apps/web/lib/stores/filter-store.ts
@apps/web/lib/constants.ts
@apps/web/components/ui/card.tsx
@apps/web/components/ui/badge.tsx
@apps/web/messages/en.json
@apps/web/messages/ru.json
@apps/web/messages/kk.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dashboard view with condition donut, repair queue, inspection stats, heatmap</name>
  <files>
    apps/web/components/dashboard/dashboard-view.tsx,
    apps/web/components/dashboard/condition-donut.tsx,
    apps/web/components/dashboard/repair-queue.tsx,
    apps/web/components/dashboard/inspection-stats.tsx,
    apps/web/components/dashboard/heatmap-view.tsx,
    apps/web/app/[locale]/dashboard/page.tsx
  </files>
  <read_first>
    apps/web/app/[locale]/dashboard/page.tsx (existing placeholder — being replaced),
    apps/web/lib/api/client.ts (from Plan 01 — useStructuresGeoJSON hook returns StructureCollection),
    apps/web/lib/api/types.ts (from Plan 01 — StructureCollection, StructureFeature, StructureProperties, ConditionStatus),
    apps/web/lib/stores/filter-store.ts (from Plan 01 — useFilterStore with district, basin, type, condition, inspectionStatus filters),
    apps/web/lib/constants.ts (from Plan 01 — STATUS_COLORS_HEX mapping condition to hex color),
    apps/web/components/ui/card.tsx (from Plan 01 — shadcn Card component: Card, CardHeader, CardTitle, CardContent),
    apps/web/components/ui/badge.tsx (from Plan 01 — shadcn Badge for status indicators),
    apps/web/components/layout/app-shell.tsx (layout structure: main has p-8 padding),
    apps/web/messages/en.json (dashboard namespace keys from Plan 01: conditionDistribution, repairQueue, inspectionCoverage, geographicHeatmap, totalStructures, needsRepair, needsInspection, coverageRate, structuresByDistrict, structuresByType, noData, plus condition.* and inspectionStatus.* keys in map namespace),
    apps/web/messages/ru.json (same dashboard keys in Russian),
    apps/web/messages/kk.json (same dashboard keys in Kazakh),
    apps/web/app/[locale]/map/page.tsx (from Plan 01 — reference for how Server Component renders client components)
  </read_first>
  <behavior>
    - Dashboard page at /ru/dashboard renders all 4 visualization sections
    - Condition donut shows segments for each condition status with correct colors
    - Repair queue lists structures with repair or critical condition
    - Inspection stats show total structures count, structures needing inspection, coverage percentage
    - Heatmap shows structure counts by district
    - All labels are translated using useTranslations('dashboard') and useTranslations('map')
    - Dashboard data respects filter state (if filters are set, data is filtered)
    - Empty state shown when no structures match current filters
  </behavior>
  <action>
Create components/dashboard/dashboard-view.tsx as a 'use client' component. This is the orchestration component that renders all 4 chart subcomponents. Import useTranslations from next-intl. Import useStructuresGeoJSON from @/lib/api/client. Import useFilterStore from @/lib/stores/filter-store. Import ConditionDonut, RepairQueue, InspectionStats, HeatmapView from their respective files. Call useStructuresGeoJSON(useFilterStore()) to get the filtered structure data. Pass the data (and loading state) to each subcomponent. Render a grid layout: use shadcn Card components to wrap each section. Layout: condition donut and inspection stats in top row, repair queue and heatmap in bottom row (or a responsive grid). Show a loading state when data is loading and an empty state (t('noData')) when data.features is empty.

Create components/dashboard/condition-donut.tsx as a 'use client' component. Import PieChart, Pie, Cell, ResponsiveContainer, Tooltip, Legend from recharts. Import useTranslations from next-intl. Import STATUS_COLORS_HEX from @/lib/constants. Accept props: features (StructureFeature[]) and isLoading (boolean). Compute chart data using useMemo: count features by condition property (f.properties.condition), map to array of {name, condition, value} where name is t('condition.' + condition). Render a ResponsiveContainer with width='100%' height={300} containing a PieChart with a Pie (dataKey='value', innerRadius=60 for donut effect, outerRadius=100, paddingAngle=2). Map Cell components with fill from STATUS_COLORS_HEX[entry.condition]. Include Tooltip and Legend. Show loading state when isLoading. Show empty state when features array is empty.

Create components/dashboard/repair-queue.tsx as a 'use client' component. Import useTranslations from next-intl. Import Badge from @/components/ui/badge. Import STATUS_COLORS_HEX from @/lib/constants. Import useLocale from next-intl for TrilingualText resolution. Accept props: features (StructureFeature[]) and isLoading (boolean). Filter features to those with condition 'repair' or 'critical' using useMemo. Render a list/table showing structure name (in current locale), type (translated), district, and condition badge (colored). Sort by severity (critical before repair). Show count of items. Show loading state and empty state (t('noData') or a message that no structures need repair).

Create components/dashboard/inspection-stats.tsx as a 'use client' component. Import useTranslations from next-intl. Import Card, CardHeader, CardTitle, CardContent from @/components/ui/card. Accept props: features (StructureFeature[]) and isLoading (boolean). Compute stats using useMemo: totalStructures = features.length, needsInspection = features filter where inspectionStatus is 'overdue' or 'due_soon' or 'never', coverageRate = percentage of structures with inspectionStatus 'current'. Render 3 stat cards: total structures (t('totalStructures')), needs inspection (t('needsInspection')), coverage rate (t('coverageRate') as percentage). Each card shows the numeric value prominently. Show loading state.

Create components/dashboard/heatmap-view.tsx as a 'use client' component. Import BarChart, Bar, XAxis, YAxis, ResponsiveContainer, Tooltip from recharts. Import useTranslations from next-intl. Accept props: features (StructureFeature[]) and isLoading (boolean). Compute data using useMemo: group features by district (f.properties.district), count per district, map to array of {district, count}. Sort by count descending. Render a ResponsiveContainer with a BarChart showing district names on XAxis and counts on YAxis. Include Tooltip. Show loading and empty states. This represents the geographic distribution — a bar chart by district is the MVP approach for the heatmap (a true MapLibre heatmap layer would require a second map instance, which adds complexity; the bar chart communicates the same information: which districts have the most structures).

Replace app/[locale]/dashboard/page.tsx to render the DashboardView. Keep the existing getTranslations('dashboard') and setRequestLocale(locale) calls. The page is a Server Component that renders the DashboardView client component as a child. Structure: a header section with title (t('title')) and subtitle (t('subtitle')), then the DashboardView component. Remove the p-8 constraint by using a container with padding (the main element already has p-8 from AppShell).
  </action>
  <verify>
    <automated>cd apps/web; npm run build</automated>
  </verify>
  <done>
    DashboardView orchestrates 4 chart components. ConditionDonut renders Recharts donut with color-coded segments. RepairQueue lists structures needing repair. InspectionStats shows 3 stat cards. HeatmapView shows bar chart by district. Dashboard page renders DashboardView. Build passes.
  </done>
  <acceptance_criteria>
    - components/dashboard/dashboard-view.tsx contains 'use client' and imports useStructuresGeoJSON
    - components/dashboard/condition-donut.tsx contains 'use client' and imports from 'recharts' (PieChart, Pie, Cell)
    - components/dashboard/condition-donut.tsx uses STATUS_COLORS_HEX for Cell fill colors
    - components/dashboard/repair-queue.tsx contains 'use client' and filters for 'repair' and 'critical' conditions
    - components/dashboard/inspection-stats.tsx contains 'use client' and computes totalStructures, needsInspection, coverageRate
    - components/dashboard/heatmap-view.tsx contains 'use client' and imports from 'recharts' (BarChart, Bar)
    - app/[locale]/dashboard/page.tsx imports DashboardView from @/components/dashboard/dashboard-view
    - app/[locale]/dashboard/page.tsx no longer contains only the placeholder title/subtitle (it renders DashboardView)
    - npm run build exits with 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E test for dashboard rendering and data display</name>
  <files>
    apps/web/tests/dashboard.spec.ts
  </files>
  <read_first>
    apps/web/tests/map.spec.ts (from Plan 01 — existing E2E test pattern),
    apps/web/tests/routes.spec.ts (existing Playwright test pattern),
    apps/web/playwright.config.ts (baseURL, testMatch, webServer config),
    apps/web/messages/en.json (dashboard namespace keys for English assertions),
    apps/web/messages/ru.json (dashboard namespace keys for Russian assertions),
    apps/web/components/dashboard/dashboard-view.tsx (from Task 1 — component being tested)
  </read_first>
  <behavior>
    - /ru/dashboard renders the dashboard page with title and subtitle
    - Dashboard contains the condition distribution chart (Recharts SVG rendered)
    - Dashboard contains the repair queue section
    - Dashboard contains the inspection coverage stats
    - Dashboard contains the geographic distribution chart
    - Dashboard renders in English locale with English labels
    - Dashboard shows data (not empty state) when mock data is available
  </behavior>
  <action>
Create tests/dashboard.spec.ts (Playwright E2E test). Import test and expect from @playwright/test. Write tests:

(1) '/ru/dashboard renders dashboard title' — navigate to /ru/dashboard, assert the page heading contains the Russian dashboard.title translation.

(2) '/ru/dashboard shows condition distribution chart' — navigate to /ru/dashboard, wait for Recharts to render (look for SVG elements with class containing 'recharts' or the ResponsiveContainer). Assert that the condition distribution section heading (t('conditionDistribution') in Russian) is visible. Assert that an SVG chart element exists.

(3) '/ru/dashboard shows repair queue' — navigate to /ru/dashboard, assert the repair queue section heading is visible. Assert that at least one structure name is visible in the repair queue (mock data has structures with repair/critical condition).

(4) '/ru/dashboard shows inspection stats' — navigate to /ru/dashboard, assert the inspection coverage heading is visible. Assert that numeric values (total structures count) are visible.

(5) '/ru/dashboard shows geographic distribution' — navigate to /ru/dashboard, assert the geographic heatmap heading is visible. Assert that a Recharts bar chart SVG element exists.

(6) '/en/dashboard renders in English' — navigate to /en/dashboard, assert the page heading contains the English dashboard.title translation. Assert section headings are in English.

For waiting for Recharts rendering: Recharts renders SVG elements. Use page.waitForSelector('svg.recharts-surface', { state: 'visible' }) or a more general selector for the chart container. Add a timeout to account for data loading and chart animation.

After writing tests, run: npx playwright test tests/dashboard.spec.ts to verify all tests pass. Also run npx playwright test tests/routes.spec.ts to verify no regressions.
  </action>
  <verify>
    <automated>cd apps/web; npx playwright test tests/dashboard.spec.ts tests/routes.spec.ts</automated>
  </verify>
  <done>
    dashboard.spec.ts E2E test passes all tests. Dashboard renders with condition distribution chart, repair queue, inspection stats, and geographic distribution. Dashboard renders in both ru and en locales. All existing route tests still pass.
  </done>
  <acceptance_criteria>
    - npx playwright test tests/dashboard.spec.ts exits with 0 (all dashboard tests pass)
    - npx playwright test tests/routes.spec.ts exits with 0 (route tests still pass)
    - tests/dashboard.spec.ts contains test asserting condition distribution chart SVG exists
    - tests/dashboard.spec.ts contains test asserting repair queue section is visible
    - tests/dashboard.spec.ts contains test asserting inspection stats are visible
    - tests/dashboard.spec.ts contains test asserting geographic distribution chart exists
    - tests/dashboard.spec.ts contains test for English locale rendering
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mock data → TanStack Query → Dashboard charts | Structure data flows from mock fixtures through query hooks to Recharts visualization |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering (XSS) | repair-queue.tsx, heatmap-view.tsx | low | mitigate | Structure names and district names rendered via React JSX (auto-escaped). Recharts renders data as SVG, not innerHTML. No user-controlled content in chart labels. |
| T-02-06 | Information Disclosure | dashboard charts | low | accept | Dashboard shows mock data only — no sensitive information. All data is public registry information. |
</threat_model>

<verification>
- npm run build passes
- npx playwright test tests/dashboard.spec.ts passes
- npx playwright test tests/routes.spec.ts still passes (no regressions)
- Dashboard renders at /ru/dashboard and /en/dashboard with all 4 visualization sections
</verification>

<success_criteria>
- /dashboard shows condition distribution donut chart with color-coded segments
- /dashboard shows repair queue listing structures needing repair
- /dashboard shows inspection coverage stat cards (total, needs inspection, coverage rate)
- /dashboard shows geographic distribution by district
- Dashboard data respects filter state from Zustand store
- Dashboard renders with translated labels in all 3 locales
- All Phase 1 tests still pass
</success_criteria>

<output>
Create `.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Components
- `DashboardView` (components/dashboard/dashboard-view.tsx) — orchestrates all dashboard subcomponents, fetches data via useStructuresGeoJSON with filter store
- `ConditionDonut` (components/dashboard/condition-donut.tsx) — Recharts PieChart donut showing condition distribution with STATUS_COLORS_HEX
- `RepairQueue` (components/dashboard/repair-queue.tsx) — lists structures with repair/critical condition
- `InspectionStats` (components/dashboard/inspection-stats.tsx) — stat cards for total, needs inspection, coverage rate
- `HeatmapView` (components/dashboard/heatmap-view.tsx) — Recharts BarChart showing structure counts by district
