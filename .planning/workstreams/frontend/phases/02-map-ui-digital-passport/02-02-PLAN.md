---
phase: 02-map-ui-digital-passport
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/web/components/map/passport-panel.tsx
  - apps/web/tests/passport.spec.ts
autonomous: true
requirements: [MAP-03, DATA-04]
user_setup: []

must_haves:
  truths:
    - "User clicks a structure marker on the map and a digital passport side panel opens"
    - "Passport displays structure identity (name in current locale, structure ID, type)"
    - "Passport displays geometry (coordinates: longitude, latitude)"
    - "Passport displays administrative location (region, district, nearest settlement)"
    - "Passport displays technical specifications (height, length, capacity, year built, design type, materials)"
    - "Passport displays current status with color-coded condition badge"
    - "Passport displays source provenance (source, confidence level, last verified date)"
    - "Passport closes when user clicks outside or presses the close button"
    - "Passport renders with translated labels in all 3 locales (ru, kk, en)"
  artifacts:
    - path: "apps/web/components/map/passport-panel.tsx"
      provides: "Digital passport side panel using shadcn Sheet with 6 structured sections"
      exports: ["PassportPanel"]
      contains: "SheetContent"
  key_links:
    - from: "apps/web/components/map/map-view.tsx"
      to: "apps/web/lib/stores/selection-store.ts"
      via: "map click handler sets selectedId in selection store"
      pattern: "setSelectedId"
    - from: "apps/web/components/map/passport-panel.tsx"
      to: "apps/web/lib/stores/selection-store.ts"
      via: "PassportPanel reads selectedId to control Sheet open state"
      pattern: "useSelectionStore"
    - from: "apps/web/components/map/passport-panel.tsx"
      to: "apps/web/lib/api/client.ts"
      via: "useStructureDetail(selectedId) fetches full structure detail from mock data"
      pattern: "useStructureDetail"
---

## Phase Goal

**As a** water resource manager, **I want to** view all hydraulic structures on an interactive map with their digital passports and portfolio analytics, **so that** I can assess the condition and inspection status of infrastructure across Zhambyl Oblast.

---

<objective>
Replace the passport panel stub from Plan 01 with a full digital passport side panel. When a user clicks a structure on the map, a shadcn/ui Sheet opens on the right side showing the complete digital passport: identity, geometry, administrative location, technical specifications, current status, and source provenance — all with trilingual labels.

Purpose: MAP-03 (click structure to open passport) and DATA-04 (passport shows identity, type, geometry, admin location, technical specs, status, provenance) are the core information-access capabilities of the MVP.
Output: Working digital passport panel that opens on map click and displays all required structure information.
</objective>

<execution_context>
@C:/Users/Ferumm/.config/opencode/gsd-core/workflows/execute-plan.md
@C:/Users/Ferumm/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-RESEARCH.md
@.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-01-SUMMARY.md
@apps/web/components/map/map-view.tsx
@apps/web/components/map/passport-panel.tsx
@apps/web/lib/stores/selection-store.ts
@apps/web/lib/api/client.ts
@apps/web/lib/api/types.ts
@apps/web/components/ui/sheet.tsx
@apps/web/components/ui/badge.tsx
@apps/web/components/ui/separator.tsx
@apps/web/messages/en.json
@apps/web/messages/ru.json
@apps/web/messages/kk.json
@apps/web/i18n/routing.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement PassportPanel with all DATA-04 sections</name>
  <files>
    apps/web/components/map/passport-panel.tsx
  </files>
  <read_first>
    apps/web/components/map/passport-panel.tsx (existing stub from Plan 01 — being replaced),
    apps/web/components/map/map-view.tsx (from Plan 01 — has click handler that calls setSelectedId),
    apps/web/lib/stores/selection-store.ts (useSelectionStore with selectedId and setSelectedId),
    apps/web/lib/api/client.ts (useStructureDetail hook — takes id, returns StructureDetail, enabled when id is truthy),
    apps/web/lib/api/types.ts (StructureDetail interface: id, name TrilingualText, type, condition, inspectionStatus, district, basin, coordinates {lon,lat}, administrativeLocation {region,district,nearestSettlement}, technicalSpecs {height?,length?,capacity?,yearBuilt?,designType?,materials?}, provenance {source,confidence,lastVerified}),
    apps/web/components/ui/sheet.tsx (shadcn Sheet component — exports Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetOverlay),
    apps/web/components/ui/badge.tsx (shadcn Badge component — for condition and confidence badges),
    apps/web/components/ui/separator.tsx (shadcn Separator — for visual section dividers),
    apps/web/messages/en.json (passport namespace keys added by Plan 01: title, identity, name, structureId, type, condition, geometry, coordinates, adminLocation, region, district, nearestSettlement, technicalSpecs, height, length, capacity, yearBuilt, designType, materials, status, inspectionStatus, provenance, source, confidence, lastVerified, confidenceHigh, confidenceMedium, confidenceLow, sourceKazvodhoz, sourceOsm, sourceSatellite, sourceManual, close; ALSO map namespace enum keys from Plan 01: condition.normal, condition.inspection, condition.repair, condition.critical, condition.missing, inspectionStatus.current, inspectionStatus.overdue, inspectionStatus.due_soon, inspectionStatus.never, inspectionStatus.unknown, structureType.dam, structureType.reservoir, structureType.canal, structureType.pumping_station, structureType.spillway, structureType.other, loading, error — use useTranslations('map') for these),
    apps/web/components/ui/button.tsx (shadcn Button pattern for reference),
    apps/web/lib/utils.ts (cn() utility for className merging),
    apps/web/i18n/routing.ts (locales array — needed to resolve TrilingualText in current locale)
  </read_first>
  <behavior>
    - When selectedId is null, Sheet is closed (open=false)
    - When selectedId is set (from map click), Sheet opens (open=true) on the right side
    - Sheet title shows the structure name in the current locale (from TrilingualText)
    - Passport shows 6 sections: identity, geometry, administrative location, technical specifications, status, provenance
    - Identity section shows: name (locale-specific), structure ID, type (translated)
    - Geometry section shows: coordinates (longitude, latitude in decimal degrees)
    - Administrative location section shows: region, district, nearest settlement
    - Technical specifications section shows: height (m), length (m), capacity (Mm³), year built, design type, materials — each only if the value exists (optional fields)
    - Status section shows: condition badge with correct color (green/yellow/orange/red/gray), inspection status (translated)
    - Provenance section shows: source (translated), confidence badge (HIGH/MEDIUM/LOW), last verified date
    - Closing the Sheet (clicking overlay, pressing Escape, or close button) calls setSelectedId(null)
    - While data is loading (useStructureDetail isLoading), shows a loading state
    - All section headings and field labels are translated using useTranslations('passport') from next-intl
    - Enum values (condition, inspectionStatus, structureType) are translated using useTranslations('map') from next-intl — the map namespace owns the nested enum keys (map.condition.normal, map.inspectionStatus.current, map.structureType.dam) per the i18n layout established in Plan 01 Task 1
  </behavior>
  <action>
Replace the stub in components/map/passport-panel.tsx with the full implementation. Keep the 'use client' directive. Import Sheet, SheetContent, SheetHeader, SheetTitle from @/components/ui/sheet. Import Badge from @/components/ui/badge. Import Separator from @/components/ui/separator. Import useTranslations from next-intl. Import useLocale from next-intl to get the current locale for resolving TrilingualText fields. Import useSelectionStore from @/lib/stores/selection-store. Import useStructureDetail from @/lib/api/client. Import STATUS_COLORS_HEX from @/lib/constants for condition badge styling.

The PassportPanel component: destructure selectedId and setSelectedId from useSelectionStore. Call useStructureDetail(selectedId) to get the structure detail and isLoading state. Get the current locale via useLocale() from next-intl — use this to pick the correct language from TrilingualText fields (e.g., structure.name[locale] or structure.name.ru as fallback). Get the passport translator via useTranslations('passport') as t for section headings and field labels. Get the map enum translator via useTranslations('map') as tMap for enum value labels — the map namespace owns the nested enum keys (map.condition.normal, map.inspectionStatus.current, map.structureType.dam) per the i18n layout established in Plan 01 Task 1. The passport namespace has flat string keys (passport.condition = "Condition" heading), NOT nested objects, so enum values MUST use tMap not t. This two-translator pattern matches Plan 04's FilterPanel which uses useTranslations('filter') for labels and useTranslations('map') for enum option labels.

Sheet open state: bind open to !!selectedId (truthy when a structure is selected). onOpenChange callback: when open becomes false, call setSelectedId(null) to clear selection and close the panel. SheetContent side='right' with a width class like w-[400px] sm:w-[540px] and overflow-y-auto for scrolling.

SheetHeader contains SheetTitle with the structure name in the current locale. Below the header, render the 6 sections separated by Separator components:

1. Identity section: heading from t('identity'). Show name (structure.name[locale] || structure.name.ru), structure ID (structure.id), type label from t('type') with the translated enum value from tMap('structureType.' + structure.type).

2. Geometry section: heading from t('geometry'). Show coordinates as t('coordinates') with longitude and latitude from structure.coordinates (lon, lat) formatted to 4 decimal places.

3. Administrative location section: heading from t('adminLocation'). Show region (structure.administrativeLocation.region), district (structure.administrativeLocation.district), nearest settlement (structure.administrativeLocation.nearestSettlement). Use definition list (dl/dt/dd) or label-value pairs.

4. Technical specifications section: heading from t('technicalSpecs'). Show each spec only if it exists: height (structure.technicalSpecs.height + ' m'), length (structure.technicalSpecs.length + ' m'), capacity (structure.technicalSpecs.capacity + ' Mm³'), year built (structure.technicalSpecs.yearBuilt), design type (structure.technicalSpecs.designType), materials (structure.technicalSpecs.materials). Use conditional rendering to skip undefined/null fields.

5. Status section: heading from t('status'). Show condition as a Badge with background color from STATUS_COLORS_HEX[structure.condition] and text from tMap('condition.' + structure.condition). Show inspection status as translated text from tMap('inspectionStatus.' + structure.inspectionStatus).

6. Provenance section: heading from t('provenance'). Show source as translated text (t('source' + capitalized source) or a lookup), confidence as a Badge (t('confidenceHigh')/'confidenceMedium'/'confidenceLow' based on structure.provenance.confidence), last verified date (structure.provenance.lastVerified formatted as date).

Handle loading state: when isLoading is true, render a loading indicator (e.g., a spinner or tMap('loading') text — the loading key is in the map namespace, not passport). Handle the case where structure is null/undefined (should not happen when selectedId is set, but defensive coding).
  </action>
  <verify>
    <automated>cd apps/web; npm run build</automated>
  </verify>
  <done>
    PassportPanel component replaces the stub with full implementation. Sheet opens on structure click, shows all 6 sections (identity, geometry, admin location, technical specs, status, provenance) with trilingual labels. Sheet closes on overlay click, Escape key, or close button. Loading state shown while data fetches. Build passes without errors.
  </done>
  <acceptance_criteria>
    - components/map/passport-panel.tsx contains 'use client' directive
    - passport-panel.tsx imports Sheet, SheetContent from @/components/ui/sheet
    - passport-panel.tsx imports useSelectionStore from @/lib/stores/selection-store
    - passport-panel.tsx imports useStructureDetail from @/lib/api/client
    - passport-panel.tsx imports useTranslations from next-intl
    - passport-panel.tsx calls useTranslations('passport') for section headings AND useTranslations('map') for enum value labels (condition.*, inspectionStatus.*, structureType.*)
    - passport-panel.tsx contains Sheet with open={!!selectedId} or equivalent
    - passport-panel.tsx contains onOpenChange that calls setSelectedId(null) when closed
    - passport-panel.tsx renders at least 6 sections (identity, geometry, adminLocation, technicalSpecs, status, provenance)
    - passport-panel.tsx uses Badge component for condition display
    - passport-panel.tsx resolves TrilingualText using current locale (useLocale or locale param)
    - npm run build exits with 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: E2E test for click structure → passport opens</name>
  <files>
    apps/web/tests/passport.spec.ts
  </files>
  <read_first>
    apps/web/tests/map.spec.ts (from Plan 01 — existing map E2E test pattern),
    apps/web/tests/routes.spec.ts (existing Playwright test pattern: test() with page.goto and expect),
    apps/web/playwright.config.ts (baseURL=http://localhost:3000, testMatch=['**/*.spec.ts'], fullyParallel=false, webServer reuseExistingServer),
    apps/web/messages/en.json (passport namespace keys for English assertions),
    apps/web/messages/ru.json (passport namespace keys for Russian assertions),
    apps/web/components/map/passport-panel.tsx (from Task 1 — the component being tested)
  </read_first>
  <behavior>
    - Navigate to /ru/map, click a structure marker, passport Sheet opens on the right
    - Passport Sheet contains the identity section heading
    - Passport Sheet contains the provenance section heading
    - Passport Sheet contains a close button or overlay that closes the panel
    - After closing, the Sheet is no longer visible
    - Test works in at least 2 locales (ru and en)
  </behavior>
  <action>
Create tests/passport.spec.ts (Playwright E2E test). Import test and expect from @playwright/test. Write tests:

(1) 'Click structure opens passport panel' — navigate to /ru/map, wait for .maplibregl-canvas to be visible (map loaded), wait for structure markers to render (wait for the canvas or a short timeout for GeoJSON source to load), click on a structure marker position. Since clicking exact canvas coordinates is fragile, use page.mouse.click at the map center or use page.evaluate to programmatically trigger the map click via the map instance. Alternatively, locate a structure marker by its rendered position. After click, assert that the Sheet content is visible (look for [data-state='open'] on the Sheet or the SheetContent role). Assert that the passport title or identity heading text is visible.

(2) 'Passport shows provenance section' — after opening the passport (same setup as test 1), assert that the provenance section heading text is visible in the Sheet content. Use the translated text from the passport namespace.

(3) 'Passport closes on overlay click' — after opening the passport, click outside the Sheet (on the overlay or map area), assert the Sheet is no longer visible (look for [data-state='closed'] or absence of Sheet content).

(4) 'Passport works in English locale' — navigate to /en/map, perform the same click-to-open flow, assert that English translated headings are visible in the passport.

For clicking structure markers: MapLibre renders markers on a WebGL canvas, which Playwright cannot directly query by selector. Use one of these approaches: (a) page.mouse.click at approximate marker positions (center of map where structures are clustered), (b) page.evaluate to access the map instance and fire a click event at a coordinate with a known structure, (c) use the map's queryRenderedFeatures API. Approach (a) is simplest for E2E — click at the map center area where structures are likely rendered. If the click misses, add a retry or click at multiple positions.

Add a beforeEach or test-level setup that waits for the map to fully load before attempting clicks. Use page.waitForSelector('.maplibregl-canvas', { state: 'visible' }) followed by a short page.waitForTimeout or page.waitForFunction to ensure the GeoJSON source has loaded.

After writing tests, run: npx playwright test tests/passport.spec.ts to verify all tests pass. Also run npx playwright test tests/routes.spec.ts tests/map.spec.ts to verify no regressions.
  </action>
  <verify>
    <automated>cd apps/web; npx playwright test tests/passport.spec.ts tests/map.spec.ts tests/routes.spec.ts</automated>
  </verify>
  <done>
    passport.spec.ts E2E test passes all tests. Clicking a structure opens the passport Sheet with identity and provenance sections visible. Closing the Sheet works. Test passes in both ru and en locales. All existing E2E tests (map.spec.ts, routes.spec.ts) still pass.
  </done>
  <acceptance_criteria>
    - npx playwright test tests/passport.spec.ts exits with 0 (all passport tests pass)
    - npx playwright test tests/map.spec.ts exits with 0 (map tests still pass)
    - npx playwright test tests/routes.spec.ts exits with 0 (route tests still pass)
    - tests/passport.spec.ts contains test that clicks a structure and asserts Sheet opens
    - tests/passport.spec.ts contains test that asserts provenance section is visible
    - tests/passport.spec.ts contains test that closes the Sheet and asserts it disappears
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Map click → Selection store → Passport | Structure ID from MapLibre click event flows to Zustand store to TanStack Query to passport render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering (XSS) | passport-panel.tsx | medium | mitigate | Structure names and all data rendered via React JSX interpolation (auto-escaped). No dangerouslySetInnerHTML. No MapLibre setHTML() with data content. |
| T-02-05 | Tampering (ID injection) | selection-store, client.ts | low | mitigate | Structure ID from click is used as TanStack Query key and passed to mockStructureById. Mock data lookup is safe (array find). Real API will validate ID format. |
</threat_model>

<verification>
- npm run build passes
- npx playwright test tests/passport.spec.ts passes
- npx playwright test tests/map.spec.ts tests/routes.spec.ts still pass (no regressions)
- Clicking a structure on /ru/map opens passport with all 6 sections
- Passport closes correctly
</verification>

<success_criteria>
- Click any structure on the map → digital passport Sheet opens on right side
- Passport shows: identity (name, ID, type), geometry (coordinates), administrative location (region, district, nearest settlement), technical specifications (height, length, capacity, year, design, materials), current status (condition badge, inspection status), source provenance (source, confidence, last verified)
- Passport labels are translated in all 3 locales
- Passport closes via overlay click, Escape key, or close button
- Loading state shows while structure detail fetches
</success_criteria>

<output>
Create `.planning/workstreams/frontend/phases/02-map-ui-digital-passport/02-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### Components
- `PassportPanel` (components/map/passport-panel.tsx) — full implementation replacing Plan 01 stub. Renders shadcn Sheet with 6 sections: identity, geometry, administrative location, technical specifications, status, provenance. Reads selectedId from useSelectionStore, fetches detail via useStructureDetail, uses useTranslations('passport') and useLocale for trilingual rendering.
