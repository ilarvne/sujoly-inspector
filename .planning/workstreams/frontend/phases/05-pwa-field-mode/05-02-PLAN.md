---
phase: 05-pwa-field-mode
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - apps/web/package.json
  - apps/web/lib/db/types.ts
  - apps/web/lib/db/field-db.ts
  - apps/web/lib/stores/connectivity-store.ts
  - apps/web/lib/stores/field-mode-store.ts
  - apps/web/lib/sync/sync-engine.ts
  - apps/web/lib/sync/conflict-resolution.ts
  - apps/web/lib/sync/voice-transcription.ts
autonomous: true
requirements: [FIELD-02, FIELD-03, FIELD-04]
user_setup: []

must_haves:
  truths:
    - "Dexie packages installed: dexie, dexie-react-hooks"
    - "Dexie database at lib/db/field-db.ts with 4 tables: fieldInspections, fieldPhotos, fieldVoiceNotes, syncQueue"
    - "Field capture types at lib/db/types.ts"
    - "Connectivity store (Zustand) tracking online/offline + pending sync count"
    - "Field mode store (Zustand, persist) tracking field mode enabled + lastSyncAt"
    - "Sync engine at lib/sync/sync-engine.ts processing sync queue on online event"
    - "Conflict resolution at lib/sync/conflict-resolution.ts with field-level merge"
    - "Voice transcription service at lib/sync/voice-transcription.ts (mock KK/RU)"
    - "Build succeeds"
  artifacts:
    - path: "apps/web/lib/db/field-db.ts"
      provides: "Dexie database with typed tables for offline field data"
      contains: "EntityTable"
    - path: "apps/web/lib/sync/sync-engine.ts"
      provides: "Sync queue processor with field-level merge and voice transcription"
      contains: "processSyncQueue"
---

## Phase Goal

Offline data persistence and sync engine.

<objective>
Install Dexie.js, create IndexedDB schema with 4 tables, create connectivity and field mode Zustand stores, implement sync engine with field-level merge conflict resolution, and mock voice transcription service.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Dexie + create DB schema + types + stores + sync engine</name>
  <files>
    apps/web/package.json,
    apps/web/lib/db/types.ts,
    apps/web/lib/db/field-db.ts,
    apps/web/lib/stores/connectivity-store.ts,
    apps/web/lib/stores/field-mode-store.ts,
    apps/web/lib/sync/sync-engine.ts,
    apps/web/lib/sync/conflict-resolution.ts,
    apps/web/lib/sync/voice-transcription.ts
  </files>
  <behavior>
    - dexie and dexie-react-hooks installed
    - FieldDB with 4 tables and proper indexes
    - SyncStatus enum: pending, syncing, confirmed, failed, conflict
    - Connectivity store tracks isOnline and pendingSyncCount
    - Field mode store persists fieldModeEnabled and lastSyncAt
    - Sync engine processes queue, updates status, triggers transcription
    - Conflict resolution compares field-by-field, not last-write-wins
    - Voice transcription mock returns placeholder text with language
    - Build passes
  </behavior>
  <action>
Install Dexie:
```bash
cd apps/web && npm install dexie dexie-react-hooks
```
Create all lib/db/, lib/stores/, lib/sync/ files.
  </action>
</task>

</tasks>
