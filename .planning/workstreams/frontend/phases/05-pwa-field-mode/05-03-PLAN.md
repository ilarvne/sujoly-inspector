---
phase: 05-pwa-field-mode
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - apps/web/app/[locale]/field/page.tsx
  - apps/web/components/field/field-inspection-form.tsx
  - apps/web/components/field/photo-capture.tsx
  - apps/web/components/field/voice-note-recorder.tsx
  - apps/web/components/field/gps-correction.tsx
  - apps/web/components/field/field-mode-indicator.tsx
  - apps/web/components/field/sync-status-badge.tsx
  - apps/web/components/layout/app-shell.tsx
  - apps/web/messages/en.json
  - apps/web/messages/ru.json
  - apps/web/messages/kk.json
autonomous: true
requirements: [FIELD-02]
user_setup: []

must_haves:
  truths:
    - "/field route page renders field inspection form"
    - "Photo capture component with file input + camera support"
    - "Voice note recorder with MediaRecorder API + fallback"
    - "GPS correction with geolocation API + manual lat/lon override"
    - "Field mode indicator in header showing online/offline + pending count"
    - "Sync status badge per record (pending/syncing/confirmed/failed/conflict)"
    - "All field UI is trilingual (RU/KK/EN)"
    - "Build passes"
  artifacts:
    - path: "apps/web/components/field/field-inspection-form.tsx"
      provides: "Main field inspection form with photo, voice, GPS capture"
      contains: "useLiveQuery"
    - path: "apps/web/app/[locale]/field/page.tsx"
      provides: "Field mode page route"
      contains: "FieldInspectionForm"
---

## Phase Goal

Field capture UI for offline inspection forms.

<objective>
Create the /field route with comprehensive inspection form supporting photo capture, voice recording, GPS correction, and structured findings. Add field mode indicator to header. All UI trilingual.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create field capture components + /field page + header indicator</name>
  <files>
    apps/web/app/[locale]/field/page.tsx,
    apps/web/components/field/field-inspection-form.tsx,
    apps/web/components/field/photo-capture.tsx,
    apps/web/components/field/voice-note-recorder.tsx,
    apps/web/components/field/gps-correction.tsx,
    apps/web/components/field/field-mode-indicator.tsx,
    apps/web/components/field/sync-status-badge.tsx,
    apps/web/components/layout/app-shell.tsx,
    apps/web/messages/en.json,
    apps/web/messages/ru.json,
    apps/web/messages/kk.json
  </files>
  <behavior>
    - /field page renders with permission guard (inspector, engineer, admin roles)
    - Field inspection form saves to IndexedDB via Dexie
    - Photo capture stores photo metadata + blob in IndexedDB
    - Voice recorder uses MediaRecorder API with graceful fallback
    - GPS correction uses Geolocation API + manual override
    - Field mode indicator shows in header with online/offline status + pending count
    - Sync status badge shows per-record status
    - All text trilingual
    - Build passes
  </behavior>
  <action>
Create all field components, /field page, modify app-shell for indicator, add i18n keys.
  </action>
</task>

</tasks>
