---
phase: 05-pwa-field-mode
plan: 04
type: execute
wave: 2
depends_on: ["05-02", "05-03"]
files_modified:
  - apps/web/components/field/sync-queue-panel.tsx
  - apps/web/components/field/conflict-resolution-dialog.tsx
  - apps/web/components/field/voice-transcription-status.tsx
  - apps/web/app/[locale]/field/page.tsx
  - apps/web/tests/field-db.test.ts
  - apps/web/tests/sync-engine.test.ts
autonomous: true
requirements: [FIELD-03, FIELD-04, FIELD-05]
user_setup: []

must_haves:
  truths:
    - "Sync queue panel lists all pending/failed/conflict records with status badges"
    - "Conflict resolution dialog shows field-by-field comparison with accept local/server/merge"
    - "Voice transcription status component shows pending/complete states"
    - "Unit tests for Dexie database and sync engine pass"
    - "Build passes"
  artifacts:
    - path: "apps/web/components/field/sync-queue-panel.tsx"
      provides: "Panel showing all sync queue entries with status"
      contains: "useLiveQuery"
    - path: "apps/web/components/field/conflict-resolution-dialog.tsx"
      provides: "Dialog for resolving field-level merge conflicts"
      contains: "conflict"
    - path: "apps/web/tests/field-db.test.ts"
      provides: "Unit tests for Dexie database operations"
      contains: "describe"
    - path: "apps/web/tests/sync-engine.test.ts"
      provides: "Unit tests for sync engine and conflict resolution"
      contains: "describe"
---

## Phase Goal

Sync status UI, conflict resolution, voice transcription status, and tests.

<objective>
Create sync queue panel, conflict resolution dialog, voice transcription status component. Wire sync panel into /field page. Write unit tests for Dexie db and sync engine.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create sync queue panel + conflict dialog + voice transcription status + tests</name>
  <files>
    apps/web/components/field/sync-queue-panel.tsx,
    apps/web/components/field/conflict-resolution-dialog.tsx,
    apps/web/components/field/voice-transcription-status.tsx,
    apps/web/app/[locale]/field/page.tsx,
    apps/web/tests/field-db.test.ts,
    apps/web/tests/sync-engine.test.ts
  </files>
  <behavior>
    - Sync queue panel uses useLiveQuery to reactively show sync queue entries
    - Each entry shows structure ID, type, timestamp, sync status badge
    - Conflict dialog shows field-by-field comparison (local vs server)
    - User can accept local, accept server, or merge per field
    - Voice transcription status shows pending/complete with text preview
    - Unit tests cover Dexie CRUD, sync engine processing, conflict resolution
    - Build passes, tests pass
  </behavior>
  <action>
Create sync queue panel, conflict resolution dialog, voice transcription status. Wire into /field page. Create unit tests.
  </action>
</task>

</tasks>
