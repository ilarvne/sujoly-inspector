---
phase: 05-pwa-field-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/package.json
  - apps/web/next.config.ts
  - apps/web/app/sw.ts
  - apps/web/public/manifest.json
  - apps/web/app/[locale]/layout.tsx
  - apps/web/app/[locale]/~offline/page.tsx
  - apps/web/components/pwa/sw-register.tsx
  - apps/web/messages/en.json
  - apps/web/messages/ru.json
  - apps/web/messages/kk.json
  - apps/web/lib/constants.ts
autonomous: true
requirements: [FIELD-01]
user_setup: []

must_haves:
  truths:
    - "Serwist packages installed: @serwist/next, serwist"
    - "next.config.ts wrapped with Serwist config (swSrc=app/sw.ts, swDest=public/sw.js)"
    - "Service worker source at app/sw.ts with defaultCache runtime caching"
    - "PWA manifest.json in public/ with name, icons, display standalone, theme color"
    - "SerwistProvider wrapping app in layout.tsx"
    - "PWA metadata (manifest, appleWebApp, viewport themeColor) in layout"
    - "Offline fallback page at /[locale]/~offline"
    - "SW registration component at components/pwa/sw-register.tsx"
    - "i18n namespaces added: pwa, field, sync to all 3 message files"
    - "Field route added to nav items and constants"
    - "Build succeeds with all PWA changes"
  artifacts:
    - path: "apps/web/app/sw.ts"
      provides: "Service worker source with Serwist defaultCache"
      contains: "defaultCache"
    - path: "apps/web/public/manifest.json"
      provides: "PWA manifest for installable app"
      contains: "display"
    - path: "apps/web/components/pwa/sw-register.tsx"
      provides: "Client component for SW registration"
      contains: "SerwistProvider"
  key_links:
    - from: "apps/web/next.config.ts"
      to: "apps/web/app/sw.ts"
      via: "Serwist config compiles sw.ts to public/sw.js during build"
      pattern: "swSrc"
---

## Phase Goal

Installable PWA with service worker for offline access.

<objective>
Set up Serwist service worker, PWA manifest, offline fallback page, and all i18n keys for Phase 5. This is the foundation that Plans 02-04 build upon.
</objective>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Serwist + create SW + manifest + configure next.config</name>
  <files>
    apps/web/package.json,
    apps/web/next.config.ts,
    apps/web/app/sw.ts,
    apps/web/public/manifest.json,
    apps/web/components/pwa/sw-register.tsx,
    apps/web/app/[locale]/layout.tsx,
    apps/web/app/[locale]/~offline/page.tsx,
    apps/web/lib/constants.ts
  </files>
  <behavior>
    - @serwist/next and serwist installed
    - next.config.ts wraps config with Serwist
    - app/sw.ts has Serwist service worker with defaultCache
    - public/manifest.json has PWA manifest
    - layout.tsx has SerwistProvider, manifest metadata, viewport themeColor
    - Offline fallback page exists
    - /field route added to navItems
    - Build passes
  </behavior>
  <action>
Install Serwist packages:
```bash
cd apps/web && npm install @serwist/next serwist
```

Create app/sw.ts, public/manifest.json, components/pwa/sw-register.tsx, app/[locale]/~offline/page.tsx. Modify next.config.ts, layout.tsx, constants.ts. Add i18n keys to all 3 message files.
  </action>
</task>

</tasks>
